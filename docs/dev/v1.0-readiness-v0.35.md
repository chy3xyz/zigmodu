# v0.35.0 → v1.0 差距评估

> **取代 [`v1.0-readiness-v0.32.md`](v1.0-readiness-v0.32.md)**（那一份是 v0.32.0 / 09-21 的快照，
> 保留在 `docs/dev/` 原地，因为源码注释按路径引用它）。口径与它一致：**逐条写明"谁验证的"**，
> 判定只认代码里的证据（`文件:行` / 测试名 / 一条可复现的命令），文档自己的旧结论一律**复核不采信**。
>
> 方法：v0.32 那 17 条逐条对着 **v0.35.0**（`4fcf663`，工作树干净）的代码复核，四个**只读**子代理
> 分工 + 主代理 grep 交叉验证，**未跑任何构建**。局限见 §八 —— 这份复核和它复核的那些结论**出自
> 同一批 AI**，这本身就是下面 B-14 那条差距。

## 一、一句话

**功能 + 安全完成度 ≈ 88%；"能称为 1.0" 的完成度 ≈ 70%。** 差的那 30% 里几乎没有一条是"还没写
功能"，是三件事：**Raft 侧身份绑定（A-1）、混合版本对跑（B-11）、独立验证（B-14）**。

v0.32 那份写的是"功能+安全 ≈ 85%，而 1.0 的门槛在证据（B 组）"。这一轮的移动**几乎全在 B 组**：
soak 覆盖、fuzz、bench 证据、走私端到端四条**已关**，A 组只动了子项。

## 二、逐条复核（v0.32 的 17 条）

### A 组：写代码能关的（9 条）—— **0 已关 · 4 部分 · 5 未关**

| # | 条目 | 今天 | 证据 |
|---|------|------|------|
| A-1 | Raft 入站不绑定身份 | **部分** | L1 逐帧 HMAC + L2 成员校验都在（`cluster/RaftElection.zig:525`/`:589`/`:1153` 的 `peerId(req) == null` 拒收；单测 `with a cluster_secret, a loopback AppendEntries round-trip is signed end to end`），但**总线那套每节点凭证没推广过来**：Raft 仍是一把 `cluster_secret`（`RaftElection.zig:255`、`RaftTransport.zig:877`），`bindInbound`/`bindOutbound` 只在 `core/DistributedEventBus.zig`。**持 PSK 者可冒充任意节点 —— 与 v0.32 时同形** |
| A-2 | 无加密（帧明文） | 未关 | `core/cluster/TlsTransport.zig:5-8` 文件头自述"生产用边车"；`TlsConfig` 全仓**无消费者**；`RaftTransport`/`DistributedEventBus` 走裸 socket |
| A-3 | 无密钥轮换 / 撤销 | 未关 | 集群面 `rotate\|revok\|kid` **零命中**；`setOwnKey`/`setPeerKey` 只能覆盖写、**没有 remove**；`JwksKeyRing` 只服务 HTTP JWT |
| A-4 | Raft 日志无界增长 | **部分** | `compactLog(up_to_index, snapshot_bytes)` 在（`RaftElection.zig:1096`）但**生产零调用**（唯一调用点是测试 `:2195`）、无 leader 侧发送快照的路径、无自动阈值。已修的那半是入站条目数上界（`:637-656`，测试 `:2330`） |
| A-5 | 非阻塞 IO（ConnPool/libpq 占 pooled worker） | **部分** | 等待被切成 50 ms 段可取消（`sqlx/sqlx.zig:4577-4586`）、默认 `query_timeout_ms = 30000`（`:4861`）、连接建立硬编码 10 s（`:2288`）；**没有异步 IO**，`query_timeout_ms = 0` 时仍无界。真正的出口是 `.blocking` 声明 —— 机制在（A-6），**没有任何 API 会自动把一个 sqlx 调用搬过去** |
| A-6 | 执行层剩余 | **部分** | 已关的子项：StopPolicy ✅、监督树 ✅（`runtime/supervisor.zig`）、affinity **原语** ✅（`runtime/affinity.zig`，Linux 真 pin、macOS/Windows 返回 `error.Unsupported`，`.affinity` 到 `spawn` 仍不做）、**µs 级 `PrecisionTimer`** ✅（p50 0 ns / p99 1 µs）、**Replay → WAL** ✅（`runtime/delivery_log.zig` 的 `ZDL1` 段文件 + `recorder.zig` 的 `ReplayFromLog`）。仍开：**公平性加权与优先级**（`grep priority\|weight src/runtime/scheduler.zig runtime.zig` → **0 命中**，`docs/RUNTIME.md` §12.7/§12.15 明说缓做）、**deterministic runtime**（无 `DeterministicMode`，`recorder.zig:88-90` 自述无进程级确定性）、**Distributed Worker 统一** |
| A-7 | `ws_uring` 只在 Linux 生效 | 未关 | 解析器已并轨到共享校验器、两个真缺陷已修（第 54 批），但**真实 io_uring 行为只能在 Linux 上验**，本机（macOS）验不了 |
| A-8 | `ConcurrentError` 分支树内不可测 | 未关 | `testing.io` 的 `concurrent_limit = unlimited`，树内构造不出该分支 |
| A-9 | 混合版本硬切 | 未关 | `src/soak_cluster.zig:18-21` 明写 "mixed-version (rolling-upgrade) pairs are **out of scope here**" |

### B 组：要"证据"的（6 条）—— **4 已关 · 2 未关**（这才是 1.0 的门槛）

| # | 条目 | 今天 | 证据 |
|---|------|------|------|
| B-10 | cluster/bus 没有长时覆盖 | **已关** | `src/soak_cluster.zig`：同进程 3 节点 `ClusterBootstrap` + 真 `RaftTransport` + 每节点 bus 凭证握手，5 组断言（seq 无洞无重 / leader 稳定 / 三份日志逐字节一致 / fd+RSS+线程不增长 / teardown 回收）；夜间 `soak-cluster`（`ci.yml:807`）+ 每次 push 的 `soak-smoke` 小预算（`ci.yml:200`）。**如实说**：它是并发正确性 soak，**不是 24 h 长跑**（文件头 `:17-20`） |
| B-11 | 混合版本从未对跑 | 未关 | 只有"旧帧被拒"的**同进程**单测（`RaftTransport.zig:1451`、`DistributedEventBus.zig:4044`）；**从未真跑过新旧两个二进制** |
| B-12 | 没有 fuzz | **已关** | 3 个 fuzz 块（`api/Server.zig:6460`、`cluster/RaftTransport.zig:1268`、`DistributedEventBus.zig:4865`）+ `scripts/check-production.sh` 的 `FUZZ_ROOTS` 双向 ratchet（第 60 批；有牙齿：往 `core/Time.zig` 写一句 fuzz 字样即红）。**如实说**：push/PR 上只跑"声明 ratchet"，**真正执行** fuzz 的是夜间 job 的 `Fuzz (bounded)` 步（`ci.yml:831-867`） |
| B-13 | benchmark 证据薄 | **已关** | `scripts/check-bench.sh`：`THRESHOLD=2.0` + 32 条 CI 基线（`bench-baseline.ci.json`）+ 32 条 `max_alloc_per_op` 预算；CI `benchmark` job（`ci.yml:869`，master push 限定）。`TimerWheel x100K` 的双峰**没有消除**，但被降级成"候选回归 + `HOST-MOVED`/`RE-RUN-BEFORE-FIX` 诊断"（`check-bench.sh:213-253`） |
| B-14 | 没有独立验证 | 未关 | 所有"已闭环"仍是同一批 AI 自证 —— **包括这一份**（见 §八） |
| B-15 | CL/TE 走私是静态推导 | **已关** | `examples/production-deploy/smuggling-e2e/`：真 nginx（buffering on/off）+ 5 条载荷 + 4 条对照。未覆盖：Envoy、HTTP/2 前端、TLS 终结层 |

### C 组：写代码关不掉的（2 条）

| # | 条目 | 今天 | 读数 |
|---|------|------|------|
| C-16 | 治理 | 仍在 | 879 提交，作者串：`Antigravity Agent` **629** / `neox33` 220 / `ZigModu Developer` 29 / **`chy3xyz`（仓库所有者）1**；`gh api`：stars **3**、forks **0**、open issues **0**、外部贡献者 0 |
| C-17 | 可持续性 | 仍在 | 一个只由 AI 迭代的 17 万行框架（`src/` 276 文件 / 168,975 行），"维护者消失"的风险不因多修几个 bug 而下降 |

## 三、分域完成度（判断，依据是上一节的条目表）

| 域 | 完成度 | 距 1.0 还差什么 |
|---|---|---|
| Application（模块 / DI / HTTP / DB / 安全） | **~95%** | 基本无阻塞项；本轮还在加硬（每条写路径有上界、H2 大响应、热路径分配契约） |
| Runtime 原语 | **~90%** | 公平性加权 / 优先级、deterministic runtime、Distributed Worker 统一 |
| 集群（Raft / bus） | **~70%** | Raft 每节点身份、轮换/撤销、明文→TLS、日志压缩策略、混合版本 |
| 可验证性 / 证据 | **~75%** | **混合版本对跑、独立验证**、小时级持续长跑 |
| 量化 / 低延迟 | **~60%** | affinity 未到 `spawn`、无 NUMA、无 priority、determinism 未闭环（µs 计时与 WAL replay 已有） |
| 治理 / 可持续性（外部信任视角） | **~10%** | 不是技术能修的 |

## 四、本轮（v0.33–v0.35）跨过的

**证据面**：B-10 soak-cluster · B-12 fuzz + 声明 ratchet · B-13 bench 基线与分配预算 ·
B-15 真实 nginx 走私端到端。
**能力面**：Replay → WAL/段文件 + `ReplayFromLog` · µs 级 `PrecisionTimer` · 阻塞池分离
（`Application.withBlockingThreads`）+ 阻塞池指标 · HTTPS 出站池化 · 热路径零分配**契约测试**
（`src/runtime/alloc_contract_test.zig`，6 条断言精确 0）。
**正确性面**：每条服务端写路径都有上界（H1 缓冲 / streaming / SSE / H2 帧 / WS 帧 / 扩展层 WS / WebMonitor）·
H2 大响应体不再被拒（真因：整段 body 编成一个 DATA 帧）· H2 新流窗口按对端 SETTINGS ·
写失败粘住（`ConnWriter`/`BoundedWriter`）· Raft 选举两条缺陷 · 总线定长帧 + 身份握手 · `ws_uring` 两缺陷。

## 五、判断

```text
按"功能 + 安全完成度"      ≈ 1.0 的 88%
   剩下的 12% 里
   ├── A 组（9 条，其中 5 条有界可做）      → 一条条做，可做完
   └── B-11 / B-14（对跑、独立人）          → 这才是门槛
按"能称为 1.0"             ≈ 70%
```

**把 A 组全做完也不会让它变成 1.0。** 1.0 意味着"**可证明**它在生产里站得住"，而证明需要的是
**有人（或某种机制）能推翻我们**：混合版本对跑、独立验证、以及小时级而不是分钟级的持续运行。

### 如果只能做一件

**混合版本对跑（B-11）。**

v0.32 那份的答案是"soak + 混合版本对跑"，**soak 已经做了**（B-10 已关），剩下这一件：

- 它是**唯一**能证伪 cluster/bus 那批改动的实验（v0.32 的原话是"soak 不会，它跑的是另一个子系统"——
  现在 soak 跑的是同一个子系统了，但它**明确不管**混合版本）；
- 它同时是所有多节点部署的**升级路径**问题：现在是硬切，用户升级必须整体停，而"整体停"这件事
  在 3 节点 Raft 上意味着**失去多数派**；
- 它的产物是一个可复现的脚本（旧二进制 + 新二进制对跑，断言"要么拒绝并说清楚，要么正确降级"），
  而不是一段论证。

排第二的是 **A-1（Raft 侧每节点身份）** —— 总线那套握手是现成模板，而它挡着"集群内任何节点都能
冒充任何节点"这条结论；排第三是 **B-14（独立验证）**，它便宜（把复现脚本交给第二个人/另一套工具跑），
但它才是"自证"与"被验证"的分界。

## 六、各条"未验证"的现状（照抄口径，不粉饰）

| 条目 | 现状 |
|---|---|
| 混合版本（B-11） | **从未对跑** |
| 独立验证（B-14） | **从未有过**（本文件也在其中） |
| `ws_uring` 真实行为（A-7） | **Linux 才能验**，本机没验 |
| `ConcurrentError` 分支（A-8） | **树内不可测**（`concurrent_limit = unlimited`） |
| Windows 上的出站 connect 超时 | `RaftTransport.connectTimeout` 在 Windows **回落无界**（`RaftTransport.zig:452-458`） |
| HTTPS 连接复用 | 有池、有单元证据（`https_clients_created == 1`），**loopback 复用没有端到端观测**（自签对端不在系统信任库里） |
| `TimerWheel x100K` | 双峰仍在，**判定不修**（扩容价），降级为候选回归 + 诊断 |
| 双实例 `permissionGateWith` | 构造性论证，无对拍 |
| CL/TE 走私（B-15） | nginx 拓扑已闭环；**Envoy / H2 前端 / TLS 终结层未覆盖** |

## 七、本次顺带清掉/留下的文档对账

复核过程中发现 5 处**文档与代码不符**（第 64 批清掉了 4 处、留下 1 处待办）：

| 位置 | 原写法 | 实测 |
|---|---|---|
| `src/runtime/recorder.zig:81-82` | "从字节重放是**下一刀**" | 已实现（`ReplayFromLog`，§13.10）→ 已改 |
| `docs/RUNTIME.md` §12.7 | "**不做 µs 级 timer**" | µs 级定时器已落地（`PrecisionTimer`，§12.15）；仍无独立 `LowLatencyClock` **类型** → 已改 |
| `docs/RUNTIME.md` §12 | "注册 **19** 条 `zigmodu_runtime_*`" | **25** 条（13 通用 + 6 CPU 池 + 6 阻塞池）→ 已改 |
| `CHANGELOG.md`（阻塞池那批） | "从 25 条变 31 条" | 该提交前后实测 **17 → 25**，今天仍 25 → 已加修正注 |
| `docs/dev/cluster-auth-design.md` | 标题"集群入站零认证 —— **未实现**" | L1/L2 已实现；**Raft 身份绑定**仍未实现 → 标题与 `docs/dev/README.md` 那行已改 |
| `docs/dev/v1.0-gap.md` | "出站 dial **无界**" | 已由 v0.34.0 第 29 批关掉（`RaftTransport.connectTimeout`）；Windows 仍无界 → 已加注 |

**留一条待办**：`docs/RUNTIME.md` §13.9 的决策记录与 §13.10 的实现状态并存是**有意的**
（前者是决策、后者是状态），但读者容易只读到前者 —— 已在 §13.9 D5 加了一行指回 §13.10。

## 八、这份复核自己的局限

1. **出自同一批 AI。** 上面每一条"已关"都是 AI 读代码 + AI 判定的结果，与 v0.32 那份同源。
   这正是 B-14 未关的含义 —— 本文件不是它的反面证据，而是它的又一例。
2. **只读，没有跑构建。** 判定依据是代码/测试名/脚本内容，不是"我跑了一遍"。
   少数判定（B-12 的 fuzz 真跑时机、B-13 的 CI job 触发）依赖 `.github/workflows/*.yml`
   的静态内容，**无法在树内验证某次 run 的实际结论**。
3. **平台读数来自 API**（stars/forks/贡献者），不是仓库里的证据，会随时间变。
4. **"完成度"是我的判断**，不是可加总的分母；它的依据只有 §二 那张逐条表。
