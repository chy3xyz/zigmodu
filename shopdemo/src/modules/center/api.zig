//! Auto-generated HTTP API for module: center
//! Generated by zmodu orm — uses `zigmodu.http_server` + `ctx.bindJson` / `ctx.jsonStruct`.

const std = @import("std");
const zigmodu = @import("zigmodu");
const service = @import("service.zig");
const model = @import("model.zig");

pub const CenterApi = struct {
    service: *service.CenterService,

    pub fn init(service_ptr: *service.CenterService) CenterApi {
        return .{ .service = service_ptr };
    }

    pub fn registerRoutes(self: *CenterApi, group: *zigmodu.http_server.RouteGroup) !void {
        try group.get("/zmodu_center_menus", listZmoduCenterMenus, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_center_menus/:id", getZmoduCenterMenu, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_center_menus", createZmoduCenterMenu, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_center_menus/:id", updateZmoduCenterMenu, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_center_menus/:id", deleteZmoduCenterMenu, @ptrCast(@alignCast(self)));
    }

    fn listZmoduCenterMenus(ctx: *zigmodu.http_server.Context) !void {
        const self: *CenterApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[center] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[center] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduCenterMenus(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduCenterMenu(ctx: *zigmodu.http_server.Context) !void {
        const self: *CenterApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[center] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[center] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduCenterMenu(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduCenterMenu(ctx: *zigmodu.http_server.Context) !void {
        const self: *CenterApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduCenterMenu) catch {
            std.log.warn("[center] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduCenterMenu(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduCenterMenu(ctx: *zigmodu.http_server.Context) !void {
        const self: *CenterApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduCenterMenu) catch {
            std.log.warn("[center] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduCenterMenu(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduCenterMenu(ctx: *zigmodu.http_server.Context) !void {
        const self: *CenterApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[center] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduCenterMenu(id);
        try ctx.json(204, "");
    }

};
