//! Auto-generated service for module: center
//! Generated by zmodu orm — templates: tools/zmodu/src/templates/orm/sqlx/
//!
//! ╔═══════════════════════════════════════════════════════════╗
//! ║  AI Metadata: module=center | layer=service      ║
//! ║  role=CRUD delegation | extends=service_ext.zig           ║
//! ╚═══════════════════════════════════════════════════════════╝

const std = @import("std");
const zigmodu = @import("zigmodu");
const model = @import("model.zig");
const persistence = @import("persistence.zig");

pub const CenterService = struct {
    persistence: *persistence.CenterPersistence,
    // EventBus — uncomment to enable event-driven communication:
    // event_bus: zigmodu.TypedEventBus(CenterEvent),

    pub fn init(persistence_ptr: *persistence.CenterPersistence) CenterService {
        return .{ .persistence = persistence_ptr };
    }

    // Event hooks (uncomment and define event types to enable):
    // fn afterCreate(self: *CenterService, entity: anytype) !void {
    //     try self.event_bus.publish(.{ .id = entity.id });
    // }
    // fn afterUpdate(self: *CenterService, entity: anytype) !void {
    //     try self.event_bus.publish(.{ .id = entity.id });
    // }
    // fn afterDelete(self: *CenterService, id: i64) !void {
    //     try self.event_bus.publish(.{ .id = id });
    // }

    pub fn listZmoduCenterMenus(self: *CenterService, page: usize, size: usize) !zigmodu.orm.PageResult(model.ZmoduCenterMenu) {
        var repo = self.persistence.zmoduCenterMenuRepo();
        return try repo.findPage(page, size);
    }

    pub fn getZmoduCenterMenu(self: *CenterService, id: i64) !?model.ZmoduCenterMenu {
        var repo = self.persistence.zmoduCenterMenuRepo();
        return try repo.findById(id);
    }

    pub fn createZmoduCenterMenu(self: *CenterService, entity: model.ZmoduCenterMenu) !model.ZmoduCenterMenu {
        var repo = self.persistence.zmoduCenterMenuRepo();
        return try repo.insert(entity);
    }

    pub fn updateZmoduCenterMenu(self: *CenterService, entity: model.ZmoduCenterMenu) !void {
        var repo = self.persistence.zmoduCenterMenuRepo();
        return try repo.update(entity);
    }

    pub fn deleteZmoduCenterMenu(self: *CenterService, id: i64) !void {
        var repo = self.persistence.zmoduCenterMenuRepo();
        return try repo.delete(id);
    }

};
