//! Auto-generated HTTP API for module: printer
//! Generated by zmodu orm — uses `zigmodu.http_server` + `ctx.bindJson` / `ctx.jsonStruct`.

const std = @import("std");
const zigmodu = @import("zigmodu");
const service = @import("service.zig");
const model = @import("model.zig");

pub const PrinterApi = struct {
    service: *service.PrinterService,

    pub fn init(service_ptr: *service.PrinterService) PrinterApi {
        return .{ .service = service_ptr };
    }

    pub fn registerRoutes(self: *PrinterApi, group: *zigmodu.http_server.RouteGroup) !void {
        try group.get("/zmodu_printers", listZmoduPrinters, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_printers/:id", getZmoduPrinter, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_printers", createZmoduPrinter, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_printers/:id", updateZmoduPrinter, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_printers/:id", deleteZmoduPrinter, @ptrCast(@alignCast(self)));
    }

    fn listZmoduPrinters(ctx: *zigmodu.http_server.Context) !void {
        const self: *PrinterApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[printer] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[printer] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduPrinters(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduPrinter(ctx: *zigmodu.http_server.Context) !void {
        const self: *PrinterApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[printer] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[printer] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduPrinter(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduPrinter(ctx: *zigmodu.http_server.Context) !void {
        const self: *PrinterApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduPrinter) catch {
            std.log.warn("[printer] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduPrinter(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduPrinter(ctx: *zigmodu.http_server.Context) !void {
        const self: *PrinterApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduPrinter) catch {
            std.log.warn("[printer] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduPrinter(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduPrinter(ctx: *zigmodu.http_server.Context) !void {
        const self: *PrinterApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[printer] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduPrinter(id);
        try ctx.json(204, "");
    }

};
