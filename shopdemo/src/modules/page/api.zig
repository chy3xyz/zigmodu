//! Auto-generated HTTP API for module: page
//! Generated by zmodu orm — uses `zigmodu.http_server` + `ctx.bindJson` / `ctx.jsonStruct`.

const std = @import("std");
const zigmodu = @import("zigmodu");
const service = @import("service.zig");
const model = @import("model.zig");

pub const PageApi = struct {
    service: *service.PageService,

    pub fn init(service_ptr: *service.PageService) PageApi {
        return .{ .service = service_ptr };
    }

    pub fn registerRoutes(self: *PageApi, group: *zigmodu.http_server.RouteGroup) !void {
        try group.get("/zmodu_pages", listZmoduPages, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_pages/:id", getZmoduPage, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_pages", createZmoduPage, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_pages/:id", updateZmoduPage, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_pages/:id", deleteZmoduPage, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_page_categorys", listZmoduPageCategorys, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_page_categorys/:id", getZmoduPageCategory, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_page_categorys", createZmoduPageCategory, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_page_categorys/:id", updateZmoduPageCategory, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_page_categorys/:id", deleteZmoduPageCategory, @ptrCast(@alignCast(self)));
    }

    fn listZmoduPages(ctx: *zigmodu.http_server.Context) !void {
        const self: *PageApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[page] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[page] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduPages(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduPage(ctx: *zigmodu.http_server.Context) !void {
        const self: *PageApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[page] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[page] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduPage(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduPage(ctx: *zigmodu.http_server.Context) !void {
        const self: *PageApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduPage) catch {
            std.log.warn("[page] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduPage(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduPage(ctx: *zigmodu.http_server.Context) !void {
        const self: *PageApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduPage) catch {
            std.log.warn("[page] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduPage(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduPage(ctx: *zigmodu.http_server.Context) !void {
        const self: *PageApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[page] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduPage(id);
        try ctx.json(204, "");
    }

    fn listZmoduPageCategorys(ctx: *zigmodu.http_server.Context) !void {
        const self: *PageApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[page] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[page] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduPageCategorys(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduPageCategory(ctx: *zigmodu.http_server.Context) !void {
        const self: *PageApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[page] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[page] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduPageCategory(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduPageCategory(ctx: *zigmodu.http_server.Context) !void {
        const self: *PageApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduPageCategory) catch {
            std.log.warn("[page] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduPageCategory(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduPageCategory(ctx: *zigmodu.http_server.Context) !void {
        const self: *PageApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduPageCategory) catch {
            std.log.warn("[page] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduPageCategory(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduPageCategory(ctx: *zigmodu.http_server.Context) !void {
        const self: *PageApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[page] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduPageCategory(id);
        try ctx.json(204, "");
    }

};
