//! Auto-generated service for module: balance
//! Generated by zmodu orm — templates: tools/zmodu/src/templates/orm/sqlx/
//!
//! ╔═══════════════════════════════════════════════════════════╗
//! ║  AI Metadata: module=balance | layer=service      ║
//! ║  role=CRUD delegation | extends=service_ext.zig           ║
//! ╚═══════════════════════════════════════════════════════════╝

const std = @import("std");
const zigmodu = @import("zigmodu");
const model = @import("model.zig");
const persistence = @import("persistence.zig");

pub const BalanceService = struct {
    persistence: *persistence.BalancePersistence,
    // EventBus — uncomment to enable event-driven communication:
    // event_bus: zigmodu.TypedEventBus(BalanceEvent),

    pub fn init(persistence_ptr: *persistence.BalancePersistence) BalanceService {
        return .{ .persistence = persistence_ptr };
    }

    // Event hooks (uncomment and define event types to enable):
    // fn afterCreate(self: *BalanceService, entity: anytype) !void {
    //     try self.event_bus.publish(.{ .id = entity.id });
    // }
    // fn afterUpdate(self: *BalanceService, entity: anytype) !void {
    //     try self.event_bus.publish(.{ .id = entity.id });
    // }
    // fn afterDelete(self: *BalanceService, id: i64) !void {
    //     try self.event_bus.publish(.{ .id = id });
    // }

    pub fn listZmoduBalanceOrders(self: *BalanceService, page: usize, size: usize) !zigmodu.orm.PageResult(model.ZmoduBalanceOrder) {
        var repo = self.persistence.zmoduBalanceOrderRepo();
        return try repo.findPage(page, size);
    }

    pub fn getZmoduBalanceOrder(self: *BalanceService, id: i64) !?model.ZmoduBalanceOrder {
        var repo = self.persistence.zmoduBalanceOrderRepo();
        return try repo.findById(id);
    }

    pub fn createZmoduBalanceOrder(self: *BalanceService, entity: model.ZmoduBalanceOrder) !model.ZmoduBalanceOrder {
        var repo = self.persistence.zmoduBalanceOrderRepo();
        return try repo.insert(entity);
    }

    pub fn updateZmoduBalanceOrder(self: *BalanceService, entity: model.ZmoduBalanceOrder) !void {
        var repo = self.persistence.zmoduBalanceOrderRepo();
        return try repo.update(entity);
    }

    pub fn deleteZmoduBalanceOrder(self: *BalanceService, id: i64) !void {
        var repo = self.persistence.zmoduBalanceOrderRepo();
        return try repo.delete(id);
    }

    pub fn listZmoduBalancePlans(self: *BalanceService, page: usize, size: usize) !zigmodu.orm.PageResult(model.ZmoduBalancePlan) {
        var repo = self.persistence.zmoduBalancePlanRepo();
        return try repo.findPage(page, size);
    }

    pub fn getZmoduBalancePlan(self: *BalanceService, id: i64) !?model.ZmoduBalancePlan {
        var repo = self.persistence.zmoduBalancePlanRepo();
        return try repo.findById(id);
    }

    pub fn createZmoduBalancePlan(self: *BalanceService, entity: model.ZmoduBalancePlan) !model.ZmoduBalancePlan {
        var repo = self.persistence.zmoduBalancePlanRepo();
        return try repo.insert(entity);
    }

    pub fn updateZmoduBalancePlan(self: *BalanceService, entity: model.ZmoduBalancePlan) !void {
        var repo = self.persistence.zmoduBalancePlanRepo();
        return try repo.update(entity);
    }

    pub fn deleteZmoduBalancePlan(self: *BalanceService, id: i64) !void {
        var repo = self.persistence.zmoduBalancePlanRepo();
        return try repo.delete(id);
    }

};
