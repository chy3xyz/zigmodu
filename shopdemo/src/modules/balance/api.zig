//! Auto-generated HTTP API for module: balance
//! Generated by zmodu orm — uses `zigmodu.http_server` + `ctx.bindJson` / `ctx.jsonStruct`.

const std = @import("std");
const zigmodu = @import("zigmodu");
const service = @import("service.zig");
const model = @import("model.zig");

pub const BalanceApi = struct {
    service: *service.BalanceService,

    pub fn init(service_ptr: *service.BalanceService) BalanceApi {
        return .{ .service = service_ptr };
    }

    pub fn registerRoutes(self: *BalanceApi, group: *zigmodu.http_server.RouteGroup) !void {
        try group.get("/zmodu_balance_orders", listZmoduBalanceOrders, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_balance_orders/:id", getZmoduBalanceOrder, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_balance_orders", createZmoduBalanceOrder, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_balance_orders/:id", updateZmoduBalanceOrder, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_balance_orders/:id", deleteZmoduBalanceOrder, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_balance_plans", listZmoduBalancePlans, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_balance_plans/:id", getZmoduBalancePlan, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_balance_plans", createZmoduBalancePlan, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_balance_plans/:id", updateZmoduBalancePlan, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_balance_plans/:id", deleteZmoduBalancePlan, @ptrCast(@alignCast(self)));
    }

    fn listZmoduBalanceOrders(ctx: *zigmodu.http_server.Context) !void {
        const self: *BalanceApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[balance] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[balance] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduBalanceOrders(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduBalanceOrder(ctx: *zigmodu.http_server.Context) !void {
        const self: *BalanceApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[balance] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[balance] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduBalanceOrder(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduBalanceOrder(ctx: *zigmodu.http_server.Context) !void {
        const self: *BalanceApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduBalanceOrder) catch {
            std.log.warn("[balance] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduBalanceOrder(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduBalanceOrder(ctx: *zigmodu.http_server.Context) !void {
        const self: *BalanceApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduBalanceOrder) catch {
            std.log.warn("[balance] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduBalanceOrder(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduBalanceOrder(ctx: *zigmodu.http_server.Context) !void {
        const self: *BalanceApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[balance] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduBalanceOrder(id);
        try ctx.json(204, "");
    }

    fn listZmoduBalancePlans(ctx: *zigmodu.http_server.Context) !void {
        const self: *BalanceApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[balance] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[balance] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduBalancePlans(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduBalancePlan(ctx: *zigmodu.http_server.Context) !void {
        const self: *BalanceApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[balance] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[balance] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduBalancePlan(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduBalancePlan(ctx: *zigmodu.http_server.Context) !void {
        const self: *BalanceApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduBalancePlan) catch {
            std.log.warn("[balance] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduBalancePlan(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduBalancePlan(ctx: *zigmodu.http_server.Context) !void {
        const self: *BalanceApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduBalancePlan) catch {
            std.log.warn("[balance] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduBalancePlan(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduBalancePlan(ctx: *zigmodu.http_server.Context) !void {
        const self: *BalanceApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[balance] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduBalancePlan(id);
        try ctx.json(204, "");
    }

};
