//! Auto-generated HTTP API for module: agent
//! Generated by zmodu orm — uses `zigmodu.http_server` + `ctx.bindJson` / `ctx.jsonStruct`.

const std = @import("std");
const zigmodu = @import("zigmodu");
const service = @import("service.zig");
const model = @import("model.zig");

pub const AgentApi = struct {
    service: *service.AgentService,

    pub fn init(service_ptr: *service.AgentService) AgentApi {
        return .{ .service = service_ptr };
    }

    pub fn registerRoutes(self: *AgentApi, group: *zigmodu.http_server.RouteGroup) !void {
        try group.get("/zmodu_agent_applys", listZmoduAgentApplys, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_agent_applys/:id", getZmoduAgentApply, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_agent_applys", createZmoduAgentApply, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_agent_applys/:id", updateZmoduAgentApply, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_agent_applys/:id", deleteZmoduAgentApply, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_agent_capitals", listZmoduAgentCapitals, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_agent_capitals/:id", getZmoduAgentCapital, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_agent_capitals", createZmoduAgentCapital, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_agent_capitals/:id", updateZmoduAgentCapital, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_agent_capitals/:id", deleteZmoduAgentCapital, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_agent_cashs", listZmoduAgentCashs, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_agent_cashs/:id", getZmoduAgentCash, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_agent_cashs", createZmoduAgentCash, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_agent_cashs/:id", updateZmoduAgentCash, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_agent_cashs/:id", deleteZmoduAgentCash, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_agent_grades", listZmoduAgentGrades, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_agent_grades/:id", getZmoduAgentGrade, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_agent_grades", createZmoduAgentGrade, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_agent_grades/:id", updateZmoduAgentGrade, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_agent_grades/:id", deleteZmoduAgentGrade, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_agent_grade_logs", listZmoduAgentGradeLogs, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_agent_grade_logs/:id", getZmoduAgentGradeLog, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_agent_grade_logs", createZmoduAgentGradeLog, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_agent_grade_logs/:id", updateZmoduAgentGradeLog, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_agent_grade_logs/:id", deleteZmoduAgentGradeLog, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_agent_grade_tasks", listZmoduAgentGradeTasks, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_agent_grade_tasks/:id", getZmoduAgentGradeTask, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_agent_grade_tasks", createZmoduAgentGradeTask, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_agent_grade_tasks/:id", updateZmoduAgentGradeTask, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_agent_grade_tasks/:id", deleteZmoduAgentGradeTask, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_agent_orders", listZmoduAgentOrders, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_agent_orders/:id", getZmoduAgentOrder, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_agent_orders", createZmoduAgentOrder, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_agent_orders/:id", updateZmoduAgentOrder, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_agent_orders/:id", deleteZmoduAgentOrder, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_agent_posters", listZmoduAgentPosters, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_agent_posters/:id", getZmoduAgentPoster, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_agent_posters", createZmoduAgentPoster, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_agent_posters/:id", updateZmoduAgentPoster, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_agent_posters/:id", deleteZmoduAgentPoster, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_agent_products", listZmoduAgentProducts, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_agent_products/:id", getZmoduAgentProduct, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_agent_products", createZmoduAgentProduct, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_agent_products/:id", updateZmoduAgentProduct, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_agent_products/:id", deleteZmoduAgentProduct, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_agent_settings", listZmoduAgentSettings, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_agent_settings/:id", getZmoduAgentSetting, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_agent_settings", createZmoduAgentSetting, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_agent_settings/:id", updateZmoduAgentSetting, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_agent_settings/:id", deleteZmoduAgentSetting, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_agent_users", listZmoduAgentUsers, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_agent_users/:id", getZmoduAgentUser, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_agent_users", createZmoduAgentUser, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_agent_users/:id", updateZmoduAgentUser, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_agent_users/:id", deleteZmoduAgentUser, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_agent_user_products", listZmoduAgentUserProducts, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_agent_user_products/:id", getZmoduAgentUserProduct, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_agent_user_products", createZmoduAgentUserProduct, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_agent_user_products/:id", updateZmoduAgentUserProduct, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_agent_user_products/:id", deleteZmoduAgentUserProduct, @ptrCast(@alignCast(self)));
    }

    fn listZmoduAgentApplys(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[agent] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[agent] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduAgentApplys(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduAgentApply(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[agent] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[agent] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduAgentApply(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduAgentApply(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAgentApply) catch {
            std.log.warn("[agent] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduAgentApply(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduAgentApply(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAgentApply) catch {
            std.log.warn("[agent] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduAgentApply(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduAgentApply(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[agent] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduAgentApply(id);
        try ctx.json(204, "");
    }

    fn listZmoduAgentCapitals(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[agent] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[agent] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduAgentCapitals(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduAgentCapital(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[agent] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[agent] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduAgentCapital(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduAgentCapital(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAgentCapital) catch {
            std.log.warn("[agent] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduAgentCapital(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduAgentCapital(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAgentCapital) catch {
            std.log.warn("[agent] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduAgentCapital(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduAgentCapital(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[agent] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduAgentCapital(id);
        try ctx.json(204, "");
    }

    fn listZmoduAgentCashs(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[agent] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[agent] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduAgentCashs(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduAgentCash(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[agent] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[agent] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduAgentCash(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduAgentCash(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAgentCash) catch {
            std.log.warn("[agent] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduAgentCash(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduAgentCash(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAgentCash) catch {
            std.log.warn("[agent] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduAgentCash(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduAgentCash(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[agent] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduAgentCash(id);
        try ctx.json(204, "");
    }

    fn listZmoduAgentGrades(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[agent] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[agent] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduAgentGrades(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduAgentGrade(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[agent] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[agent] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduAgentGrade(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduAgentGrade(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAgentGrade) catch {
            std.log.warn("[agent] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduAgentGrade(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduAgentGrade(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAgentGrade) catch {
            std.log.warn("[agent] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduAgentGrade(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduAgentGrade(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[agent] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduAgentGrade(id);
        try ctx.json(204, "");
    }

    fn listZmoduAgentGradeLogs(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[agent] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[agent] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduAgentGradeLogs(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduAgentGradeLog(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[agent] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[agent] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduAgentGradeLog(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduAgentGradeLog(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAgentGradeLog) catch {
            std.log.warn("[agent] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduAgentGradeLog(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduAgentGradeLog(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAgentGradeLog) catch {
            std.log.warn("[agent] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduAgentGradeLog(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduAgentGradeLog(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[agent] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduAgentGradeLog(id);
        try ctx.json(204, "");
    }

    fn listZmoduAgentGradeTasks(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[agent] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[agent] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduAgentGradeTasks(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduAgentGradeTask(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[agent] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[agent] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduAgentGradeTask(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduAgentGradeTask(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAgentGradeTask) catch {
            std.log.warn("[agent] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduAgentGradeTask(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduAgentGradeTask(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAgentGradeTask) catch {
            std.log.warn("[agent] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduAgentGradeTask(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduAgentGradeTask(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[agent] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduAgentGradeTask(id);
        try ctx.json(204, "");
    }

    fn listZmoduAgentOrders(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[agent] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[agent] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduAgentOrders(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduAgentOrder(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[agent] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[agent] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduAgentOrder(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduAgentOrder(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAgentOrder) catch {
            std.log.warn("[agent] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduAgentOrder(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduAgentOrder(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAgentOrder) catch {
            std.log.warn("[agent] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduAgentOrder(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduAgentOrder(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[agent] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduAgentOrder(id);
        try ctx.json(204, "");
    }

    fn listZmoduAgentPosters(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[agent] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[agent] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduAgentPosters(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduAgentPoster(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[agent] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[agent] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduAgentPoster(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduAgentPoster(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAgentPoster) catch {
            std.log.warn("[agent] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduAgentPoster(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduAgentPoster(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAgentPoster) catch {
            std.log.warn("[agent] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduAgentPoster(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduAgentPoster(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[agent] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduAgentPoster(id);
        try ctx.json(204, "");
    }

    fn listZmoduAgentProducts(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[agent] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[agent] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduAgentProducts(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduAgentProduct(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[agent] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[agent] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduAgentProduct(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduAgentProduct(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAgentProduct) catch {
            std.log.warn("[agent] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduAgentProduct(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduAgentProduct(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAgentProduct) catch {
            std.log.warn("[agent] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduAgentProduct(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduAgentProduct(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[agent] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduAgentProduct(id);
        try ctx.json(204, "");
    }

    fn listZmoduAgentSettings(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[agent] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[agent] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduAgentSettings(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduAgentSetting(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[agent] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[agent] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduAgentSetting(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduAgentSetting(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAgentSetting) catch {
            std.log.warn("[agent] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduAgentSetting(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduAgentSetting(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAgentSetting) catch {
            std.log.warn("[agent] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduAgentSetting(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduAgentSetting(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[agent] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduAgentSetting(id);
        try ctx.json(204, "");
    }

    fn listZmoduAgentUsers(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[agent] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[agent] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduAgentUsers(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduAgentUser(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[agent] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[agent] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduAgentUser(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduAgentUser(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAgentUser) catch {
            std.log.warn("[agent] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduAgentUser(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduAgentUser(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAgentUser) catch {
            std.log.warn("[agent] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduAgentUser(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduAgentUser(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[agent] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduAgentUser(id);
        try ctx.json(204, "");
    }

    fn listZmoduAgentUserProducts(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[agent] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[agent] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduAgentUserProducts(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduAgentUserProduct(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[agent] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[agent] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduAgentUserProduct(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduAgentUserProduct(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAgentUserProduct) catch {
            std.log.warn("[agent] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduAgentUserProduct(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduAgentUserProduct(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAgentUserProduct) catch {
            std.log.warn("[agent] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduAgentUserProduct(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduAgentUserProduct(ctx: *zigmodu.http_server.Context) !void {
        const self: *AgentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[agent] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduAgentUserProduct(id);
        try ctx.json(204, "");
    }

};
