//! Auto-generated models for module: user
//! Generated by zmodu orm — templates: tools/zmodu/src/templates/orm/sqlx/
//! Do not edit generated files by hand; change SQL and re-run `zmodu orm`.

const std = @import("std");

pub const ZmoduUser = struct {
    pub const sql_table_name: []const u8 = "zmodu_user";
    user_id: i64,
    open_id: []const u8,
    mpopen_id: ?[]const u8,
    appopen_id: ?[]const u8,
    union_id: ?[]const u8,
    app_user: ?[]const u8,
    reg_source: ?[]const u8,
    nickName: []const u8,
    mobile: ?[]const u8,
    password: []const u8,
    avatarUrl: []const u8,
    gender: i64,
    country: []const u8,
    province: []const u8,
    city: []const u8,
    address_id: i64,
    balance: f64,
    points: f64,
    pay_money: f64,
    expend_money: f64,
    grade_id: i64,
    referee_id: i64,
    total_points: ?f64,
    total_invite: ?i64,
    freeze_money: f64,
    cash_money: f64,
    user_type: i64,
    gift_money: ?i64,
    real_name: ?[]const u8,
    agent_time: i64,
    is_delete: i64,
    app_id: i64,
    create_time: i64,
    update_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("user_id");
        try jws.write(self.user_id);
        try jws.objectField("open_id");
        try jws.write(self.open_id);
        try jws.objectField("mpopen_id");
        if (self.mpopen_id) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("appopen_id");
        if (self.appopen_id) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("union_id");
        if (self.union_id) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("app_user");
        if (self.app_user) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("reg_source");
        if (self.reg_source) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("nickName");
        try jws.write(self.nickName);
        try jws.objectField("mobile");
        if (self.mobile) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("password");
        try jws.write(self.password);
        try jws.objectField("avatarUrl");
        try jws.write(self.avatarUrl);
        try jws.objectField("gender");
        try jws.write(self.gender);
        try jws.objectField("country");
        try jws.write(self.country);
        try jws.objectField("province");
        try jws.write(self.province);
        try jws.objectField("city");
        try jws.write(self.city);
        try jws.objectField("address_id");
        try jws.write(self.address_id);
        try jws.objectField("balance");
        try jws.write(self.balance);
        try jws.objectField("points");
        try jws.write(self.points);
        try jws.objectField("pay_money");
        try jws.write(self.pay_money);
        try jws.objectField("expend_money");
        try jws.write(self.expend_money);
        try jws.objectField("grade_id");
        try jws.write(self.grade_id);
        try jws.objectField("referee_id");
        try jws.write(self.referee_id);
        try jws.objectField("total_points");
        if (self.total_points) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("total_invite");
        if (self.total_invite) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("freeze_money");
        try jws.write(self.freeze_money);
        try jws.objectField("cash_money");
        try jws.write(self.cash_money);
        try jws.objectField("user_type");
        try jws.write(self.user_type);
        try jws.objectField("gift_money");
        if (self.gift_money) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("real_name");
        if (self.real_name) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("agent_time");
        try jws.write(self.agent_time);
        try jws.objectField("is_delete");
        try jws.write(self.is_delete);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.objectField("update_time");
        try jws.write(self.update_time);
        try jws.endObject();
    }
};

pub const ZmoduUserAddress = struct {
    pub const sql_table_name: []const u8 = "zmodu_user_address";
    address_id: i64,
    name: []const u8,
    phone: []const u8,
    province_id: i64,
    city_id: i64,
    region_id: i64,
    district: ?[]const u8,
    detail: []const u8,
    user_id: i64,
    app_id: i64,
    create_time: i64,
    update_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("address_id");
        try jws.write(self.address_id);
        try jws.objectField("name");
        try jws.write(self.name);
        try jws.objectField("phone");
        try jws.write(self.phone);
        try jws.objectField("province_id");
        try jws.write(self.province_id);
        try jws.objectField("city_id");
        try jws.write(self.city_id);
        try jws.objectField("region_id");
        try jws.write(self.region_id);
        try jws.objectField("district");
        if (self.district) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("detail");
        try jws.write(self.detail);
        try jws.objectField("user_id");
        try jws.write(self.user_id);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.objectField("update_time");
        try jws.write(self.update_time);
        try jws.endObject();
    }
};

pub const ZmoduUserBalanceLog = struct {
    pub const sql_table_name: []const u8 = "zmodu_user_balance_log";
    log_id: i64,
    user_id: i64,
    scene: i64,
    money: f64,
    describe: []const u8,
    remark: []const u8,
    app_id: i64,
    create_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("log_id");
        try jws.write(self.log_id);
        try jws.objectField("user_id");
        try jws.write(self.user_id);
        try jws.objectField("scene");
        try jws.write(self.scene);
        try jws.objectField("money");
        try jws.write(self.money);
        try jws.objectField("describe");
        try jws.write(self.describe);
        try jws.objectField("remark");
        try jws.write(self.remark);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.endObject();
    }
};

pub const ZmoduUserCart = struct {
    pub const sql_table_name: []const u8 = "zmodu_user_cart";
    cart_id: i64,
    user_id: i64,
    product_id: i64,
    spec_sku_id: []const u8,
    total_num: i64,
    join_price: f64,
    shop_supplier_id: i64,
    app_id: i64,
    create_time: i64,
    update_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("cart_id");
        try jws.write(self.cart_id);
        try jws.objectField("user_id");
        try jws.write(self.user_id);
        try jws.objectField("product_id");
        try jws.write(self.product_id);
        try jws.objectField("spec_sku_id");
        try jws.write(self.spec_sku_id);
        try jws.objectField("total_num");
        try jws.write(self.total_num);
        try jws.objectField("join_price");
        try jws.write(self.join_price);
        try jws.objectField("shop_supplier_id");
        try jws.write(self.shop_supplier_id);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.objectField("update_time");
        try jws.write(self.update_time);
        try jws.endObject();
    }
};

pub const ZmoduUserCash = struct {
    pub const sql_table_name: []const u8 = "zmodu_user_cash";
    id: i64,
    user_id: i64,
    money: f64,
    pay_type: i64,
    alipay_name: []const u8,
    alipay_account: []const u8,
    bank_name: []const u8,
    bank_account: []const u8,
    bank_card: []const u8,
    apply_status: i64,
    audit_time: i64,
    reject_reason: []const u8,
    real_money: f64,
    cash_ratio: f64,
    batch_id: ?[]const u8,
    out_bill_no: ?[]const u8,
    package_info: ?[]const u8,
    pay_time: i64,
    source: ?[]const u8,
    out_biz_no: ?[]const u8,
    app_id: i64,
    create_time: i64,
    update_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("id");
        try jws.write(self.id);
        try jws.objectField("user_id");
        try jws.write(self.user_id);
        try jws.objectField("money");
        try jws.write(self.money);
        try jws.objectField("pay_type");
        try jws.write(self.pay_type);
        try jws.objectField("alipay_name");
        try jws.write(self.alipay_name);
        try jws.objectField("alipay_account");
        try jws.write(self.alipay_account);
        try jws.objectField("bank_name");
        try jws.write(self.bank_name);
        try jws.objectField("bank_account");
        try jws.write(self.bank_account);
        try jws.objectField("bank_card");
        try jws.write(self.bank_card);
        try jws.objectField("apply_status");
        try jws.write(self.apply_status);
        try jws.objectField("audit_time");
        try jws.write(self.audit_time);
        try jws.objectField("reject_reason");
        try jws.write(self.reject_reason);
        try jws.objectField("real_money");
        try jws.write(self.real_money);
        try jws.objectField("cash_ratio");
        try jws.write(self.cash_ratio);
        try jws.objectField("batch_id");
        if (self.batch_id) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("out_bill_no");
        if (self.out_bill_no) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("package_info");
        if (self.package_info) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("pay_time");
        try jws.write(self.pay_time);
        try jws.objectField("source");
        if (self.source) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("out_biz_no");
        if (self.out_biz_no) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.objectField("update_time");
        try jws.write(self.update_time);
        try jws.endObject();
    }
};

pub const ZmoduUserCoupon = struct {
    pub const sql_table_name: []const u8 = "zmodu_user_coupon";
    user_coupon_id: i64,
    coupon_id: i64,
    name: []const u8,
    color: i64,
    coupon_type: i64,
    reduce_price: f64,
    discount: i64,
    min_price: f64,
    expire_type: i64,
    expire_day: i64,
    start_time: i64,
    end_time: i64,
    apply_range: i64,
    is_expire: i64,
    is_use: i64,
    user_id: i64,
    shop_supplier_id: i64,
    max_price: f64,
    app_id: i64,
    create_time: i64,
    update_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("user_coupon_id");
        try jws.write(self.user_coupon_id);
        try jws.objectField("coupon_id");
        try jws.write(self.coupon_id);
        try jws.objectField("name");
        try jws.write(self.name);
        try jws.objectField("color");
        try jws.write(self.color);
        try jws.objectField("coupon_type");
        try jws.write(self.coupon_type);
        try jws.objectField("reduce_price");
        try jws.write(self.reduce_price);
        try jws.objectField("discount");
        try jws.write(self.discount);
        try jws.objectField("min_price");
        try jws.write(self.min_price);
        try jws.objectField("expire_type");
        try jws.write(self.expire_type);
        try jws.objectField("expire_day");
        try jws.write(self.expire_day);
        try jws.objectField("start_time");
        try jws.write(self.start_time);
        try jws.objectField("end_time");
        try jws.write(self.end_time);
        try jws.objectField("apply_range");
        try jws.write(self.apply_range);
        try jws.objectField("is_expire");
        try jws.write(self.is_expire);
        try jws.objectField("is_use");
        try jws.write(self.is_use);
        try jws.objectField("user_id");
        try jws.write(self.user_id);
        try jws.objectField("shop_supplier_id");
        try jws.write(self.shop_supplier_id);
        try jws.objectField("max_price");
        try jws.write(self.max_price);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.objectField("update_time");
        try jws.write(self.update_time);
        try jws.endObject();
    }
};

pub const ZmoduUserFavorite = struct {
    pub const sql_table_name: []const u8 = "zmodu_user_favorite";
    favorite_id: i64,
    user_id: i64,
    pid: i64,
    type: i64,
    shop_supplier_id: ?i64,
    app_id: i64,
    create_time: i64,
    update_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("favorite_id");
        try jws.write(self.favorite_id);
        try jws.objectField("user_id");
        try jws.write(self.user_id);
        try jws.objectField("pid");
        try jws.write(self.pid);
        try jws.objectField("type");
        try jws.write(self.type);
        try jws.objectField("shop_supplier_id");
        if (self.shop_supplier_id) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.objectField("update_time");
        try jws.write(self.update_time);
        try jws.endObject();
    }
};

pub const ZmoduUserGiftLog = struct {
    pub const sql_table_name: []const u8 = "zmodu_user_gift_log";
    log_id: i64,
    user_id: i64,
    money: i64,
    describe: []const u8,
    remark: []const u8,
    app_id: i64,
    create_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("log_id");
        try jws.write(self.log_id);
        try jws.objectField("user_id");
        try jws.write(self.user_id);
        try jws.objectField("money");
        try jws.write(self.money);
        try jws.objectField("describe");
        try jws.write(self.describe);
        try jws.objectField("remark");
        try jws.write(self.remark);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.endObject();
    }
};

pub const ZmoduUserGrade = struct {
    pub const sql_table_name: []const u8 = "zmodu_user_grade";
    grade_id: i64,
    name: []const u8,
    open_money: ?i64,
    upgrade_money: i64,
    open_points: ?i64,
    upgrade_points: ?i64,
    open_invite: ?i64,
    upgrade_invite: ?i64,
    equity: i64,
    is_default: ?i64,
    remark: ?[]const u8,
    weight: ?i64,
    give_points: ?i64,
    image: []const u8,
    font_color: []const u8,
    is_delete: i64,
    app_id: i64,
    create_time: i64,
    update_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("grade_id");
        try jws.write(self.grade_id);
        try jws.objectField("name");
        try jws.write(self.name);
        try jws.objectField("open_money");
        if (self.open_money) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("upgrade_money");
        try jws.write(self.upgrade_money);
        try jws.objectField("open_points");
        if (self.open_points) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("upgrade_points");
        if (self.upgrade_points) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("open_invite");
        if (self.open_invite) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("upgrade_invite");
        if (self.upgrade_invite) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("equity");
        try jws.write(self.equity);
        try jws.objectField("is_default");
        if (self.is_default) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("remark");
        if (self.remark) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("weight");
        if (self.weight) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("give_points");
        if (self.give_points) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("image");
        try jws.write(self.image);
        try jws.objectField("font_color");
        try jws.write(self.font_color);
        try jws.objectField("is_delete");
        try jws.write(self.is_delete);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.objectField("update_time");
        try jws.write(self.update_time);
        try jws.endObject();
    }
};

pub const ZmoduUserGradeEquity = struct {
    pub const sql_table_name: []const u8 = "zmodu_user_grade_equity";
    equity_id: i64,
    name: []const u8,
    image: []const u8,
    status: i64,
    sort: i64,
    is_delete: i64,
    app_id: i64,
    create_time: i64,
    update_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("equity_id");
        try jws.write(self.equity_id);
        try jws.objectField("name");
        try jws.write(self.name);
        try jws.objectField("image");
        try jws.write(self.image);
        try jws.objectField("status");
        try jws.write(self.status);
        try jws.objectField("sort");
        try jws.write(self.sort);
        try jws.objectField("is_delete");
        try jws.write(self.is_delete);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.objectField("update_time");
        try jws.write(self.update_time);
        try jws.endObject();
    }
};

pub const ZmoduUserGradeLog = struct {
    pub const sql_table_name: []const u8 = "zmodu_user_grade_log";
    log_id: i64,
    user_id: i64,
    old_grade_id: i64,
    new_grade_id: i64,
    change_type: i64,
    remark: ?[]const u8,
    app_id: i64,
    create_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("log_id");
        try jws.write(self.log_id);
        try jws.objectField("user_id");
        try jws.write(self.user_id);
        try jws.objectField("old_grade_id");
        try jws.write(self.old_grade_id);
        try jws.objectField("new_grade_id");
        try jws.write(self.new_grade_id);
        try jws.objectField("change_type");
        try jws.write(self.change_type);
        try jws.objectField("remark");
        if (self.remark) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.endObject();
    }
};

pub const ZmoduUserPointsLog = struct {
    pub const sql_table_name: []const u8 = "zmodu_user_points_log";
    log_id: i64,
    user_id: i64,
    value: f64,
    describe: []const u8,
    remark: []const u8,
    app_id: i64,
    create_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("log_id");
        try jws.write(self.log_id);
        try jws.objectField("user_id");
        try jws.write(self.user_id);
        try jws.objectField("value");
        try jws.write(self.value);
        try jws.objectField("describe");
        try jws.write(self.describe);
        try jws.objectField("remark");
        try jws.write(self.remark);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.endObject();
    }
};

pub const ZmoduUserReferee = struct {
    pub const sql_table_name: []const u8 = "zmodu_user_referee";
    id: i64,
    user_id: i64,
    referee_id: i64,
    type: i64,
    app_id: i64,
    create_time: i64,
    update_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("id");
        try jws.write(self.id);
        try jws.objectField("user_id");
        try jws.write(self.user_id);
        try jws.objectField("referee_id");
        try jws.write(self.referee_id);
        try jws.objectField("type");
        try jws.write(self.type);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.objectField("update_time");
        try jws.write(self.update_time);
        try jws.endObject();
    }
};

pub const ZmoduUserSign = struct {
    pub const sql_table_name: []const u8 = "zmodu_user_sign";
    user_sign_id: i64,
    user_id: i64,
    sign_date: []const u8,
    sign_day: i64,
    days: i64,
    points: ?i64,
    prize: []const u8,
    coupon: ?[]const u8,
    app_id: i64,
    create_time: i64,
    update_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("user_sign_id");
        try jws.write(self.user_sign_id);
        try jws.objectField("user_id");
        try jws.write(self.user_id);
        try jws.objectField("sign_date");
        try jws.write(self.sign_date);
        try jws.objectField("sign_day");
        try jws.write(self.sign_day);
        try jws.objectField("days");
        try jws.write(self.days);
        try jws.objectField("points");
        if (self.points) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("prize");
        try jws.write(self.prize);
        try jws.objectField("coupon");
        if (self.coupon) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.objectField("update_time");
        try jws.write(self.update_time);
        try jws.endObject();
    }
};

pub const ZmoduUserTag = struct {
    pub const sql_table_name: []const u8 = "zmodu_user_tag";
    user_tag_id: i64,
    user_id: i64,
    tag_id: i64,
    app_id: i64,
    create_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("user_tag_id");
        try jws.write(self.user_tag_id);
        try jws.objectField("user_id");
        try jws.write(self.user_id);
        try jws.objectField("tag_id");
        try jws.write(self.tag_id);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.endObject();
    }
};

pub const ZmoduUserTaskLog = struct {
    pub const sql_table_name: []const u8 = "zmodu_user_task_log";
    log_id: i64,
    user_id: i64,
    task_type: []const u8,
    task_time: []const u8,
    points: f64,
    app_id: i64,
    create_time: i64,
    update_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("log_id");
        try jws.write(self.log_id);
        try jws.objectField("user_id");
        try jws.write(self.user_id);
        try jws.objectField("task_type");
        try jws.write(self.task_type);
        try jws.objectField("task_time");
        try jws.write(self.task_time);
        try jws.objectField("points");
        try jws.write(self.points);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.objectField("update_time");
        try jws.write(self.update_time);
        try jws.endObject();
    }
};

pub const ZmoduUserVisit = struct {
    pub const sql_table_name: []const u8 = "zmodu_user_visit";
    visit_id: i64,
    user_id: i64,
    shop_supplier_id: i64,
    product_id: i64,
    content: ?[]const u8,
    visitcode: ?[]const u8,
    app_id: i64,
    create_time: i64,
    update_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("visit_id");
        try jws.write(self.visit_id);
        try jws.objectField("user_id");
        try jws.write(self.user_id);
        try jws.objectField("shop_supplier_id");
        try jws.write(self.shop_supplier_id);
        try jws.objectField("product_id");
        try jws.write(self.product_id);
        try jws.objectField("content");
        if (self.content) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("visitcode");
        if (self.visitcode) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.objectField("update_time");
        try jws.write(self.update_time);
        try jws.endObject();
    }
};

