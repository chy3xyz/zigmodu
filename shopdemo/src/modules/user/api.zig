//! Auto-generated HTTP API for module: user
//! Generated by zmodu orm — uses `zigmodu.http_server` + `ctx.bindJson` / `ctx.jsonStruct`.

const std = @import("std");
const zigmodu = @import("zigmodu");
const service = @import("service.zig");
const model = @import("model.zig");

pub const UserApi = struct {
    service: *service.UserService,

    pub fn init(service_ptr: *service.UserService) UserApi {
        return .{ .service = service_ptr };
    }

    pub fn registerRoutes(self: *UserApi, group: *zigmodu.http_server.RouteGroup) !void {
        try group.get("/zmodu_users", listZmoduUsers, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_users/:id", getZmoduUser, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_users", createZmoduUser, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_users/:id", updateZmoduUser, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_users/:id", deleteZmoduUser, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_user_addresss", listZmoduUserAddresss, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_user_addresss/:id", getZmoduUserAddress, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_user_addresss", createZmoduUserAddress, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_user_addresss/:id", updateZmoduUserAddress, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_user_addresss/:id", deleteZmoduUserAddress, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_user_balance_logs", listZmoduUserBalanceLogs, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_user_balance_logs/:id", getZmoduUserBalanceLog, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_user_balance_logs", createZmoduUserBalanceLog, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_user_balance_logs/:id", updateZmoduUserBalanceLog, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_user_balance_logs/:id", deleteZmoduUserBalanceLog, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_user_carts", listZmoduUserCarts, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_user_carts/:id", getZmoduUserCart, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_user_carts", createZmoduUserCart, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_user_carts/:id", updateZmoduUserCart, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_user_carts/:id", deleteZmoduUserCart, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_user_cashs", listZmoduUserCashs, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_user_cashs/:id", getZmoduUserCash, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_user_cashs", createZmoduUserCash, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_user_cashs/:id", updateZmoduUserCash, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_user_cashs/:id", deleteZmoduUserCash, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_user_coupons", listZmoduUserCoupons, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_user_coupons/:id", getZmoduUserCoupon, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_user_coupons", createZmoduUserCoupon, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_user_coupons/:id", updateZmoduUserCoupon, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_user_coupons/:id", deleteZmoduUserCoupon, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_user_favorites", listZmoduUserFavorites, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_user_favorites/:id", getZmoduUserFavorite, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_user_favorites", createZmoduUserFavorite, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_user_favorites/:id", updateZmoduUserFavorite, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_user_favorites/:id", deleteZmoduUserFavorite, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_user_gift_logs", listZmoduUserGiftLogs, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_user_gift_logs/:id", getZmoduUserGiftLog, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_user_gift_logs", createZmoduUserGiftLog, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_user_gift_logs/:id", updateZmoduUserGiftLog, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_user_gift_logs/:id", deleteZmoduUserGiftLog, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_user_grades", listZmoduUserGrades, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_user_grades/:id", getZmoduUserGrade, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_user_grades", createZmoduUserGrade, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_user_grades/:id", updateZmoduUserGrade, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_user_grades/:id", deleteZmoduUserGrade, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_user_grade_equitys", listZmoduUserGradeEquitys, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_user_grade_equitys/:id", getZmoduUserGradeEquity, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_user_grade_equitys", createZmoduUserGradeEquity, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_user_grade_equitys/:id", updateZmoduUserGradeEquity, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_user_grade_equitys/:id", deleteZmoduUserGradeEquity, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_user_grade_logs", listZmoduUserGradeLogs, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_user_grade_logs/:id", getZmoduUserGradeLog, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_user_grade_logs", createZmoduUserGradeLog, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_user_grade_logs/:id", updateZmoduUserGradeLog, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_user_grade_logs/:id", deleteZmoduUserGradeLog, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_user_points_logs", listZmoduUserPointsLogs, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_user_points_logs/:id", getZmoduUserPointsLog, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_user_points_logs", createZmoduUserPointsLog, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_user_points_logs/:id", updateZmoduUserPointsLog, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_user_points_logs/:id", deleteZmoduUserPointsLog, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_user_referees", listZmoduUserReferees, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_user_referees/:id", getZmoduUserReferee, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_user_referees", createZmoduUserReferee, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_user_referees/:id", updateZmoduUserReferee, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_user_referees/:id", deleteZmoduUserReferee, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_user_signs", listZmoduUserSigns, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_user_signs/:id", getZmoduUserSign, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_user_signs", createZmoduUserSign, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_user_signs/:id", updateZmoduUserSign, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_user_signs/:id", deleteZmoduUserSign, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_user_tags", listZmoduUserTags, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_user_tags/:id", getZmoduUserTag, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_user_tags", createZmoduUserTag, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_user_tags/:id", updateZmoduUserTag, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_user_tags/:id", deleteZmoduUserTag, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_user_task_logs", listZmoduUserTaskLogs, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_user_task_logs/:id", getZmoduUserTaskLog, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_user_task_logs", createZmoduUserTaskLog, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_user_task_logs/:id", updateZmoduUserTaskLog, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_user_task_logs/:id", deleteZmoduUserTaskLog, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_user_visits", listZmoduUserVisits, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_user_visits/:id", getZmoduUserVisit, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_user_visits", createZmoduUserVisit, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_user_visits/:id", updateZmoduUserVisit, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_user_visits/:id", deleteZmoduUserVisit, @ptrCast(@alignCast(self)));
    }

    fn listZmoduUsers(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[user] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[user] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduUsers(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduUser(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[user] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[user] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduUser(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduUser(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduUser) catch {
            std.log.warn("[user] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduUser(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduUser(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduUser) catch {
            std.log.warn("[user] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduUser(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduUser(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[user] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduUser(id);
        try ctx.json(204, "");
    }

    fn listZmoduUserAddresss(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[user] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[user] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduUserAddresss(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduUserAddress(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[user] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[user] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduUserAddress(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduUserAddress(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduUserAddress) catch {
            std.log.warn("[user] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduUserAddress(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduUserAddress(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduUserAddress) catch {
            std.log.warn("[user] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduUserAddress(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduUserAddress(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[user] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduUserAddress(id);
        try ctx.json(204, "");
    }

    fn listZmoduUserBalanceLogs(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[user] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[user] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduUserBalanceLogs(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduUserBalanceLog(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[user] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[user] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduUserBalanceLog(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduUserBalanceLog(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduUserBalanceLog) catch {
            std.log.warn("[user] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduUserBalanceLog(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduUserBalanceLog(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduUserBalanceLog) catch {
            std.log.warn("[user] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduUserBalanceLog(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduUserBalanceLog(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[user] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduUserBalanceLog(id);
        try ctx.json(204, "");
    }

    fn listZmoduUserCarts(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[user] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[user] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduUserCarts(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduUserCart(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[user] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[user] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduUserCart(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduUserCart(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduUserCart) catch {
            std.log.warn("[user] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduUserCart(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduUserCart(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduUserCart) catch {
            std.log.warn("[user] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduUserCart(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduUserCart(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[user] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduUserCart(id);
        try ctx.json(204, "");
    }

    fn listZmoduUserCashs(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[user] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[user] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduUserCashs(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduUserCash(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[user] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[user] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduUserCash(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduUserCash(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduUserCash) catch {
            std.log.warn("[user] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduUserCash(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduUserCash(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduUserCash) catch {
            std.log.warn("[user] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduUserCash(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduUserCash(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[user] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduUserCash(id);
        try ctx.json(204, "");
    }

    fn listZmoduUserCoupons(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[user] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[user] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduUserCoupons(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduUserCoupon(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[user] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[user] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduUserCoupon(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduUserCoupon(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduUserCoupon) catch {
            std.log.warn("[user] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduUserCoupon(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduUserCoupon(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduUserCoupon) catch {
            std.log.warn("[user] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduUserCoupon(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduUserCoupon(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[user] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduUserCoupon(id);
        try ctx.json(204, "");
    }

    fn listZmoduUserFavorites(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[user] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[user] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduUserFavorites(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduUserFavorite(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[user] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[user] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduUserFavorite(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduUserFavorite(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduUserFavorite) catch {
            std.log.warn("[user] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduUserFavorite(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduUserFavorite(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduUserFavorite) catch {
            std.log.warn("[user] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduUserFavorite(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduUserFavorite(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[user] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduUserFavorite(id);
        try ctx.json(204, "");
    }

    fn listZmoduUserGiftLogs(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[user] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[user] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduUserGiftLogs(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduUserGiftLog(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[user] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[user] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduUserGiftLog(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduUserGiftLog(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduUserGiftLog) catch {
            std.log.warn("[user] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduUserGiftLog(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduUserGiftLog(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduUserGiftLog) catch {
            std.log.warn("[user] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduUserGiftLog(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduUserGiftLog(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[user] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduUserGiftLog(id);
        try ctx.json(204, "");
    }

    fn listZmoduUserGrades(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[user] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[user] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduUserGrades(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduUserGrade(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[user] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[user] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduUserGrade(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduUserGrade(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduUserGrade) catch {
            std.log.warn("[user] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduUserGrade(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduUserGrade(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduUserGrade) catch {
            std.log.warn("[user] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduUserGrade(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduUserGrade(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[user] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduUserGrade(id);
        try ctx.json(204, "");
    }

    fn listZmoduUserGradeEquitys(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[user] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[user] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduUserGradeEquitys(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduUserGradeEquity(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[user] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[user] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduUserGradeEquity(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduUserGradeEquity(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduUserGradeEquity) catch {
            std.log.warn("[user] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduUserGradeEquity(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduUserGradeEquity(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduUserGradeEquity) catch {
            std.log.warn("[user] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduUserGradeEquity(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduUserGradeEquity(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[user] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduUserGradeEquity(id);
        try ctx.json(204, "");
    }

    fn listZmoduUserGradeLogs(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[user] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[user] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduUserGradeLogs(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduUserGradeLog(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[user] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[user] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduUserGradeLog(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduUserGradeLog(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduUserGradeLog) catch {
            std.log.warn("[user] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduUserGradeLog(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduUserGradeLog(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduUserGradeLog) catch {
            std.log.warn("[user] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduUserGradeLog(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduUserGradeLog(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[user] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduUserGradeLog(id);
        try ctx.json(204, "");
    }

    fn listZmoduUserPointsLogs(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[user] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[user] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduUserPointsLogs(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduUserPointsLog(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[user] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[user] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduUserPointsLog(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduUserPointsLog(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduUserPointsLog) catch {
            std.log.warn("[user] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduUserPointsLog(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduUserPointsLog(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduUserPointsLog) catch {
            std.log.warn("[user] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduUserPointsLog(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduUserPointsLog(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[user] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduUserPointsLog(id);
        try ctx.json(204, "");
    }

    fn listZmoduUserReferees(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[user] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[user] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduUserReferees(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduUserReferee(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[user] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[user] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduUserReferee(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduUserReferee(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduUserReferee) catch {
            std.log.warn("[user] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduUserReferee(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduUserReferee(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduUserReferee) catch {
            std.log.warn("[user] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduUserReferee(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduUserReferee(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[user] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduUserReferee(id);
        try ctx.json(204, "");
    }

    fn listZmoduUserSigns(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[user] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[user] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduUserSigns(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduUserSign(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[user] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[user] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduUserSign(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduUserSign(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduUserSign) catch {
            std.log.warn("[user] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduUserSign(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduUserSign(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduUserSign) catch {
            std.log.warn("[user] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduUserSign(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduUserSign(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[user] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduUserSign(id);
        try ctx.json(204, "");
    }

    fn listZmoduUserTags(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[user] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[user] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduUserTags(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduUserTag(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[user] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[user] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduUserTag(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduUserTag(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduUserTag) catch {
            std.log.warn("[user] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduUserTag(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduUserTag(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduUserTag) catch {
            std.log.warn("[user] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduUserTag(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduUserTag(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[user] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduUserTag(id);
        try ctx.json(204, "");
    }

    fn listZmoduUserTaskLogs(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[user] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[user] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduUserTaskLogs(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduUserTaskLog(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[user] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[user] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduUserTaskLog(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduUserTaskLog(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduUserTaskLog) catch {
            std.log.warn("[user] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduUserTaskLog(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduUserTaskLog(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduUserTaskLog) catch {
            std.log.warn("[user] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduUserTaskLog(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduUserTaskLog(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[user] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduUserTaskLog(id);
        try ctx.json(204, "");
    }

    fn listZmoduUserVisits(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[user] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[user] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduUserVisits(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduUserVisit(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[user] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[user] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduUserVisit(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduUserVisit(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduUserVisit) catch {
            std.log.warn("[user] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduUserVisit(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduUserVisit(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduUserVisit) catch {
            std.log.warn("[user] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduUserVisit(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduUserVisit(ctx: *zigmodu.http_server.Context) !void {
        const self: *UserApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[user] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduUserVisit(id);
        try ctx.json(204, "");
    }

};
