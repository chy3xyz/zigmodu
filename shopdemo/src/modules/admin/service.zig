//! Auto-generated service for module: admin
//! Generated by zmodu orm — templates: tools/zmodu/src/templates/orm/sqlx/
//!
//! ╔═══════════════════════════════════════════════════════════╗
//! ║  AI Metadata: module=admin | layer=service      ║
//! ║  role=CRUD delegation | extends=service_ext.zig           ║
//! ╚═══════════════════════════════════════════════════════════╝

const std = @import("std");
const zigmodu = @import("zigmodu");
const model = @import("model.zig");
const persistence = @import("persistence.zig");

pub const AdminService = struct {
    persistence: *persistence.AdminPersistence,
    // EventBus — uncomment to enable event-driven communication:
    // event_bus: zigmodu.TypedEventBus(AdminEvent),

    pub fn init(persistence_ptr: *persistence.AdminPersistence) AdminService {
        return .{ .persistence = persistence_ptr };
    }

    // Event hooks (uncomment and define event types to enable):
    // fn afterCreate(self: *AdminService, entity: anytype) !void {
    //     try self.event_bus.publish(.{ .id = entity.id });
    // }
    // fn afterUpdate(self: *AdminService, entity: anytype) !void {
    //     try self.event_bus.publish(.{ .id = entity.id });
    // }
    // fn afterDelete(self: *AdminService, id: i64) !void {
    //     try self.event_bus.publish(.{ .id = id });
    // }

    pub fn listZmoduAdminUsers(self: *AdminService, page: usize, size: usize) !zigmodu.orm.PageResult(model.ZmoduAdminUser) {
        var repo = self.persistence.zmoduAdminUserRepo();
        return try repo.findPage(page, size);
    }

    pub fn getZmoduAdminUser(self: *AdminService, id: i64) !?model.ZmoduAdminUser {
        var repo = self.persistence.zmoduAdminUserRepo();
        return try repo.findById(id);
    }

    pub fn createZmoduAdminUser(self: *AdminService, entity: model.ZmoduAdminUser) !model.ZmoduAdminUser {
        var repo = self.persistence.zmoduAdminUserRepo();
        return try repo.insert(entity);
    }

    pub fn updateZmoduAdminUser(self: *AdminService, entity: model.ZmoduAdminUser) !void {
        var repo = self.persistence.zmoduAdminUserRepo();
        return try repo.update(entity);
    }

    pub fn deleteZmoduAdminUser(self: *AdminService, id: i64) !void {
        var repo = self.persistence.zmoduAdminUserRepo();
        return try repo.delete(id);
    }

};
