//! Auto-generated HTTP API for module: register
//! Generated by zmodu orm — uses `zigmodu.http_server` + `ctx.bindJson` / `ctx.jsonStruct`.

const std = @import("std");
const zigmodu = @import("zigmodu");
const service = @import("service.zig");
const model = @import("model.zig");

pub const RegisterApi = struct {
    service: *service.RegisterService,

    pub fn init(service_ptr: *service.RegisterService) RegisterApi {
        return .{ .service = service_ptr };
    }

    pub fn registerRoutes(self: *RegisterApi, group: *zigmodu.http_server.RouteGroup) !void {
        try group.get("/zmodu_register_records", listZmoduRegisterRecords, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_register_records/:id", getZmoduRegisterRecord, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_register_records", createZmoduRegisterRecord, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_register_records/:id", updateZmoduRegisterRecord, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_register_records/:id", deleteZmoduRegisterRecord, @ptrCast(@alignCast(self)));
    }

    fn listZmoduRegisterRecords(ctx: *zigmodu.http_server.Context) !void {
        const self: *RegisterApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[register] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[register] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduRegisterRecords(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduRegisterRecord(ctx: *zigmodu.http_server.Context) !void {
        const self: *RegisterApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[register] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[register] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduRegisterRecord(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduRegisterRecord(ctx: *zigmodu.http_server.Context) !void {
        const self: *RegisterApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduRegisterRecord) catch {
            std.log.warn("[register] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduRegisterRecord(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduRegisterRecord(ctx: *zigmodu.http_server.Context) !void {
        const self: *RegisterApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduRegisterRecord) catch {
            std.log.warn("[register] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduRegisterRecord(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduRegisterRecord(ctx: *zigmodu.http_server.Context) !void {
        const self: *RegisterApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[register] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduRegisterRecord(id);
        try ctx.json(204, "");
    }

};
