//! Auto-generated service for module: comment
//! Generated by zmodu orm — templates: tools/zmodu/src/templates/orm/sqlx/
//!
//! ╔═══════════════════════════════════════════════════════════╗
//! ║  AI Metadata: module=comment | layer=service      ║
//! ║  role=CRUD delegation | extends=service_ext.zig           ║
//! ╚═══════════════════════════════════════════════════════════╝

const std = @import("std");
const zigmodu = @import("zigmodu");
const model = @import("model.zig");
const persistence = @import("persistence.zig");

pub const CommentService = struct {
    persistence: *persistence.CommentPersistence,
    // EventBus — uncomment to enable event-driven communication:
    // event_bus: zigmodu.TypedEventBus(CommentEvent),

    pub fn init(persistence_ptr: *persistence.CommentPersistence) CommentService {
        return .{ .persistence = persistence_ptr };
    }

    // Event hooks (uncomment and define event types to enable):
    // fn afterCreate(self: *CommentService, entity: anytype) !void {
    //     try self.event_bus.publish(.{ .id = entity.id });
    // }
    // fn afterUpdate(self: *CommentService, entity: anytype) !void {
    //     try self.event_bus.publish(.{ .id = entity.id });
    // }
    // fn afterDelete(self: *CommentService, id: i64) !void {
    //     try self.event_bus.publish(.{ .id = id });
    // }

    pub fn listZmoduComments(self: *CommentService, page: usize, size: usize) !zigmodu.orm.PageResult(model.ZmoduComment) {
        var repo = self.persistence.zmoduCommentRepo();
        return try repo.findPage(page, size);
    }

    pub fn getZmoduComment(self: *CommentService, id: i64) !?model.ZmoduComment {
        var repo = self.persistence.zmoduCommentRepo();
        return try repo.findById(id);
    }

    pub fn createZmoduComment(self: *CommentService, entity: model.ZmoduComment) !model.ZmoduComment {
        var repo = self.persistence.zmoduCommentRepo();
        return try repo.insert(entity);
    }

    pub fn updateZmoduComment(self: *CommentService, entity: model.ZmoduComment) !void {
        var repo = self.persistence.zmoduCommentRepo();
        return try repo.update(entity);
    }

    pub fn deleteZmoduComment(self: *CommentService, id: i64) !void {
        var repo = self.persistence.zmoduCommentRepo();
        return try repo.delete(id);
    }

    pub fn listZmoduCommentImages(self: *CommentService, page: usize, size: usize) !zigmodu.orm.PageResult(model.ZmoduCommentImage) {
        var repo = self.persistence.zmoduCommentImageRepo();
        return try repo.findPage(page, size);
    }

    pub fn getZmoduCommentImage(self: *CommentService, id: i64) !?model.ZmoduCommentImage {
        var repo = self.persistence.zmoduCommentImageRepo();
        return try repo.findById(id);
    }

    pub fn createZmoduCommentImage(self: *CommentService, entity: model.ZmoduCommentImage) !model.ZmoduCommentImage {
        var repo = self.persistence.zmoduCommentImageRepo();
        return try repo.insert(entity);
    }

    pub fn updateZmoduCommentImage(self: *CommentService, entity: model.ZmoduCommentImage) !void {
        var repo = self.persistence.zmoduCommentImageRepo();
        return try repo.update(entity);
    }

    pub fn deleteZmoduCommentImage(self: *CommentService, id: i64) !void {
        var repo = self.persistence.zmoduCommentImageRepo();
        return try repo.delete(id);
    }

};
