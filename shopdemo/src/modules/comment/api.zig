//! Auto-generated HTTP API for module: comment
//! Generated by zmodu orm — uses `zigmodu.http_server` + `ctx.bindJson` / `ctx.jsonStruct`.

const std = @import("std");
const zigmodu = @import("zigmodu");
const service = @import("service.zig");
const model = @import("model.zig");

pub const CommentApi = struct {
    service: *service.CommentService,

    pub fn init(service_ptr: *service.CommentService) CommentApi {
        return .{ .service = service_ptr };
    }

    pub fn registerRoutes(self: *CommentApi, group: *zigmodu.http_server.RouteGroup) !void {
        try group.get("/zmodu_comments", listZmoduComments, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_comments/:id", getZmoduComment, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_comments", createZmoduComment, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_comments/:id", updateZmoduComment, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_comments/:id", deleteZmoduComment, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_comment_images", listZmoduCommentImages, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_comment_images/:id", getZmoduCommentImage, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_comment_images", createZmoduCommentImage, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_comment_images/:id", updateZmoduCommentImage, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_comment_images/:id", deleteZmoduCommentImage, @ptrCast(@alignCast(self)));
    }

    fn listZmoduComments(ctx: *zigmodu.http_server.Context) !void {
        const self: *CommentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[comment] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[comment] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduComments(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduComment(ctx: *zigmodu.http_server.Context) !void {
        const self: *CommentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[comment] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[comment] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduComment(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduComment(ctx: *zigmodu.http_server.Context) !void {
        const self: *CommentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduComment) catch {
            std.log.warn("[comment] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduComment(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduComment(ctx: *zigmodu.http_server.Context) !void {
        const self: *CommentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduComment) catch {
            std.log.warn("[comment] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduComment(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduComment(ctx: *zigmodu.http_server.Context) !void {
        const self: *CommentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[comment] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduComment(id);
        try ctx.json(204, "");
    }

    fn listZmoduCommentImages(ctx: *zigmodu.http_server.Context) !void {
        const self: *CommentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[comment] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[comment] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduCommentImages(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduCommentImage(ctx: *zigmodu.http_server.Context) !void {
        const self: *CommentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[comment] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[comment] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduCommentImage(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduCommentImage(ctx: *zigmodu.http_server.Context) !void {
        const self: *CommentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduCommentImage) catch {
            std.log.warn("[comment] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduCommentImage(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduCommentImage(ctx: *zigmodu.http_server.Context) !void {
        const self: *CommentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduCommentImage) catch {
            std.log.warn("[comment] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduCommentImage(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduCommentImage(ctx: *zigmodu.http_server.Context) !void {
        const self: *CommentApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[comment] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduCommentImage(id);
        try ctx.json(204, "");
    }

};
