//! Auto-generated service for module: return
//! Generated by zmodu orm — templates: tools/zmodu/src/templates/orm/sqlx/
//!
//! ╔═══════════════════════════════════════════════════════════╗
//! ║  AI Metadata: module=return | layer=service      ║
//! ║  role=CRUD delegation | extends=service_ext.zig           ║
//! ╚═══════════════════════════════════════════════════════════╝

const std = @import("std");
const zigmodu = @import("zigmodu");
const model = @import("model.zig");
const persistence = @import("persistence.zig");

pub const ReturnService = struct {
    persistence: *persistence.ReturnPersistence,
    // EventBus — uncomment to enable event-driven communication:
    // event_bus: zigmodu.TypedEventBus(ReturnEvent),

    pub fn init(persistence_ptr: *persistence.ReturnPersistence) ReturnService {
        return .{ .persistence = persistence_ptr };
    }

    // Event hooks (uncomment and define event types to enable):
    // fn afterCreate(self: *ReturnService, entity: anytype) !void {
    //     try self.event_bus.publish(.{ .id = entity.id });
    // }
    // fn afterUpdate(self: *ReturnService, entity: anytype) !void {
    //     try self.event_bus.publish(.{ .id = entity.id });
    // }
    // fn afterDelete(self: *ReturnService, id: i64) !void {
    //     try self.event_bus.publish(.{ .id = id });
    // }

    pub fn listZmoduReturnAddresss(self: *ReturnService, page: usize, size: usize) !zigmodu.orm.PageResult(model.ZmoduReturnAddress) {
        var repo = self.persistence.zmoduReturnAddressRepo();
        return try repo.findPage(page, size);
    }

    pub fn getZmoduReturnAddress(self: *ReturnService, id: i64) !?model.ZmoduReturnAddress {
        var repo = self.persistence.zmoduReturnAddressRepo();
        return try repo.findById(id);
    }

    pub fn createZmoduReturnAddress(self: *ReturnService, entity: model.ZmoduReturnAddress) !model.ZmoduReturnAddress {
        var repo = self.persistence.zmoduReturnAddressRepo();
        return try repo.insert(entity);
    }

    pub fn updateZmoduReturnAddress(self: *ReturnService, entity: model.ZmoduReturnAddress) !void {
        var repo = self.persistence.zmoduReturnAddressRepo();
        return try repo.update(entity);
    }

    pub fn deleteZmoduReturnAddress(self: *ReturnService, id: i64) !void {
        var repo = self.persistence.zmoduReturnAddressRepo();
        return try repo.delete(id);
    }

};
