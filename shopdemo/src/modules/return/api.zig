//! Auto-generated HTTP API for module: return
//! Generated by zmodu orm — uses `zigmodu.http_server` + `ctx.bindJson` / `ctx.jsonStruct`.

const std = @import("std");
const zigmodu = @import("zigmodu");
const service = @import("service.zig");
const model = @import("model.zig");

pub const ReturnApi = struct {
    service: *service.ReturnService,

    pub fn init(service_ptr: *service.ReturnService) ReturnApi {
        return .{ .service = service_ptr };
    }

    pub fn registerRoutes(self: *ReturnApi, group: *zigmodu.http_server.RouteGroup) !void {
        try group.get("/zmodu_return_addresss", listZmoduReturnAddresss, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_return_addresss/:id", getZmoduReturnAddress, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_return_addresss", createZmoduReturnAddress, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_return_addresss/:id", updateZmoduReturnAddress, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_return_addresss/:id", deleteZmoduReturnAddress, @ptrCast(@alignCast(self)));
    }

    fn listZmoduReturnAddresss(ctx: *zigmodu.http_server.Context) !void {
        const self: *ReturnApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[return] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[return] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduReturnAddresss(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduReturnAddress(ctx: *zigmodu.http_server.Context) !void {
        const self: *ReturnApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[return] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[return] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduReturnAddress(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduReturnAddress(ctx: *zigmodu.http_server.Context) !void {
        const self: *ReturnApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduReturnAddress) catch {
            std.log.warn("[return] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduReturnAddress(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduReturnAddress(ctx: *zigmodu.http_server.Context) !void {
        const self: *ReturnApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduReturnAddress) catch {
            std.log.warn("[return] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduReturnAddress(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduReturnAddress(ctx: *zigmodu.http_server.Context) !void {
        const self: *ReturnApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[return] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduReturnAddress(id);
        try ctx.json(204, "");
    }

};
