//! Auto-generated models for module: seckill
//! Generated by zmodu orm — templates: tools/zmodu/src/templates/orm/sqlx/
//! Do not edit generated files by hand; change SQL and re-run `zmodu orm`.

const std = @import("std");

pub const ZmoduSeckillActivity = struct {
    pub const sql_table_name: []const u8 = "zmodu_seckill_activity";
    seckill_activity_id: i64,
    title: []const u8,
    start_time: i64,
    end_time: i64,
    join_end_time: ?i64,
    status: i64,
    time_id: []const u8,
    total_num: i64,
    single_num: i64,
    sort: ?i64,
    app_id: i64,
    is_delete: ?i64,
    create_time: i64,
    update_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("seckill_activity_id");
        try jws.write(self.seckill_activity_id);
        try jws.objectField("title");
        try jws.write(self.title);
        try jws.objectField("start_time");
        try jws.write(self.start_time);
        try jws.objectField("end_time");
        try jws.write(self.end_time);
        try jws.objectField("join_end_time");
        if (self.join_end_time) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("status");
        try jws.write(self.status);
        try jws.objectField("time_id");
        try jws.write(self.time_id);
        try jws.objectField("total_num");
        try jws.write(self.total_num);
        try jws.objectField("single_num");
        try jws.write(self.single_num);
        try jws.objectField("sort");
        if (self.sort) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("is_delete");
        if (self.is_delete) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.objectField("update_time");
        try jws.write(self.update_time);
        try jws.endObject();
    }
};

pub const ZmoduSeckillProduct = struct {
    pub const sql_table_name: []const u8 = "zmodu_seckill_product";
    seckill_product_id: i64,
    product_id: i64,
    limit_num: i64,
    stock: i64,
    seckill_activity_id: i64,
    total_sales: i64,
    sales_initial: ?i64,
    join_num: ?i64,
    status: ?i64,
    sort: ?i64,
    shop_supplier_id: ?i64,
    remark: ?[]const u8,
    state: i64,
    is_delete: ?i64,
    app_id: i64,
    create_time: i64,
    update_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("seckill_product_id");
        try jws.write(self.seckill_product_id);
        try jws.objectField("product_id");
        try jws.write(self.product_id);
        try jws.objectField("limit_num");
        try jws.write(self.limit_num);
        try jws.objectField("stock");
        try jws.write(self.stock);
        try jws.objectField("seckill_activity_id");
        try jws.write(self.seckill_activity_id);
        try jws.objectField("total_sales");
        try jws.write(self.total_sales);
        try jws.objectField("sales_initial");
        if (self.sales_initial) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("join_num");
        if (self.join_num) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("status");
        if (self.status) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("sort");
        if (self.sort) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("shop_supplier_id");
        if (self.shop_supplier_id) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("remark");
        if (self.remark) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("state");
        try jws.write(self.state);
        try jws.objectField("is_delete");
        if (self.is_delete) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.objectField("update_time");
        try jws.write(self.update_time);
        try jws.endObject();
    }
};

pub const ZmoduSeckillProductSku = struct {
    pub const sql_table_name: []const u8 = "zmodu_seckill_product_sku";
    seckill_product_sku_id: i64,
    seckill_product_id: i64,
    seckill_activity_id: ?i64,
    product_id: i64,
    product_sku_id: i64,
    product_attr: ?[]const u8,
    seckill_price: f64,
    seckill_stock: i64,
    product_price: ?f64,
    sales_num: i64,
    app_id: i64,
    create_time: i64,
    update_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("seckill_product_sku_id");
        try jws.write(self.seckill_product_sku_id);
        try jws.objectField("seckill_product_id");
        try jws.write(self.seckill_product_id);
        try jws.objectField("seckill_activity_id");
        if (self.seckill_activity_id) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("product_id");
        try jws.write(self.product_id);
        try jws.objectField("product_sku_id");
        try jws.write(self.product_sku_id);
        try jws.objectField("product_attr");
        if (self.product_attr) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("seckill_price");
        try jws.write(self.seckill_price);
        try jws.objectField("seckill_stock");
        try jws.write(self.seckill_stock);
        try jws.objectField("product_price");
        if (self.product_price) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("sales_num");
        try jws.write(self.sales_num);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.objectField("update_time");
        try jws.write(self.update_time);
        try jws.endObject();
    }
};

pub const ZmoduSeckillTime = struct {
    pub const sql_table_name: []const u8 = "zmodu_seckill_time";
    id: i64,
    title: []const u8,
    start_time: []const u8,
    end_time: []const u8,
    status: i64,
    app_id: i64,
    create_time: i64,
    update_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("id");
        try jws.write(self.id);
        try jws.objectField("title");
        try jws.write(self.title);
        try jws.objectField("start_time");
        try jws.write(self.start_time);
        try jws.objectField("end_time");
        try jws.write(self.end_time);
        try jws.objectField("status");
        try jws.write(self.status);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.objectField("update_time");
        try jws.write(self.update_time);
        try jws.endObject();
    }
};

