//! Auto-generated service for module: spec
//! Generated by zmodu orm — templates: tools/zmodu/src/templates/orm/sqlx/
//!
//! ╔═══════════════════════════════════════════════════════════╗
//! ║  AI Metadata: module=spec | layer=service      ║
//! ║  role=CRUD delegation | extends=service_ext.zig           ║
//! ╚═══════════════════════════════════════════════════════════╝

const std = @import("std");
const zigmodu = @import("zigmodu");
const model = @import("model.zig");
const persistence = @import("persistence.zig");

pub const SpecService = struct {
    persistence: *persistence.SpecPersistence,
    // EventBus — uncomment to enable event-driven communication:
    // event_bus: zigmodu.TypedEventBus(SpecEvent),

    pub fn init(persistence_ptr: *persistence.SpecPersistence) SpecService {
        return .{ .persistence = persistence_ptr };
    }

    // Event hooks (uncomment and define event types to enable):
    // fn afterCreate(self: *SpecService, entity: anytype) !void {
    //     try self.event_bus.publish(.{ .id = entity.id });
    // }
    // fn afterUpdate(self: *SpecService, entity: anytype) !void {
    //     try self.event_bus.publish(.{ .id = entity.id });
    // }
    // fn afterDelete(self: *SpecService, id: i64) !void {
    //     try self.event_bus.publish(.{ .id = id });
    // }

    pub fn listZmoduSpecs(self: *SpecService, page: usize, size: usize) !zigmodu.orm.PageResult(model.ZmoduSpec) {
        var repo = self.persistence.zmoduSpecRepo();
        return try repo.findPage(page, size);
    }

    pub fn getZmoduSpec(self: *SpecService, id: i64) !?model.ZmoduSpec {
        var repo = self.persistence.zmoduSpecRepo();
        return try repo.findById(id);
    }

    pub fn createZmoduSpec(self: *SpecService, entity: model.ZmoduSpec) !model.ZmoduSpec {
        var repo = self.persistence.zmoduSpecRepo();
        return try repo.insert(entity);
    }

    pub fn updateZmoduSpec(self: *SpecService, entity: model.ZmoduSpec) !void {
        var repo = self.persistence.zmoduSpecRepo();
        return try repo.update(entity);
    }

    pub fn deleteZmoduSpec(self: *SpecService, id: i64) !void {
        var repo = self.persistence.zmoduSpecRepo();
        return try repo.delete(id);
    }

    pub fn listZmoduSpecValues(self: *SpecService, page: usize, size: usize) !zigmodu.orm.PageResult(model.ZmoduSpecValue) {
        var repo = self.persistence.zmoduSpecValueRepo();
        return try repo.findPage(page, size);
    }

    pub fn getZmoduSpecValue(self: *SpecService, id: i64) !?model.ZmoduSpecValue {
        var repo = self.persistence.zmoduSpecValueRepo();
        return try repo.findById(id);
    }

    pub fn createZmoduSpecValue(self: *SpecService, entity: model.ZmoduSpecValue) !model.ZmoduSpecValue {
        var repo = self.persistence.zmoduSpecValueRepo();
        return try repo.insert(entity);
    }

    pub fn updateZmoduSpecValue(self: *SpecService, entity: model.ZmoduSpecValue) !void {
        var repo = self.persistence.zmoduSpecValueRepo();
        return try repo.update(entity);
    }

    pub fn deleteZmoduSpecValue(self: *SpecService, id: i64) !void {
        var repo = self.persistence.zmoduSpecValueRepo();
        return try repo.delete(id);
    }

};
