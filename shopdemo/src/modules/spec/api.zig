//! Auto-generated HTTP API for module: spec
//! Generated by zmodu orm — uses `zigmodu.http_server` + `ctx.bindJson` / `ctx.jsonStruct`.

const std = @import("std");
const zigmodu = @import("zigmodu");
const service = @import("service.zig");
const model = @import("model.zig");

pub const SpecApi = struct {
    service: *service.SpecService,

    pub fn init(service_ptr: *service.SpecService) SpecApi {
        return .{ .service = service_ptr };
    }

    pub fn registerRoutes(self: *SpecApi, group: *zigmodu.http_server.RouteGroup) !void {
        try group.get("/zmodu_specs", listZmoduSpecs, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_specs/:id", getZmoduSpec, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_specs", createZmoduSpec, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_specs/:id", updateZmoduSpec, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_specs/:id", deleteZmoduSpec, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_spec_values", listZmoduSpecValues, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_spec_values/:id", getZmoduSpecValue, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_spec_values", createZmoduSpecValue, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_spec_values/:id", updateZmoduSpecValue, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_spec_values/:id", deleteZmoduSpecValue, @ptrCast(@alignCast(self)));
    }

    fn listZmoduSpecs(ctx: *zigmodu.http_server.Context) !void {
        const self: *SpecApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[spec] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[spec] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduSpecs(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduSpec(ctx: *zigmodu.http_server.Context) !void {
        const self: *SpecApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[spec] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[spec] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduSpec(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduSpec(ctx: *zigmodu.http_server.Context) !void {
        const self: *SpecApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSpec) catch {
            std.log.warn("[spec] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduSpec(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduSpec(ctx: *zigmodu.http_server.Context) !void {
        const self: *SpecApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSpec) catch {
            std.log.warn("[spec] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduSpec(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduSpec(ctx: *zigmodu.http_server.Context) !void {
        const self: *SpecApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[spec] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduSpec(id);
        try ctx.json(204, "");
    }

    fn listZmoduSpecValues(ctx: *zigmodu.http_server.Context) !void {
        const self: *SpecApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[spec] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[spec] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduSpecValues(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduSpecValue(ctx: *zigmodu.http_server.Context) !void {
        const self: *SpecApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[spec] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[spec] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduSpecValue(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduSpecValue(ctx: *zigmodu.http_server.Context) !void {
        const self: *SpecApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSpecValue) catch {
            std.log.warn("[spec] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduSpecValue(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduSpecValue(ctx: *zigmodu.http_server.Context) !void {
        const self: *SpecApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSpecValue) catch {
            std.log.warn("[spec] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduSpecValue(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduSpecValue(ctx: *zigmodu.http_server.Context) !void {
        const self: *SpecApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[spec] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduSpecValue(id);
        try ctx.json(204, "");
    }

};
