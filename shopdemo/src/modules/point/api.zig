//! Auto-generated HTTP API for module: point
//! Generated by zmodu orm — uses `zigmodu.http_server` + `ctx.bindJson` / `ctx.jsonStruct`.

const std = @import("std");
const zigmodu = @import("zigmodu");
const service = @import("service.zig");
const model = @import("model.zig");

pub const PointApi = struct {
    service: *service.PointService,

    pub fn init(service_ptr: *service.PointService) PointApi {
        return .{ .service = service_ptr };
    }

    pub fn registerRoutes(self: *PointApi, group: *zigmodu.http_server.RouteGroup) !void {
        try group.get("/zmodu_point_products", listZmoduPointProducts, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_point_products/:id", getZmoduPointProduct, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_point_products", createZmoduPointProduct, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_point_products/:id", updateZmoduPointProduct, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_point_products/:id", deleteZmoduPointProduct, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_point_product_skus", listZmoduPointProductSkus, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_point_product_skus/:id", getZmoduPointProductSku, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_point_product_skus", createZmoduPointProductSku, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_point_product_skus/:id", updateZmoduPointProductSku, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_point_product_skus/:id", deleteZmoduPointProductSku, @ptrCast(@alignCast(self)));
    }

    fn listZmoduPointProducts(ctx: *zigmodu.http_server.Context) !void {
        const self: *PointApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[point] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[point] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduPointProducts(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduPointProduct(ctx: *zigmodu.http_server.Context) !void {
        const self: *PointApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[point] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[point] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduPointProduct(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduPointProduct(ctx: *zigmodu.http_server.Context) !void {
        const self: *PointApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduPointProduct) catch {
            std.log.warn("[point] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduPointProduct(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduPointProduct(ctx: *zigmodu.http_server.Context) !void {
        const self: *PointApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduPointProduct) catch {
            std.log.warn("[point] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduPointProduct(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduPointProduct(ctx: *zigmodu.http_server.Context) !void {
        const self: *PointApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[point] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduPointProduct(id);
        try ctx.json(204, "");
    }

    fn listZmoduPointProductSkus(ctx: *zigmodu.http_server.Context) !void {
        const self: *PointApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[point] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[point] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduPointProductSkus(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduPointProductSku(ctx: *zigmodu.http_server.Context) !void {
        const self: *PointApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[point] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[point] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduPointProductSku(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduPointProductSku(ctx: *zigmodu.http_server.Context) !void {
        const self: *PointApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduPointProductSku) catch {
            std.log.warn("[point] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduPointProductSku(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduPointProductSku(ctx: *zigmodu.http_server.Context) !void {
        const self: *PointApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduPointProductSku) catch {
            std.log.warn("[point] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduPointProductSku(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduPointProductSku(ctx: *zigmodu.http_server.Context) !void {
        const self: *PointApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[point] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduPointProductSku(id);
        try ctx.json(204, "");
    }

};
