//! Auto-generated service for module: point
//! Generated by zmodu orm — templates: tools/zmodu/src/templates/orm/sqlx/
//!
//! ╔═══════════════════════════════════════════════════════════╗
//! ║  AI Metadata: module=point | layer=service      ║
//! ║  role=CRUD delegation | extends=service_ext.zig           ║
//! ╚═══════════════════════════════════════════════════════════╝

const std = @import("std");
const zigmodu = @import("zigmodu");
const model = @import("model.zig");
const persistence = @import("persistence.zig");

pub const PointService = struct {
    persistence: *persistence.PointPersistence,
    // EventBus — uncomment to enable event-driven communication:
    // event_bus: zigmodu.TypedEventBus(PointEvent),

    pub fn init(persistence_ptr: *persistence.PointPersistence) PointService {
        return .{ .persistence = persistence_ptr };
    }

    // Event hooks (uncomment and define event types to enable):
    // fn afterCreate(self: *PointService, entity: anytype) !void {
    //     try self.event_bus.publish(.{ .id = entity.id });
    // }
    // fn afterUpdate(self: *PointService, entity: anytype) !void {
    //     try self.event_bus.publish(.{ .id = entity.id });
    // }
    // fn afterDelete(self: *PointService, id: i64) !void {
    //     try self.event_bus.publish(.{ .id = id });
    // }

    pub fn listZmoduPointProducts(self: *PointService, page: usize, size: usize) !zigmodu.orm.PageResult(model.ZmoduPointProduct) {
        var repo = self.persistence.zmoduPointProductRepo();
        return try repo.findPage(page, size);
    }

    pub fn getZmoduPointProduct(self: *PointService, id: i64) !?model.ZmoduPointProduct {
        var repo = self.persistence.zmoduPointProductRepo();
        return try repo.findById(id);
    }

    pub fn createZmoduPointProduct(self: *PointService, entity: model.ZmoduPointProduct) !model.ZmoduPointProduct {
        var repo = self.persistence.zmoduPointProductRepo();
        return try repo.insert(entity);
    }

    pub fn updateZmoduPointProduct(self: *PointService, entity: model.ZmoduPointProduct) !void {
        var repo = self.persistence.zmoduPointProductRepo();
        return try repo.update(entity);
    }

    pub fn deleteZmoduPointProduct(self: *PointService, id: i64) !void {
        var repo = self.persistence.zmoduPointProductRepo();
        return try repo.delete(id);
    }

    pub fn listZmoduPointProductSkus(self: *PointService, page: usize, size: usize) !zigmodu.orm.PageResult(model.ZmoduPointProductSku) {
        var repo = self.persistence.zmoduPointProductSkuRepo();
        return try repo.findPage(page, size);
    }

    pub fn getZmoduPointProductSku(self: *PointService, id: i64) !?model.ZmoduPointProductSku {
        var repo = self.persistence.zmoduPointProductSkuRepo();
        return try repo.findById(id);
    }

    pub fn createZmoduPointProductSku(self: *PointService, entity: model.ZmoduPointProductSku) !model.ZmoduPointProductSku {
        var repo = self.persistence.zmoduPointProductSkuRepo();
        return try repo.insert(entity);
    }

    pub fn updateZmoduPointProductSku(self: *PointService, entity: model.ZmoduPointProductSku) !void {
        var repo = self.persistence.zmoduPointProductSkuRepo();
        return try repo.update(entity);
    }

    pub fn deleteZmoduPointProductSku(self: *PointService, id: i64) !void {
        var repo = self.persistence.zmoduPointProductSkuRepo();
        return try repo.delete(id);
    }

};
