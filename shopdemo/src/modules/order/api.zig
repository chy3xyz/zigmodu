//! Auto-generated HTTP API for module: order
//! Generated by zmodu orm — uses `zigmodu.http_server` + `ctx.bindJson` / `ctx.jsonStruct`.

const std = @import("std");
const zigmodu = @import("zigmodu");
const service = @import("service.zig");
const model = @import("model.zig");

pub const OrderApi = struct {
    service: *service.OrderService,

    pub fn init(service_ptr: *service.OrderService) OrderApi {
        return .{ .service = service_ptr };
    }

    pub fn registerRoutes(self: *OrderApi, group: *zigmodu.http_server.RouteGroup) !void {
        try group.get("/zmodu_orders", listZmoduOrders, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_orders/:id", getZmoduOrder, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_orders", createZmoduOrder, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_orders/:id", updateZmoduOrder, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_orders/:id", deleteZmoduOrder, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_order_addresss", listZmoduOrderAddresss, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_order_addresss/:id", getZmoduOrderAddress, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_order_addresss", createZmoduOrderAddress, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_order_addresss/:id", updateZmoduOrderAddress, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_order_addresss/:id", deleteZmoduOrderAddress, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_order_advances", listZmoduOrderAdvances, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_order_advances/:id", getZmoduOrderAdvance, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_order_advances", createZmoduOrderAdvance, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_order_advances/:id", updateZmoduOrderAdvance, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_order_advances/:id", deleteZmoduOrderAdvance, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_order_deliverys", listZmoduOrderDeliverys, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_order_deliverys/:id", getZmoduOrderDelivery, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_order_deliverys", createZmoduOrderDelivery, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_order_deliverys/:id", updateZmoduOrderDelivery, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_order_deliverys/:id", deleteZmoduOrderDelivery, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_order_extracts", listZmoduOrderExtracts, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_order_extracts/:id", getZmoduOrderExtract, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_order_extracts", createZmoduOrderExtract, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_order_extracts/:id", updateZmoduOrderExtract, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_order_extracts/:id", deleteZmoduOrderExtract, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_order_products", listZmoduOrderProducts, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_order_products/:id", getZmoduOrderProduct, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_order_products", createZmoduOrderProduct, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_order_products/:id", updateZmoduOrderProduct, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_order_products/:id", deleteZmoduOrderProduct, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_order_refunds", listZmoduOrderRefunds, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_order_refunds/:id", getZmoduOrderRefund, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_order_refunds", createZmoduOrderRefund, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_order_refunds/:id", updateZmoduOrderRefund, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_order_refunds/:id", deleteZmoduOrderRefund, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_order_refund_addresss", listZmoduOrderRefundAddresss, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_order_refund_addresss/:id", getZmoduOrderRefundAddress, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_order_refund_addresss", createZmoduOrderRefundAddress, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_order_refund_addresss/:id", updateZmoduOrderRefundAddress, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_order_refund_addresss/:id", deleteZmoduOrderRefundAddress, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_order_refund_images", listZmoduOrderRefundImages, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_order_refund_images/:id", getZmoduOrderRefundImage, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_order_refund_images", createZmoduOrderRefundImage, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_order_refund_images/:id", updateZmoduOrderRefundImage, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_order_refund_images/:id", deleteZmoduOrderRefundImage, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_order_settleds", listZmoduOrderSettleds, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_order_settleds/:id", getZmoduOrderSettled, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_order_settleds", createZmoduOrderSettled, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_order_settleds/:id", updateZmoduOrderSettled, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_order_settleds/:id", deleteZmoduOrderSettled, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_order_trades", listZmoduOrderTrades, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_order_trades/:id", getZmoduOrderTrade, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_order_trades", createZmoduOrderTrade, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_order_trades/:id", updateZmoduOrderTrade, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_order_trades/:id", deleteZmoduOrderTrade, @ptrCast(@alignCast(self)));
    }

    fn listZmoduOrders(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[order] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[order] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduOrders(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduOrder(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[order] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[order] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduOrder(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduOrder(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduOrder) catch {
            std.log.warn("[order] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduOrder(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduOrder(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduOrder) catch {
            std.log.warn("[order] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduOrder(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduOrder(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[order] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduOrder(id);
        try ctx.json(204, "");
    }

    fn listZmoduOrderAddresss(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[order] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[order] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduOrderAddresss(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduOrderAddress(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[order] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[order] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduOrderAddress(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduOrderAddress(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduOrderAddress) catch {
            std.log.warn("[order] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduOrderAddress(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduOrderAddress(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduOrderAddress) catch {
            std.log.warn("[order] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduOrderAddress(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduOrderAddress(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[order] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduOrderAddress(id);
        try ctx.json(204, "");
    }

    fn listZmoduOrderAdvances(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[order] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[order] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduOrderAdvances(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduOrderAdvance(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[order] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[order] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduOrderAdvance(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduOrderAdvance(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduOrderAdvance) catch {
            std.log.warn("[order] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduOrderAdvance(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduOrderAdvance(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduOrderAdvance) catch {
            std.log.warn("[order] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduOrderAdvance(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduOrderAdvance(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[order] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduOrderAdvance(id);
        try ctx.json(204, "");
    }

    fn listZmoduOrderDeliverys(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[order] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[order] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduOrderDeliverys(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduOrderDelivery(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[order] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[order] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduOrderDelivery(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduOrderDelivery(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduOrderDelivery) catch {
            std.log.warn("[order] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduOrderDelivery(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduOrderDelivery(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduOrderDelivery) catch {
            std.log.warn("[order] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduOrderDelivery(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduOrderDelivery(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[order] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduOrderDelivery(id);
        try ctx.json(204, "");
    }

    fn listZmoduOrderExtracts(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[order] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[order] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduOrderExtracts(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduOrderExtract(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[order] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[order] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduOrderExtract(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduOrderExtract(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduOrderExtract) catch {
            std.log.warn("[order] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduOrderExtract(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduOrderExtract(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduOrderExtract) catch {
            std.log.warn("[order] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduOrderExtract(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduOrderExtract(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[order] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduOrderExtract(id);
        try ctx.json(204, "");
    }

    fn listZmoduOrderProducts(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[order] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[order] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduOrderProducts(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduOrderProduct(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[order] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[order] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduOrderProduct(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduOrderProduct(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduOrderProduct) catch {
            std.log.warn("[order] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduOrderProduct(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduOrderProduct(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduOrderProduct) catch {
            std.log.warn("[order] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduOrderProduct(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduOrderProduct(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[order] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduOrderProduct(id);
        try ctx.json(204, "");
    }

    fn listZmoduOrderRefunds(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[order] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[order] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduOrderRefunds(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduOrderRefund(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[order] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[order] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduOrderRefund(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduOrderRefund(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduOrderRefund) catch {
            std.log.warn("[order] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduOrderRefund(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduOrderRefund(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduOrderRefund) catch {
            std.log.warn("[order] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduOrderRefund(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduOrderRefund(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[order] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduOrderRefund(id);
        try ctx.json(204, "");
    }

    fn listZmoduOrderRefundAddresss(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[order] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[order] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduOrderRefundAddresss(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduOrderRefundAddress(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[order] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[order] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduOrderRefundAddress(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduOrderRefundAddress(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduOrderRefundAddress) catch {
            std.log.warn("[order] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduOrderRefundAddress(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduOrderRefundAddress(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduOrderRefundAddress) catch {
            std.log.warn("[order] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduOrderRefundAddress(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduOrderRefundAddress(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[order] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduOrderRefundAddress(id);
        try ctx.json(204, "");
    }

    fn listZmoduOrderRefundImages(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[order] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[order] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduOrderRefundImages(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduOrderRefundImage(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[order] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[order] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduOrderRefundImage(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduOrderRefundImage(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduOrderRefundImage) catch {
            std.log.warn("[order] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduOrderRefundImage(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduOrderRefundImage(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduOrderRefundImage) catch {
            std.log.warn("[order] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduOrderRefundImage(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduOrderRefundImage(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[order] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduOrderRefundImage(id);
        try ctx.json(204, "");
    }

    fn listZmoduOrderSettleds(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[order] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[order] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduOrderSettleds(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduOrderSettled(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[order] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[order] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduOrderSettled(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduOrderSettled(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduOrderSettled) catch {
            std.log.warn("[order] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduOrderSettled(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduOrderSettled(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduOrderSettled) catch {
            std.log.warn("[order] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduOrderSettled(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduOrderSettled(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[order] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduOrderSettled(id);
        try ctx.json(204, "");
    }

    fn listZmoduOrderTrades(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[order] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[order] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduOrderTrades(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduOrderTrade(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[order] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[order] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduOrderTrade(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduOrderTrade(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduOrderTrade) catch {
            std.log.warn("[order] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduOrderTrade(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduOrderTrade(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduOrderTrade) catch {
            std.log.warn("[order] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduOrderTrade(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduOrderTrade(ctx: *zigmodu.http_server.Context) !void {
        const self: *OrderApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[order] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduOrderTrade(id);
        try ctx.json(204, "");
    }

};
