//! Auto-generated HTTP API for module: sms
//! Generated by zmodu orm — uses `zigmodu.http_server` + `ctx.bindJson` / `ctx.jsonStruct`.

const std = @import("std");
const zigmodu = @import("zigmodu");
const service = @import("service.zig");
const model = @import("model.zig");

pub const SmsApi = struct {
    service: *service.SmsService,

    pub fn init(service_ptr: *service.SmsService) SmsApi {
        return .{ .service = service_ptr };
    }

    pub fn registerRoutes(self: *SmsApi, group: *zigmodu.http_server.RouteGroup) !void {
        try group.get("/zmodu_smss", listZmoduSmss, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_smss/:id", getZmoduSms, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_smss", createZmoduSms, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_smss/:id", updateZmoduSms, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_smss/:id", deleteZmoduSms, @ptrCast(@alignCast(self)));
    }

    fn listZmoduSmss(ctx: *zigmodu.http_server.Context) !void {
        const self: *SmsApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[sms] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[sms] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduSmss(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduSms(ctx: *zigmodu.http_server.Context) !void {
        const self: *SmsApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[sms] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[sms] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduSms(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduSms(ctx: *zigmodu.http_server.Context) !void {
        const self: *SmsApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSms) catch {
            std.log.warn("[sms] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduSms(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduSms(ctx: *zigmodu.http_server.Context) !void {
        const self: *SmsApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSms) catch {
            std.log.warn("[sms] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduSms(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduSms(ctx: *zigmodu.http_server.Context) !void {
        const self: *SmsApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[sms] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduSms(id);
        try ctx.json(204, "");
    }

};
