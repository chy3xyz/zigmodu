//! Auto-generated HTTP API for module: supplier
//! Generated by zmodu orm — uses `zigmodu.http_server` + `ctx.bindJson` / `ctx.jsonStruct`.

const std = @import("std");
const zigmodu = @import("zigmodu");
const service = @import("service.zig");
const model = @import("model.zig");

pub const SupplierApi = struct {
    service: *service.SupplierService,

    pub fn init(service_ptr: *service.SupplierService) SupplierApi {
        return .{ .service = service_ptr };
    }

    pub fn registerRoutes(self: *SupplierApi, group: *zigmodu.http_server.RouteGroup) !void {
        try group.get("/zmodu_suppliers", listZmoduSuppliers, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_suppliers/:id", getZmoduSupplier, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_suppliers", createZmoduSupplier, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_suppliers/:id", updateZmoduSupplier, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_suppliers/:id", deleteZmoduSupplier, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_supplier_accesss", listZmoduSupplierAccesss, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_supplier_accesss/:id", getZmoduSupplierAccess, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_supplier_accesss", createZmoduSupplierAccess, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_supplier_accesss/:id", updateZmoduSupplierAccess, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_supplier_accesss/:id", deleteZmoduSupplierAccess, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_supplier_accounts", listZmoduSupplierAccounts, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_supplier_accounts/:id", getZmoduSupplierAccount, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_supplier_accounts", createZmoduSupplierAccount, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_supplier_accounts/:id", updateZmoduSupplierAccount, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_supplier_accounts/:id", deleteZmoduSupplierAccount, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_supplier_applys", listZmoduSupplierApplys, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_supplier_applys/:id", getZmoduSupplierApply, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_supplier_applys", createZmoduSupplierApply, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_supplier_applys/:id", updateZmoduSupplierApply, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_supplier_applys/:id", deleteZmoduSupplierApply, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_supplier_capitals", listZmoduSupplierCapitals, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_supplier_capitals/:id", getZmoduSupplierCapital, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_supplier_capitals", createZmoduSupplierCapital, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_supplier_capitals/:id", updateZmoduSupplierCapital, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_supplier_capitals/:id", deleteZmoduSupplierCapital, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_supplier_cashs", listZmoduSupplierCashs, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_supplier_cashs/:id", getZmoduSupplierCash, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_supplier_cashs", createZmoduSupplierCash, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_supplier_cashs/:id", updateZmoduSupplierCash, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_supplier_cashs/:id", deleteZmoduSupplierCash, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_supplier_categorys", listZmoduSupplierCategorys, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_supplier_categorys/:id", getZmoduSupplierCategory, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_supplier_categorys", createZmoduSupplierCategory, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_supplier_categorys/:id", updateZmoduSupplierCategory, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_supplier_categorys/:id", deleteZmoduSupplierCategory, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_supplier_deposit_orders", listZmoduSupplierDepositOrders, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_supplier_deposit_orders/:id", getZmoduSupplierDepositOrder, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_supplier_deposit_orders", createZmoduSupplierDepositOrder, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_supplier_deposit_orders/:id", updateZmoduSupplierDepositOrder, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_supplier_deposit_orders/:id", deleteZmoduSupplierDepositOrder, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_supplier_deposit_refunds", listZmoduSupplierDepositRefunds, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_supplier_deposit_refunds/:id", getZmoduSupplierDepositRefund, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_supplier_deposit_refunds", createZmoduSupplierDepositRefund, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_supplier_deposit_refunds/:id", updateZmoduSupplierDepositRefund, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_supplier_deposit_refunds/:id", deleteZmoduSupplierDepositRefund, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_supplier_login_logs", listZmoduSupplierLoginLogs, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_supplier_login_logs/:id", getZmoduSupplierLoginLog, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_supplier_login_logs", createZmoduSupplierLoginLog, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_supplier_login_logs/:id", updateZmoduSupplierLoginLog, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_supplier_login_logs/:id", deleteZmoduSupplierLoginLog, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_supplier_opt_logs", listZmoduSupplierOptLogs, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_supplier_opt_logs/:id", getZmoduSupplierOptLog, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_supplier_opt_logs", createZmoduSupplierOptLog, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_supplier_opt_logs/:id", updateZmoduSupplierOptLog, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_supplier_opt_logs/:id", deleteZmoduSupplierOptLog, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_supplier_roles", listZmoduSupplierRoles, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_supplier_roles/:id", getZmoduSupplierRole, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_supplier_roles", createZmoduSupplierRole, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_supplier_roles/:id", updateZmoduSupplierRole, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_supplier_roles/:id", deleteZmoduSupplierRole, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_supplier_role_accesss", listZmoduSupplierRoleAccesss, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_supplier_role_accesss/:id", getZmoduSupplierRoleAccess, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_supplier_role_accesss", createZmoduSupplierRoleAccess, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_supplier_role_accesss/:id", updateZmoduSupplierRoleAccess, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_supplier_role_accesss/:id", deleteZmoduSupplierRoleAccess, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_supplier_services", listZmoduSupplierServices, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_supplier_services/:id", getZmoduSupplierService, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_supplier_services", createZmoduSupplierService, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_supplier_services/:id", updateZmoduSupplierService, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_supplier_services/:id", deleteZmoduSupplierService, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_supplier_service_applys", listZmoduSupplierServiceApplys, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_supplier_service_applys/:id", getZmoduSupplierServiceApply, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_supplier_service_applys", createZmoduSupplierServiceApply, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_supplier_service_applys/:id", updateZmoduSupplierServiceApply, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_supplier_service_applys/:id", deleteZmoduSupplierServiceApply, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_supplier_service_securitys", listZmoduSupplierServiceSecuritys, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_supplier_service_securitys/:id", getZmoduSupplierServiceSecurity, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_supplier_service_securitys", createZmoduSupplierServiceSecurity, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_supplier_service_securitys/:id", updateZmoduSupplierServiceSecurity, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_supplier_service_securitys/:id", deleteZmoduSupplierServiceSecurity, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_supplier_users", listZmoduSupplierUsers, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_supplier_users/:id", getZmoduSupplierUser, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_supplier_users", createZmoduSupplierUser, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_supplier_users/:id", updateZmoduSupplierUser, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_supplier_users/:id", deleteZmoduSupplierUser, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_supplier_user_roles", listZmoduSupplierUserRoles, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_supplier_user_roles/:id", getZmoduSupplierUserRole, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_supplier_user_roles", createZmoduSupplierUserRole, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_supplier_user_roles/:id", updateZmoduSupplierUserRole, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_supplier_user_roles/:id", deleteZmoduSupplierUserRole, @ptrCast(@alignCast(self)));
    }

    fn listZmoduSuppliers(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[supplier] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[supplier] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduSuppliers(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduSupplier(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[supplier] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[supplier] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduSupplier(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduSupplier(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSupplier) catch {
            std.log.warn("[supplier] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduSupplier(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduSupplier(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSupplier) catch {
            std.log.warn("[supplier] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduSupplier(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduSupplier(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[supplier] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduSupplier(id);
        try ctx.json(204, "");
    }

    fn listZmoduSupplierAccesss(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[supplier] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[supplier] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduSupplierAccesss(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduSupplierAccess(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[supplier] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[supplier] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduSupplierAccess(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduSupplierAccess(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSupplierAccess) catch {
            std.log.warn("[supplier] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduSupplierAccess(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduSupplierAccess(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSupplierAccess) catch {
            std.log.warn("[supplier] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduSupplierAccess(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduSupplierAccess(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[supplier] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduSupplierAccess(id);
        try ctx.json(204, "");
    }

    fn listZmoduSupplierAccounts(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[supplier] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[supplier] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduSupplierAccounts(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduSupplierAccount(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[supplier] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[supplier] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduSupplierAccount(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduSupplierAccount(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSupplierAccount) catch {
            std.log.warn("[supplier] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduSupplierAccount(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduSupplierAccount(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSupplierAccount) catch {
            std.log.warn("[supplier] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduSupplierAccount(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduSupplierAccount(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[supplier] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduSupplierAccount(id);
        try ctx.json(204, "");
    }

    fn listZmoduSupplierApplys(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[supplier] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[supplier] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduSupplierApplys(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduSupplierApply(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[supplier] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[supplier] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduSupplierApply(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduSupplierApply(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSupplierApply) catch {
            std.log.warn("[supplier] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduSupplierApply(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduSupplierApply(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSupplierApply) catch {
            std.log.warn("[supplier] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduSupplierApply(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduSupplierApply(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[supplier] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduSupplierApply(id);
        try ctx.json(204, "");
    }

    fn listZmoduSupplierCapitals(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[supplier] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[supplier] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduSupplierCapitals(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduSupplierCapital(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[supplier] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[supplier] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduSupplierCapital(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduSupplierCapital(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSupplierCapital) catch {
            std.log.warn("[supplier] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduSupplierCapital(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduSupplierCapital(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSupplierCapital) catch {
            std.log.warn("[supplier] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduSupplierCapital(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduSupplierCapital(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[supplier] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduSupplierCapital(id);
        try ctx.json(204, "");
    }

    fn listZmoduSupplierCashs(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[supplier] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[supplier] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduSupplierCashs(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduSupplierCash(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[supplier] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[supplier] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduSupplierCash(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduSupplierCash(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSupplierCash) catch {
            std.log.warn("[supplier] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduSupplierCash(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduSupplierCash(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSupplierCash) catch {
            std.log.warn("[supplier] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduSupplierCash(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduSupplierCash(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[supplier] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduSupplierCash(id);
        try ctx.json(204, "");
    }

    fn listZmoduSupplierCategorys(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[supplier] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[supplier] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduSupplierCategorys(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduSupplierCategory(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[supplier] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[supplier] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduSupplierCategory(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduSupplierCategory(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSupplierCategory) catch {
            std.log.warn("[supplier] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduSupplierCategory(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduSupplierCategory(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSupplierCategory) catch {
            std.log.warn("[supplier] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduSupplierCategory(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduSupplierCategory(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[supplier] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduSupplierCategory(id);
        try ctx.json(204, "");
    }

    fn listZmoduSupplierDepositOrders(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[supplier] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[supplier] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduSupplierDepositOrders(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduSupplierDepositOrder(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[supplier] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[supplier] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduSupplierDepositOrder(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduSupplierDepositOrder(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSupplierDepositOrder) catch {
            std.log.warn("[supplier] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduSupplierDepositOrder(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduSupplierDepositOrder(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSupplierDepositOrder) catch {
            std.log.warn("[supplier] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduSupplierDepositOrder(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduSupplierDepositOrder(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[supplier] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduSupplierDepositOrder(id);
        try ctx.json(204, "");
    }

    fn listZmoduSupplierDepositRefunds(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[supplier] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[supplier] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduSupplierDepositRefunds(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduSupplierDepositRefund(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[supplier] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[supplier] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduSupplierDepositRefund(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduSupplierDepositRefund(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSupplierDepositRefund) catch {
            std.log.warn("[supplier] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduSupplierDepositRefund(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduSupplierDepositRefund(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSupplierDepositRefund) catch {
            std.log.warn("[supplier] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduSupplierDepositRefund(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduSupplierDepositRefund(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[supplier] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduSupplierDepositRefund(id);
        try ctx.json(204, "");
    }

    fn listZmoduSupplierLoginLogs(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[supplier] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[supplier] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduSupplierLoginLogs(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduSupplierLoginLog(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[supplier] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[supplier] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduSupplierLoginLog(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduSupplierLoginLog(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSupplierLoginLog) catch {
            std.log.warn("[supplier] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduSupplierLoginLog(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduSupplierLoginLog(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSupplierLoginLog) catch {
            std.log.warn("[supplier] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduSupplierLoginLog(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduSupplierLoginLog(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[supplier] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduSupplierLoginLog(id);
        try ctx.json(204, "");
    }

    fn listZmoduSupplierOptLogs(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[supplier] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[supplier] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduSupplierOptLogs(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduSupplierOptLog(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[supplier] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[supplier] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduSupplierOptLog(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduSupplierOptLog(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSupplierOptLog) catch {
            std.log.warn("[supplier] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduSupplierOptLog(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduSupplierOptLog(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSupplierOptLog) catch {
            std.log.warn("[supplier] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduSupplierOptLog(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduSupplierOptLog(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[supplier] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduSupplierOptLog(id);
        try ctx.json(204, "");
    }

    fn listZmoduSupplierRoles(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[supplier] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[supplier] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduSupplierRoles(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduSupplierRole(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[supplier] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[supplier] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduSupplierRole(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduSupplierRole(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSupplierRole) catch {
            std.log.warn("[supplier] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduSupplierRole(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduSupplierRole(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSupplierRole) catch {
            std.log.warn("[supplier] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduSupplierRole(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduSupplierRole(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[supplier] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduSupplierRole(id);
        try ctx.json(204, "");
    }

    fn listZmoduSupplierRoleAccesss(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[supplier] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[supplier] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduSupplierRoleAccesss(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduSupplierRoleAccess(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[supplier] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[supplier] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduSupplierRoleAccess(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduSupplierRoleAccess(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSupplierRoleAccess) catch {
            std.log.warn("[supplier] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduSupplierRoleAccess(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduSupplierRoleAccess(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSupplierRoleAccess) catch {
            std.log.warn("[supplier] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduSupplierRoleAccess(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduSupplierRoleAccess(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[supplier] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduSupplierRoleAccess(id);
        try ctx.json(204, "");
    }

    fn listZmoduSupplierServices(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[supplier] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[supplier] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduSupplierServices(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduSupplierService(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[supplier] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[supplier] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduSupplierService(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduSupplierService(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSupplierService) catch {
            std.log.warn("[supplier] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduSupplierService(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduSupplierService(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSupplierService) catch {
            std.log.warn("[supplier] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduSupplierService(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduSupplierService(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[supplier] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduSupplierService(id);
        try ctx.json(204, "");
    }

    fn listZmoduSupplierServiceApplys(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[supplier] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[supplier] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduSupplierServiceApplys(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduSupplierServiceApply(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[supplier] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[supplier] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduSupplierServiceApply(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduSupplierServiceApply(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSupplierServiceApply) catch {
            std.log.warn("[supplier] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduSupplierServiceApply(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduSupplierServiceApply(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSupplierServiceApply) catch {
            std.log.warn("[supplier] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduSupplierServiceApply(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduSupplierServiceApply(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[supplier] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduSupplierServiceApply(id);
        try ctx.json(204, "");
    }

    fn listZmoduSupplierServiceSecuritys(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[supplier] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[supplier] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduSupplierServiceSecuritys(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduSupplierServiceSecurity(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[supplier] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[supplier] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduSupplierServiceSecurity(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduSupplierServiceSecurity(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSupplierServiceSecurity) catch {
            std.log.warn("[supplier] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduSupplierServiceSecurity(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduSupplierServiceSecurity(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSupplierServiceSecurity) catch {
            std.log.warn("[supplier] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduSupplierServiceSecurity(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduSupplierServiceSecurity(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[supplier] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduSupplierServiceSecurity(id);
        try ctx.json(204, "");
    }

    fn listZmoduSupplierUsers(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[supplier] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[supplier] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduSupplierUsers(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduSupplierUser(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[supplier] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[supplier] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduSupplierUser(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduSupplierUser(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSupplierUser) catch {
            std.log.warn("[supplier] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduSupplierUser(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduSupplierUser(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSupplierUser) catch {
            std.log.warn("[supplier] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduSupplierUser(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduSupplierUser(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[supplier] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduSupplierUser(id);
        try ctx.json(204, "");
    }

    fn listZmoduSupplierUserRoles(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[supplier] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[supplier] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduSupplierUserRoles(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduSupplierUserRole(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[supplier] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[supplier] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduSupplierUserRole(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduSupplierUserRole(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSupplierUserRole) catch {
            std.log.warn("[supplier] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduSupplierUserRole(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduSupplierUserRole(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduSupplierUserRole) catch {
            std.log.warn("[supplier] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduSupplierUserRole(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduSupplierUserRole(ctx: *zigmodu.http_server.Context) !void {
        const self: *SupplierApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[supplier] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduSupplierUserRole(id);
        try ctx.json(204, "");
    }

};
