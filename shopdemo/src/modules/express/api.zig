//! Auto-generated HTTP API for module: express
//! Generated by zmodu orm — uses `zigmodu.http_server` + `ctx.bindJson` / `ctx.jsonStruct`.

const std = @import("std");
const zigmodu = @import("zigmodu");
const service = @import("service.zig");
const model = @import("model.zig");

pub const ExpressApi = struct {
    service: *service.ExpressService,

    pub fn init(service_ptr: *service.ExpressService) ExpressApi {
        return .{ .service = service_ptr };
    }

    pub fn registerRoutes(self: *ExpressApi, group: *zigmodu.http_server.RouteGroup) !void {
        try group.get("/zmodu_expresss", listZmoduExpresss, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_expresss/:id", getZmoduExpress, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_expresss", createZmoduExpress, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_expresss/:id", updateZmoduExpress, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_expresss/:id", deleteZmoduExpress, @ptrCast(@alignCast(self)));
    }

    fn listZmoduExpresss(ctx: *zigmodu.http_server.Context) !void {
        const self: *ExpressApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[express] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[express] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduExpresss(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduExpress(ctx: *zigmodu.http_server.Context) !void {
        const self: *ExpressApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[express] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[express] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduExpress(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduExpress(ctx: *zigmodu.http_server.Context) !void {
        const self: *ExpressApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduExpress) catch {
            std.log.warn("[express] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduExpress(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduExpress(ctx: *zigmodu.http_server.Context) !void {
        const self: *ExpressApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduExpress) catch {
            std.log.warn("[express] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduExpress(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduExpress(ctx: *zigmodu.http_server.Context) !void {
        const self: *ExpressApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[express] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduExpress(id);
        try ctx.json(204, "");
    }

};
