//! Auto-generated HTTP API for module: buy
//! Generated by zmodu orm — uses `zigmodu.http_server` + `ctx.bindJson` / `ctx.jsonStruct`.

const std = @import("std");
const zigmodu = @import("zigmodu");
const service = @import("service.zig");
const model = @import("model.zig");

pub const BuyApi = struct {
    service: *service.BuyService,

    pub fn init(service_ptr: *service.BuyService) BuyApi {
        return .{ .service = service_ptr };
    }

    pub fn registerRoutes(self: *BuyApi, group: *zigmodu.http_server.RouteGroup) !void {
        try group.get("/zmodu_buy_activitys", listZmoduBuyActivitys, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_buy_activitys/:id", getZmoduBuyActivity, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_buy_activitys", createZmoduBuyActivity, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_buy_activitys/:id", updateZmoduBuyActivity, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_buy_activitys/:id", deleteZmoduBuyActivity, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_buy_activity_products", listZmoduBuyActivityProducts, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_buy_activity_products/:id", getZmoduBuyActivityProduct, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_buy_activity_products", createZmoduBuyActivityProduct, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_buy_activity_products/:id", updateZmoduBuyActivityProduct, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_buy_activity_products/:id", deleteZmoduBuyActivityProduct, @ptrCast(@alignCast(self)));
    }

    fn listZmoduBuyActivitys(ctx: *zigmodu.http_server.Context) !void {
        const self: *BuyApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[buy] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[buy] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduBuyActivitys(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduBuyActivity(ctx: *zigmodu.http_server.Context) !void {
        const self: *BuyApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[buy] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[buy] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduBuyActivity(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduBuyActivity(ctx: *zigmodu.http_server.Context) !void {
        const self: *BuyApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduBuyActivity) catch {
            std.log.warn("[buy] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduBuyActivity(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduBuyActivity(ctx: *zigmodu.http_server.Context) !void {
        const self: *BuyApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduBuyActivity) catch {
            std.log.warn("[buy] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduBuyActivity(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduBuyActivity(ctx: *zigmodu.http_server.Context) !void {
        const self: *BuyApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[buy] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduBuyActivity(id);
        try ctx.json(204, "");
    }

    fn listZmoduBuyActivityProducts(ctx: *zigmodu.http_server.Context) !void {
        const self: *BuyApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[buy] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[buy] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduBuyActivityProducts(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduBuyActivityProduct(ctx: *zigmodu.http_server.Context) !void {
        const self: *BuyApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[buy] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[buy] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduBuyActivityProduct(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduBuyActivityProduct(ctx: *zigmodu.http_server.Context) !void {
        const self: *BuyApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduBuyActivityProduct) catch {
            std.log.warn("[buy] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduBuyActivityProduct(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduBuyActivityProduct(ctx: *zigmodu.http_server.Context) !void {
        const self: *BuyApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduBuyActivityProduct) catch {
            std.log.warn("[buy] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduBuyActivityProduct(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduBuyActivityProduct(ctx: *zigmodu.http_server.Context) !void {
        const self: *BuyApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[buy] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduBuyActivityProduct(id);
        try ctx.json(204, "");
    }

};
