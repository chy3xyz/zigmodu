//! Auto-generated service for module: setting
//! Generated by zmodu orm — templates: tools/zmodu/src/templates/orm/sqlx/
//!
//! ╔═══════════════════════════════════════════════════════════╗
//! ║  AI Metadata: module=setting | layer=service      ║
//! ║  role=CRUD delegation | extends=service_ext.zig           ║
//! ╚═══════════════════════════════════════════════════════════╝

const std = @import("std");
const zigmodu = @import("zigmodu");
const model = @import("model.zig");
const persistence = @import("persistence.zig");

pub const SettingService = struct {
    persistence: *persistence.SettingPersistence,
    // EventBus — uncomment to enable event-driven communication:
    // event_bus: zigmodu.TypedEventBus(SettingEvent),

    pub fn init(persistence_ptr: *persistence.SettingPersistence) SettingService {
        return .{ .persistence = persistence_ptr };
    }

    // Event hooks (uncomment and define event types to enable):
    // fn afterCreate(self: *SettingService, entity: anytype) !void {
    //     try self.event_bus.publish(.{ .id = entity.id });
    // }
    // fn afterUpdate(self: *SettingService, entity: anytype) !void {
    //     try self.event_bus.publish(.{ .id = entity.id });
    // }
    // fn afterDelete(self: *SettingService, id: i64) !void {
    //     try self.event_bus.publish(.{ .id = id });
    // }

    pub fn listZmoduSettings(self: *SettingService, page: usize, size: usize) !zigmodu.orm.PageResult(model.ZmoduSetting) {
        var repo = self.persistence.zmoduSettingRepo();
        return try repo.findPage(page, size);
    }

    pub fn getZmoduSetting(self: *SettingService, id: i64) !?model.ZmoduSetting {
        var repo = self.persistence.zmoduSettingRepo();
        return try repo.findById(id);
    }

    pub fn createZmoduSetting(self: *SettingService, entity: model.ZmoduSetting) !model.ZmoduSetting {
        var repo = self.persistence.zmoduSettingRepo();
        return try repo.insert(entity);
    }

    pub fn updateZmoduSetting(self: *SettingService, entity: model.ZmoduSetting) !void {
        var repo = self.persistence.zmoduSettingRepo();
        return try repo.update(entity);
    }

    pub fn deleteZmoduSetting(self: *SettingService, id: i64) !void {
        var repo = self.persistence.zmoduSettingRepo();
        return try repo.delete(id);
    }

};
