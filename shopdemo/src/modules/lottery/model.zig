//! Auto-generated models for module: lottery
//! Generated by zmodu orm — templates: tools/zmodu/src/templates/orm/sqlx/
//! Do not edit generated files by hand; change SQL and re-run `zmodu orm`.

const std = @import("std");

pub const ZmoduLottery = struct {
    pub const sql_table_name: []const u8 = "zmodu_lottery";
    lottery_id: i64,
    times: i64,
    points: f64,
    total_num: i64,
    status: i64,
    image_id: i64,
    content: ?[]const u8,
    type: i64,
    user_type: i64,
    grades: ?[]const u8,
    start_time: i64,
    end_time: i64,
    is_delete: i64,
    app_id: i64,
    create_time: i64,
    update_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("lottery_id");
        try jws.write(self.lottery_id);
        try jws.objectField("times");
        try jws.write(self.times);
        try jws.objectField("points");
        try jws.write(self.points);
        try jws.objectField("total_num");
        try jws.write(self.total_num);
        try jws.objectField("status");
        try jws.write(self.status);
        try jws.objectField("image_id");
        try jws.write(self.image_id);
        try jws.objectField("content");
        if (self.content) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("type");
        try jws.write(self.type);
        try jws.objectField("user_type");
        try jws.write(self.user_type);
        try jws.objectField("grades");
        if (self.grades) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("start_time");
        try jws.write(self.start_time);
        try jws.objectField("end_time");
        try jws.write(self.end_time);
        try jws.objectField("is_delete");
        try jws.write(self.is_delete);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.objectField("update_time");
        try jws.write(self.update_time);
        try jws.endObject();
    }
};

pub const ZmoduLotteryPrize = struct {
    pub const sql_table_name: []const u8 = "zmodu_lottery_prize";
    prize_id: i64,
    lottery_id: i64,
    name: []const u8,
    stock: i64,
    draw_num: i64,
    type: i64,
    image: ?[]const u8,
    is_default: i64,
    award_id: i64,
    status: i64,
    points: f64,
    is_play: i64,
    weight: i64,
    probability: f64,
    prompt: []const u8,
    balance: f64,
    product_price: f64,
    sort: i64,
    app_id: i64,
    create_time: i64,
    update_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("prize_id");
        try jws.write(self.prize_id);
        try jws.objectField("lottery_id");
        try jws.write(self.lottery_id);
        try jws.objectField("name");
        try jws.write(self.name);
        try jws.objectField("stock");
        try jws.write(self.stock);
        try jws.objectField("draw_num");
        try jws.write(self.draw_num);
        try jws.objectField("type");
        try jws.write(self.type);
        try jws.objectField("image");
        if (self.image) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("is_default");
        try jws.write(self.is_default);
        try jws.objectField("award_id");
        try jws.write(self.award_id);
        try jws.objectField("status");
        try jws.write(self.status);
        try jws.objectField("points");
        try jws.write(self.points);
        try jws.objectField("is_play");
        try jws.write(self.is_play);
        try jws.objectField("weight");
        try jws.write(self.weight);
        try jws.objectField("probability");
        try jws.write(self.probability);
        try jws.objectField("prompt");
        try jws.write(self.prompt);
        try jws.objectField("balance");
        try jws.write(self.balance);
        try jws.objectField("product_price");
        try jws.write(self.product_price);
        try jws.objectField("sort");
        try jws.write(self.sort);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.objectField("update_time");
        try jws.write(self.update_time);
        try jws.endObject();
    }
};

pub const ZmoduLotteryRecord = struct {
    pub const sql_table_name: []const u8 = "zmodu_lottery_record";
    record_id: i64,
    record_name: []const u8,
    user_id: i64,
    prize_type: i64,
    award_id: i64,
    status: i64,
    prize_num: f64,
    prize_id: i64,
    is_play: i64,
    image: ?[]const u8,
    name: []const u8,
    phone: []const u8,
    province_id: i64,
    city_id: i64,
    region_id: i64,
    detail: []const u8,
    express_id: i64,
    express_no: []const u8,
    delivery_status: i64,
    delivery_time: i64,
    remark: ?[]const u8,
    product_price: f64,
    app_id: i64,
    create_time: i64,
    update_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("record_id");
        try jws.write(self.record_id);
        try jws.objectField("record_name");
        try jws.write(self.record_name);
        try jws.objectField("user_id");
        try jws.write(self.user_id);
        try jws.objectField("prize_type");
        try jws.write(self.prize_type);
        try jws.objectField("award_id");
        try jws.write(self.award_id);
        try jws.objectField("status");
        try jws.write(self.status);
        try jws.objectField("prize_num");
        try jws.write(self.prize_num);
        try jws.objectField("prize_id");
        try jws.write(self.prize_id);
        try jws.objectField("is_play");
        try jws.write(self.is_play);
        try jws.objectField("image");
        if (self.image) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("name");
        try jws.write(self.name);
        try jws.objectField("phone");
        try jws.write(self.phone);
        try jws.objectField("province_id");
        try jws.write(self.province_id);
        try jws.objectField("city_id");
        try jws.write(self.city_id);
        try jws.objectField("region_id");
        try jws.write(self.region_id);
        try jws.objectField("detail");
        try jws.write(self.detail);
        try jws.objectField("express_id");
        try jws.write(self.express_id);
        try jws.objectField("express_no");
        try jws.write(self.express_no);
        try jws.objectField("delivery_status");
        try jws.write(self.delivery_status);
        try jws.objectField("delivery_time");
        try jws.write(self.delivery_time);
        try jws.objectField("remark");
        if (self.remark) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("product_price");
        try jws.write(self.product_price);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.objectField("update_time");
        try jws.write(self.update_time);
        try jws.endObject();
    }
};

