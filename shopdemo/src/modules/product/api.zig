//! Auto-generated HTTP API for module: product
//! Generated by zmodu orm — uses `zigmodu.http_server` + `ctx.bindJson` / `ctx.jsonStruct`.

const std = @import("std");
const zigmodu = @import("zigmodu");
const service = @import("service.zig");
const model = @import("model.zig");

pub const ProductApi = struct {
    service: *service.ProductService,

    pub fn init(service_ptr: *service.ProductService) ProductApi {
        return .{ .service = service_ptr };
    }

    pub fn registerRoutes(self: *ProductApi, group: *zigmodu.http_server.RouteGroup) !void {
        try group.get("/zmodu_products", listZmoduProducts, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_products/:id", getZmoduProduct, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_products", createZmoduProduct, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_products/:id", updateZmoduProduct, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_products/:id", deleteZmoduProduct, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_product_images", listZmoduProductImages, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_product_images/:id", getZmoduProductImage, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_product_images", createZmoduProductImage, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_product_images/:id", updateZmoduProductImage, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_product_images/:id", deleteZmoduProductImage, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_product_reduces", listZmoduProductReduces, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_product_reduces/:id", getZmoduProductReduce, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_product_reduces", createZmoduProductReduce, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_product_reduces/:id", updateZmoduProductReduce, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_product_reduces/:id", deleteZmoduProductReduce, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_product_skus", listZmoduProductSkus, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_product_skus/:id", getZmoduProductSku, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_product_skus", createZmoduProductSku, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_product_skus/:id", updateZmoduProductSku, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_product_skus/:id", deleteZmoduProductSku, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_product_spec_rels", listZmoduProductSpecRels, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_product_spec_rels/:id", getZmoduProductSpecRel, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_product_spec_rels", createZmoduProductSpecRel, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_product_spec_rels/:id", updateZmoduProductSpecRel, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_product_spec_rels/:id", deleteZmoduProductSpecRel, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_product_virtuals", listZmoduProductVirtuals, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_product_virtuals/:id", getZmoduProductVirtual, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_product_virtuals", createZmoduProductVirtual, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_product_virtuals/:id", updateZmoduProductVirtual, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_product_virtuals/:id", deleteZmoduProductVirtual, @ptrCast(@alignCast(self)));
    }

    fn listZmoduProducts(ctx: *zigmodu.http_server.Context) !void {
        const self: *ProductApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[product] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[product] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduProducts(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduProduct(ctx: *zigmodu.http_server.Context) !void {
        const self: *ProductApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[product] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[product] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduProduct(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduProduct(ctx: *zigmodu.http_server.Context) !void {
        const self: *ProductApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduProduct) catch {
            std.log.warn("[product] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduProduct(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduProduct(ctx: *zigmodu.http_server.Context) !void {
        const self: *ProductApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduProduct) catch {
            std.log.warn("[product] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduProduct(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduProduct(ctx: *zigmodu.http_server.Context) !void {
        const self: *ProductApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[product] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduProduct(id);
        try ctx.json(204, "");
    }

    fn listZmoduProductImages(ctx: *zigmodu.http_server.Context) !void {
        const self: *ProductApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[product] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[product] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduProductImages(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduProductImage(ctx: *zigmodu.http_server.Context) !void {
        const self: *ProductApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[product] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[product] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduProductImage(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduProductImage(ctx: *zigmodu.http_server.Context) !void {
        const self: *ProductApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduProductImage) catch {
            std.log.warn("[product] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduProductImage(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduProductImage(ctx: *zigmodu.http_server.Context) !void {
        const self: *ProductApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduProductImage) catch {
            std.log.warn("[product] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduProductImage(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduProductImage(ctx: *zigmodu.http_server.Context) !void {
        const self: *ProductApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[product] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduProductImage(id);
        try ctx.json(204, "");
    }

    fn listZmoduProductReduces(ctx: *zigmodu.http_server.Context) !void {
        const self: *ProductApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[product] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[product] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduProductReduces(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduProductReduce(ctx: *zigmodu.http_server.Context) !void {
        const self: *ProductApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[product] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[product] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduProductReduce(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduProductReduce(ctx: *zigmodu.http_server.Context) !void {
        const self: *ProductApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduProductReduce) catch {
            std.log.warn("[product] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduProductReduce(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduProductReduce(ctx: *zigmodu.http_server.Context) !void {
        const self: *ProductApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduProductReduce) catch {
            std.log.warn("[product] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduProductReduce(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduProductReduce(ctx: *zigmodu.http_server.Context) !void {
        const self: *ProductApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[product] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduProductReduce(id);
        try ctx.json(204, "");
    }

    fn listZmoduProductSkus(ctx: *zigmodu.http_server.Context) !void {
        const self: *ProductApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[product] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[product] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduProductSkus(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduProductSku(ctx: *zigmodu.http_server.Context) !void {
        const self: *ProductApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[product] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[product] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduProductSku(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduProductSku(ctx: *zigmodu.http_server.Context) !void {
        const self: *ProductApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduProductSku) catch {
            std.log.warn("[product] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduProductSku(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduProductSku(ctx: *zigmodu.http_server.Context) !void {
        const self: *ProductApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduProductSku) catch {
            std.log.warn("[product] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduProductSku(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduProductSku(ctx: *zigmodu.http_server.Context) !void {
        const self: *ProductApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[product] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduProductSku(id);
        try ctx.json(204, "");
    }

    fn listZmoduProductSpecRels(ctx: *zigmodu.http_server.Context) !void {
        const self: *ProductApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[product] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[product] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduProductSpecRels(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduProductSpecRel(ctx: *zigmodu.http_server.Context) !void {
        const self: *ProductApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[product] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[product] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduProductSpecRel(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduProductSpecRel(ctx: *zigmodu.http_server.Context) !void {
        const self: *ProductApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduProductSpecRel) catch {
            std.log.warn("[product] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduProductSpecRel(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduProductSpecRel(ctx: *zigmodu.http_server.Context) !void {
        const self: *ProductApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduProductSpecRel) catch {
            std.log.warn("[product] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduProductSpecRel(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduProductSpecRel(ctx: *zigmodu.http_server.Context) !void {
        const self: *ProductApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[product] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduProductSpecRel(id);
        try ctx.json(204, "");
    }

    fn listZmoduProductVirtuals(ctx: *zigmodu.http_server.Context) !void {
        const self: *ProductApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[product] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[product] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduProductVirtuals(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduProductVirtual(ctx: *zigmodu.http_server.Context) !void {
        const self: *ProductApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[product] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[product] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduProductVirtual(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduProductVirtual(ctx: *zigmodu.http_server.Context) !void {
        const self: *ProductApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduProductVirtual) catch {
            std.log.warn("[product] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduProductVirtual(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduProductVirtual(ctx: *zigmodu.http_server.Context) !void {
        const self: *ProductApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduProductVirtual) catch {
            std.log.warn("[product] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduProductVirtual(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduProductVirtual(ctx: *zigmodu.http_server.Context) !void {
        const self: *ProductApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[product] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduProductVirtual(id);
        try ctx.json(204, "");
    }

};
