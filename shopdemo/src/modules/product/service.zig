//! Auto-generated service for module: product
//! Generated by zmodu orm — templates: tools/zmodu/src/templates/orm/sqlx/
//!
//! ╔═══════════════════════════════════════════════════════════╗
//! ║  AI Metadata: module=product | layer=service      ║
//! ║  role=CRUD delegation | extends=service_ext.zig           ║
//! ╚═══════════════════════════════════════════════════════════╝

const std = @import("std");
const zigmodu = @import("zigmodu");
const model = @import("model.zig");
const persistence = @import("persistence.zig");

pub const ProductService = struct {
    persistence: *persistence.ProductPersistence,
    // EventBus — uncomment to enable event-driven communication:
    // event_bus: zigmodu.TypedEventBus(ProductEvent),

    pub fn init(persistence_ptr: *persistence.ProductPersistence) ProductService {
        return .{ .persistence = persistence_ptr };
    }

    // Event hooks (uncomment and define event types to enable):
    // fn afterCreate(self: *ProductService, entity: anytype) !void {
    //     try self.event_bus.publish(.{ .id = entity.id });
    // }
    // fn afterUpdate(self: *ProductService, entity: anytype) !void {
    //     try self.event_bus.publish(.{ .id = entity.id });
    // }
    // fn afterDelete(self: *ProductService, id: i64) !void {
    //     try self.event_bus.publish(.{ .id = id });
    // }

    pub fn listZmoduProducts(self: *ProductService, page: usize, size: usize) !zigmodu.orm.PageResult(model.ZmoduProduct) {
        var repo = self.persistence.zmoduProductRepo();
        return try repo.findPage(page, size);
    }

    pub fn getZmoduProduct(self: *ProductService, id: i64) !?model.ZmoduProduct {
        var repo = self.persistence.zmoduProductRepo();
        return try repo.findById(id);
    }

    pub fn createZmoduProduct(self: *ProductService, entity: model.ZmoduProduct) !model.ZmoduProduct {
        var repo = self.persistence.zmoduProductRepo();
        return try repo.insert(entity);
    }

    pub fn updateZmoduProduct(self: *ProductService, entity: model.ZmoduProduct) !void {
        var repo = self.persistence.zmoduProductRepo();
        return try repo.update(entity);
    }

    pub fn deleteZmoduProduct(self: *ProductService, id: i64) !void {
        var repo = self.persistence.zmoduProductRepo();
        return try repo.delete(id);
    }

    pub fn listZmoduProductImages(self: *ProductService, page: usize, size: usize) !zigmodu.orm.PageResult(model.ZmoduProductImage) {
        var repo = self.persistence.zmoduProductImageRepo();
        return try repo.findPage(page, size);
    }

    pub fn getZmoduProductImage(self: *ProductService, id: i64) !?model.ZmoduProductImage {
        var repo = self.persistence.zmoduProductImageRepo();
        return try repo.findById(id);
    }

    pub fn createZmoduProductImage(self: *ProductService, entity: model.ZmoduProductImage) !model.ZmoduProductImage {
        var repo = self.persistence.zmoduProductImageRepo();
        return try repo.insert(entity);
    }

    pub fn updateZmoduProductImage(self: *ProductService, entity: model.ZmoduProductImage) !void {
        var repo = self.persistence.zmoduProductImageRepo();
        return try repo.update(entity);
    }

    pub fn deleteZmoduProductImage(self: *ProductService, id: i64) !void {
        var repo = self.persistence.zmoduProductImageRepo();
        return try repo.delete(id);
    }

    pub fn listZmoduProductReduces(self: *ProductService, page: usize, size: usize) !zigmodu.orm.PageResult(model.ZmoduProductReduce) {
        var repo = self.persistence.zmoduProductReduceRepo();
        return try repo.findPage(page, size);
    }

    pub fn getZmoduProductReduce(self: *ProductService, id: i64) !?model.ZmoduProductReduce {
        var repo = self.persistence.zmoduProductReduceRepo();
        return try repo.findById(id);
    }

    pub fn createZmoduProductReduce(self: *ProductService, entity: model.ZmoduProductReduce) !model.ZmoduProductReduce {
        var repo = self.persistence.zmoduProductReduceRepo();
        return try repo.insert(entity);
    }

    pub fn updateZmoduProductReduce(self: *ProductService, entity: model.ZmoduProductReduce) !void {
        var repo = self.persistence.zmoduProductReduceRepo();
        return try repo.update(entity);
    }

    pub fn deleteZmoduProductReduce(self: *ProductService, id: i64) !void {
        var repo = self.persistence.zmoduProductReduceRepo();
        return try repo.delete(id);
    }

    pub fn listZmoduProductSkus(self: *ProductService, page: usize, size: usize) !zigmodu.orm.PageResult(model.ZmoduProductSku) {
        var repo = self.persistence.zmoduProductSkuRepo();
        return try repo.findPage(page, size);
    }

    pub fn getZmoduProductSku(self: *ProductService, id: i64) !?model.ZmoduProductSku {
        var repo = self.persistence.zmoduProductSkuRepo();
        return try repo.findById(id);
    }

    pub fn createZmoduProductSku(self: *ProductService, entity: model.ZmoduProductSku) !model.ZmoduProductSku {
        var repo = self.persistence.zmoduProductSkuRepo();
        return try repo.insert(entity);
    }

    pub fn updateZmoduProductSku(self: *ProductService, entity: model.ZmoduProductSku) !void {
        var repo = self.persistence.zmoduProductSkuRepo();
        return try repo.update(entity);
    }

    pub fn deleteZmoduProductSku(self: *ProductService, id: i64) !void {
        var repo = self.persistence.zmoduProductSkuRepo();
        return try repo.delete(id);
    }

    pub fn listZmoduProductSpecRels(self: *ProductService, page: usize, size: usize) !zigmodu.orm.PageResult(model.ZmoduProductSpecRel) {
        var repo = self.persistence.zmoduProductSpecRelRepo();
        return try repo.findPage(page, size);
    }

    pub fn getZmoduProductSpecRel(self: *ProductService, id: i64) !?model.ZmoduProductSpecRel {
        var repo = self.persistence.zmoduProductSpecRelRepo();
        return try repo.findById(id);
    }

    pub fn createZmoduProductSpecRel(self: *ProductService, entity: model.ZmoduProductSpecRel) !model.ZmoduProductSpecRel {
        var repo = self.persistence.zmoduProductSpecRelRepo();
        return try repo.insert(entity);
    }

    pub fn updateZmoduProductSpecRel(self: *ProductService, entity: model.ZmoduProductSpecRel) !void {
        var repo = self.persistence.zmoduProductSpecRelRepo();
        return try repo.update(entity);
    }

    pub fn deleteZmoduProductSpecRel(self: *ProductService, id: i64) !void {
        var repo = self.persistence.zmoduProductSpecRelRepo();
        return try repo.delete(id);
    }

    pub fn listZmoduProductVirtuals(self: *ProductService, page: usize, size: usize) !zigmodu.orm.PageResult(model.ZmoduProductVirtual) {
        var repo = self.persistence.zmoduProductVirtualRepo();
        return try repo.findPage(page, size);
    }

    pub fn getZmoduProductVirtual(self: *ProductService, id: i64) !?model.ZmoduProductVirtual {
        var repo = self.persistence.zmoduProductVirtualRepo();
        return try repo.findById(id);
    }

    pub fn createZmoduProductVirtual(self: *ProductService, entity: model.ZmoduProductVirtual) !model.ZmoduProductVirtual {
        var repo = self.persistence.zmoduProductVirtualRepo();
        return try repo.insert(entity);
    }

    pub fn updateZmoduProductVirtual(self: *ProductService, entity: model.ZmoduProductVirtual) !void {
        var repo = self.persistence.zmoduProductVirtualRepo();
        return try repo.update(entity);
    }

    pub fn deleteZmoduProductVirtual(self: *ProductService, id: i64) !void {
        var repo = self.persistence.zmoduProductVirtualRepo();
        return try repo.delete(id);
    }

};
