//! Auto-generated models for module: advance
//! Generated by zmodu orm — templates: tools/zmodu/src/templates/orm/sqlx/
//! Do not edit generated files by hand; change SQL and re-run `zmodu orm`.

const std = @import("std");

pub const ZmoduAdvanceProduct = struct {
    pub const sql_table_name: []const u8 = "zmodu_advance_product";
    advance_product_id: i64,
    product_id: i64,
    limit_num: i64,
    stock: i64,
    sort: ?i64,
    total_sales: i64,
    sales_initial: ?i64,
    join_num: ?i64,
    start_time: i64,
    end_time: i64,
    money: f64,
    status: i64,
    reduce_money: f64,
    audit_status: i64,
    shop_supplier_id: i64,
    app_id: i64,
    is_delete: ?i64,
    create_time: i64,
    update_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("advance_product_id");
        try jws.write(self.advance_product_id);
        try jws.objectField("product_id");
        try jws.write(self.product_id);
        try jws.objectField("limit_num");
        try jws.write(self.limit_num);
        try jws.objectField("stock");
        try jws.write(self.stock);
        try jws.objectField("sort");
        if (self.sort) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("total_sales");
        try jws.write(self.total_sales);
        try jws.objectField("sales_initial");
        if (self.sales_initial) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("join_num");
        if (self.join_num) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("start_time");
        try jws.write(self.start_time);
        try jws.objectField("end_time");
        try jws.write(self.end_time);
        try jws.objectField("money");
        try jws.write(self.money);
        try jws.objectField("status");
        try jws.write(self.status);
        try jws.objectField("reduce_money");
        try jws.write(self.reduce_money);
        try jws.objectField("audit_status");
        try jws.write(self.audit_status);
        try jws.objectField("shop_supplier_id");
        try jws.write(self.shop_supplier_id);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("is_delete");
        if (self.is_delete) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.objectField("update_time");
        try jws.write(self.update_time);
        try jws.endObject();
    }
};

pub const ZmoduAdvanceProductSku = struct {
    pub const sql_table_name: []const u8 = "zmodu_advance_product_sku";
    advance_product_sku_id: i64,
    advance_product_id: i64,
    product_id: i64,
    product_sku_id: i64,
    product_attr: ?[]const u8,
    advance_price: f64,
    advance_stock: i64,
    product_price: ?f64,
    app_id: i64,
    create_time: i64,
    update_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("advance_product_sku_id");
        try jws.write(self.advance_product_sku_id);
        try jws.objectField("advance_product_id");
        try jws.write(self.advance_product_id);
        try jws.objectField("product_id");
        try jws.write(self.product_id);
        try jws.objectField("product_sku_id");
        try jws.write(self.product_sku_id);
        try jws.objectField("product_attr");
        if (self.product_attr) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("advance_price");
        try jws.write(self.advance_price);
        try jws.objectField("advance_stock");
        try jws.write(self.advance_stock);
        try jws.objectField("product_price");
        if (self.product_price) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.objectField("update_time");
        try jws.write(self.update_time);
        try jws.endObject();
    }
};

