//! Auto-generated HTTP API for module: advance
//! Generated by zmodu orm — uses `zigmodu.http_server` + `ctx.bindJson` / `ctx.jsonStruct`.

const std = @import("std");
const zigmodu = @import("zigmodu");
const service = @import("service.zig");
const model = @import("model.zig");

pub const AdvanceApi = struct {
    service: *service.AdvanceService,

    pub fn init(service_ptr: *service.AdvanceService) AdvanceApi {
        return .{ .service = service_ptr };
    }

    pub fn registerRoutes(self: *AdvanceApi, group: *zigmodu.http_server.RouteGroup) !void {
        try group.get("/zmodu_advance_products", listZmoduAdvanceProducts, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_advance_products/:id", getZmoduAdvanceProduct, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_advance_products", createZmoduAdvanceProduct, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_advance_products/:id", updateZmoduAdvanceProduct, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_advance_products/:id", deleteZmoduAdvanceProduct, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_advance_product_skus", listZmoduAdvanceProductSkus, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_advance_product_skus/:id", getZmoduAdvanceProductSku, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_advance_product_skus", createZmoduAdvanceProductSku, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_advance_product_skus/:id", updateZmoduAdvanceProductSku, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_advance_product_skus/:id", deleteZmoduAdvanceProductSku, @ptrCast(@alignCast(self)));
    }

    fn listZmoduAdvanceProducts(ctx: *zigmodu.http_server.Context) !void {
        const self: *AdvanceApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[advance] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[advance] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduAdvanceProducts(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduAdvanceProduct(ctx: *zigmodu.http_server.Context) !void {
        const self: *AdvanceApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[advance] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[advance] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduAdvanceProduct(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduAdvanceProduct(ctx: *zigmodu.http_server.Context) !void {
        const self: *AdvanceApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAdvanceProduct) catch {
            std.log.warn("[advance] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduAdvanceProduct(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduAdvanceProduct(ctx: *zigmodu.http_server.Context) !void {
        const self: *AdvanceApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAdvanceProduct) catch {
            std.log.warn("[advance] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduAdvanceProduct(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduAdvanceProduct(ctx: *zigmodu.http_server.Context) !void {
        const self: *AdvanceApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[advance] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduAdvanceProduct(id);
        try ctx.json(204, "");
    }

    fn listZmoduAdvanceProductSkus(ctx: *zigmodu.http_server.Context) !void {
        const self: *AdvanceApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[advance] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[advance] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduAdvanceProductSkus(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduAdvanceProductSku(ctx: *zigmodu.http_server.Context) !void {
        const self: *AdvanceApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[advance] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[advance] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduAdvanceProductSku(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduAdvanceProductSku(ctx: *zigmodu.http_server.Context) !void {
        const self: *AdvanceApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAdvanceProductSku) catch {
            std.log.warn("[advance] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduAdvanceProductSku(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduAdvanceProductSku(ctx: *zigmodu.http_server.Context) !void {
        const self: *AdvanceApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAdvanceProductSku) catch {
            std.log.warn("[advance] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduAdvanceProductSku(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduAdvanceProductSku(ctx: *zigmodu.http_server.Context) !void {
        const self: *AdvanceApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[advance] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduAdvanceProductSku(id);
        try ctx.json(204, "");
    }

};
