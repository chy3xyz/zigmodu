//! Auto-generated HTTP API for module: plus
//! Generated by zmodu orm — uses `zigmodu.http_server` + `ctx.bindJson` / `ctx.jsonStruct`.

const std = @import("std");
const zigmodu = @import("zigmodu");
const service = @import("service.zig");
const model = @import("model.zig");

pub const PlusApi = struct {
    service: *service.PlusService,

    pub fn init(service_ptr: *service.PlusService) PlusApi {
        return .{ .service = service_ptr };
    }

    pub fn registerRoutes(self: *PlusApi, group: *zigmodu.http_server.RouteGroup) !void {
        try group.get("/zmodu_plus_categorys", listZmoduPlusCategorys, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_plus_categorys/:id", getZmoduPlusCategory, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_plus_categorys", createZmoduPlusCategory, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_plus_categorys/:id", updateZmoduPlusCategory, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_plus_categorys/:id", deleteZmoduPlusCategory, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_plus_wx_collections", listZmoduPlusWxCollections, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_plus_wx_collections/:id", getZmoduPlusWxCollection, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_plus_wx_collections", createZmoduPlusWxCollection, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_plus_wx_collections/:id", updateZmoduPlusWxCollection, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_plus_wx_collections/:id", deleteZmoduPlusWxCollection, @ptrCast(@alignCast(self)));
    }

    fn listZmoduPlusCategorys(ctx: *zigmodu.http_server.Context) !void {
        const self: *PlusApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[plus] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[plus] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduPlusCategorys(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduPlusCategory(ctx: *zigmodu.http_server.Context) !void {
        const self: *PlusApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[plus] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[plus] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduPlusCategory(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduPlusCategory(ctx: *zigmodu.http_server.Context) !void {
        const self: *PlusApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduPlusCategory) catch {
            std.log.warn("[plus] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduPlusCategory(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduPlusCategory(ctx: *zigmodu.http_server.Context) !void {
        const self: *PlusApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduPlusCategory) catch {
            std.log.warn("[plus] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduPlusCategory(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduPlusCategory(ctx: *zigmodu.http_server.Context) !void {
        const self: *PlusApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[plus] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduPlusCategory(id);
        try ctx.json(204, "");
    }

    fn listZmoduPlusWxCollections(ctx: *zigmodu.http_server.Context) !void {
        const self: *PlusApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[plus] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[plus] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduPlusWxCollections(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduPlusWxCollection(ctx: *zigmodu.http_server.Context) !void {
        const self: *PlusApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[plus] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[plus] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduPlusWxCollection(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduPlusWxCollection(ctx: *zigmodu.http_server.Context) !void {
        const self: *PlusApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduPlusWxCollection) catch {
            std.log.warn("[plus] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduPlusWxCollection(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduPlusWxCollection(ctx: *zigmodu.http_server.Context) !void {
        const self: *PlusApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduPlusWxCollection) catch {
            std.log.warn("[plus] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduPlusWxCollection(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduPlusWxCollection(ctx: *zigmodu.http_server.Context) !void {
        const self: *PlusApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[plus] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduPlusWxCollection(id);
        try ctx.json(204, "");
    }

};
