//! Auto-generated HTTP API for module: app
//! Generated by zmodu orm — uses `zigmodu.http_server` + `ctx.bindJson` / `ctx.jsonStruct`.

const std = @import("std");
const zigmodu = @import("zigmodu");
const service = @import("service.zig");
const model = @import("model.zig");

pub const AppApi = struct {
    service: *service.AppService,

    pub fn init(service_ptr: *service.AppService) AppApi {
        return .{ .service = service_ptr };
    }

    pub fn registerRoutes(self: *AppApi, group: *zigmodu.http_server.RouteGroup) !void {
        try group.get("/zmodu_apps", listZmoduApps, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_apps/:id", getZmoduApp, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_apps", createZmoduApp, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_apps/:id", updateZmoduApp, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_apps/:id", deleteZmoduApp, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_app_mps", listZmoduAppMps, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_app_mps/:id", getZmoduAppMp, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_app_mps", createZmoduAppMp, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_app_mps/:id", updateZmoduAppMp, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_app_mps/:id", deleteZmoduAppMp, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_app_opens", listZmoduAppOpens, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_app_opens/:id", getZmoduAppOpen, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_app_opens", createZmoduAppOpen, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_app_opens/:id", updateZmoduAppOpen, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_app_opens/:id", deleteZmoduAppOpen, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_app_updates", listZmoduAppUpdates, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_app_updates/:id", getZmoduAppUpdate, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_app_updates", createZmoduAppUpdate, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_app_updates/:id", updateZmoduAppUpdate, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_app_updates/:id", deleteZmoduAppUpdate, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_app_wxs", listZmoduAppWxs, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_app_wxs/:id", getZmoduAppWx, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_app_wxs", createZmoduAppWx, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_app_wxs/:id", updateZmoduAppWx, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_app_wxs/:id", deleteZmoduAppWx, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_app_wx_lives", listZmoduAppWxLives, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_app_wx_lives/:id", getZmoduAppWxLive, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_app_wx_lives", createZmoduAppWxLive, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_app_wx_lives/:id", updateZmoduAppWxLive, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_app_wx_lives/:id", deleteZmoduAppWxLive, @ptrCast(@alignCast(self)));
    }

    fn listZmoduApps(ctx: *zigmodu.http_server.Context) !void {
        const self: *AppApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[app] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[app] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduApps(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduApp(ctx: *zigmodu.http_server.Context) !void {
        const self: *AppApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[app] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[app] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduApp(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduApp(ctx: *zigmodu.http_server.Context) !void {
        const self: *AppApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduApp) catch {
            std.log.warn("[app] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduApp(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduApp(ctx: *zigmodu.http_server.Context) !void {
        const self: *AppApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduApp) catch {
            std.log.warn("[app] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduApp(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduApp(ctx: *zigmodu.http_server.Context) !void {
        const self: *AppApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[app] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduApp(id);
        try ctx.json(204, "");
    }

    fn listZmoduAppMps(ctx: *zigmodu.http_server.Context) !void {
        const self: *AppApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[app] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[app] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduAppMps(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduAppMp(ctx: *zigmodu.http_server.Context) !void {
        const self: *AppApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[app] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[app] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduAppMp(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduAppMp(ctx: *zigmodu.http_server.Context) !void {
        const self: *AppApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAppMp) catch {
            std.log.warn("[app] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduAppMp(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduAppMp(ctx: *zigmodu.http_server.Context) !void {
        const self: *AppApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAppMp) catch {
            std.log.warn("[app] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduAppMp(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduAppMp(ctx: *zigmodu.http_server.Context) !void {
        const self: *AppApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[app] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduAppMp(id);
        try ctx.json(204, "");
    }

    fn listZmoduAppOpens(ctx: *zigmodu.http_server.Context) !void {
        const self: *AppApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[app] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[app] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduAppOpens(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduAppOpen(ctx: *zigmodu.http_server.Context) !void {
        const self: *AppApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[app] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[app] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduAppOpen(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduAppOpen(ctx: *zigmodu.http_server.Context) !void {
        const self: *AppApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAppOpen) catch {
            std.log.warn("[app] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduAppOpen(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduAppOpen(ctx: *zigmodu.http_server.Context) !void {
        const self: *AppApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAppOpen) catch {
            std.log.warn("[app] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduAppOpen(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduAppOpen(ctx: *zigmodu.http_server.Context) !void {
        const self: *AppApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[app] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduAppOpen(id);
        try ctx.json(204, "");
    }

    fn listZmoduAppUpdates(ctx: *zigmodu.http_server.Context) !void {
        const self: *AppApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[app] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[app] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduAppUpdates(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduAppUpdate(ctx: *zigmodu.http_server.Context) !void {
        const self: *AppApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[app] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[app] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduAppUpdate(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduAppUpdate(ctx: *zigmodu.http_server.Context) !void {
        const self: *AppApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAppUpdate) catch {
            std.log.warn("[app] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduAppUpdate(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduAppUpdate(ctx: *zigmodu.http_server.Context) !void {
        const self: *AppApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAppUpdate) catch {
            std.log.warn("[app] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduAppUpdate(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduAppUpdate(ctx: *zigmodu.http_server.Context) !void {
        const self: *AppApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[app] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduAppUpdate(id);
        try ctx.json(204, "");
    }

    fn listZmoduAppWxs(ctx: *zigmodu.http_server.Context) !void {
        const self: *AppApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[app] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[app] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduAppWxs(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduAppWx(ctx: *zigmodu.http_server.Context) !void {
        const self: *AppApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[app] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[app] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduAppWx(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduAppWx(ctx: *zigmodu.http_server.Context) !void {
        const self: *AppApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAppWx) catch {
            std.log.warn("[app] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduAppWx(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduAppWx(ctx: *zigmodu.http_server.Context) !void {
        const self: *AppApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAppWx) catch {
            std.log.warn("[app] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduAppWx(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduAppWx(ctx: *zigmodu.http_server.Context) !void {
        const self: *AppApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[app] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduAppWx(id);
        try ctx.json(204, "");
    }

    fn listZmoduAppWxLives(ctx: *zigmodu.http_server.Context) !void {
        const self: *AppApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[app] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[app] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduAppWxLives(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduAppWxLive(ctx: *zigmodu.http_server.Context) !void {
        const self: *AppApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[app] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[app] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduAppWxLive(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduAppWxLive(ctx: *zigmodu.http_server.Context) !void {
        const self: *AppApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAppWxLive) catch {
            std.log.warn("[app] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduAppWxLive(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduAppWxLive(ctx: *zigmodu.http_server.Context) !void {
        const self: *AppApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAppWxLive) catch {
            std.log.warn("[app] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduAppWxLive(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduAppWxLive(ctx: *zigmodu.http_server.Context) !void {
        const self: *AppApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[app] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduAppWxLive(id);
        try ctx.json(204, "");
    }

};
