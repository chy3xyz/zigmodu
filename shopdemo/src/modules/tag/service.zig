//! Auto-generated service for module: tag
//! Generated by zmodu orm — templates: tools/zmodu/src/templates/orm/sqlx/
//!
//! ╔═══════════════════════════════════════════════════════════╗
//! ║  AI Metadata: module=tag | layer=service      ║
//! ║  role=CRUD delegation | extends=service_ext.zig           ║
//! ╚═══════════════════════════════════════════════════════════╝

const std = @import("std");
const zigmodu = @import("zigmodu");
const model = @import("model.zig");
const persistence = @import("persistence.zig");

pub const TagService = struct {
    persistence: *persistence.TagPersistence,
    // EventBus — uncomment to enable event-driven communication:
    // event_bus: zigmodu.TypedEventBus(TagEvent),

    pub fn init(persistence_ptr: *persistence.TagPersistence) TagService {
        return .{ .persistence = persistence_ptr };
    }

    // Event hooks (uncomment and define event types to enable):
    // fn afterCreate(self: *TagService, entity: anytype) !void {
    //     try self.event_bus.publish(.{ .id = entity.id });
    // }
    // fn afterUpdate(self: *TagService, entity: anytype) !void {
    //     try self.event_bus.publish(.{ .id = entity.id });
    // }
    // fn afterDelete(self: *TagService, id: i64) !void {
    //     try self.event_bus.publish(.{ .id = id });
    // }

    pub fn listZmoduTags(self: *TagService, page: usize, size: usize) !zigmodu.orm.PageResult(model.ZmoduTag) {
        var repo = self.persistence.zmoduTagRepo();
        return try repo.findPage(page, size);
    }

    pub fn getZmoduTag(self: *TagService, id: i64) !?model.ZmoduTag {
        var repo = self.persistence.zmoduTagRepo();
        return try repo.findById(id);
    }

    pub fn createZmoduTag(self: *TagService, entity: model.ZmoduTag) !model.ZmoduTag {
        var repo = self.persistence.zmoduTagRepo();
        return try repo.insert(entity);
    }

    pub fn updateZmoduTag(self: *TagService, entity: model.ZmoduTag) !void {
        var repo = self.persistence.zmoduTagRepo();
        return try repo.update(entity);
    }

    pub fn deleteZmoduTag(self: *TagService, id: i64) !void {
        var repo = self.persistence.zmoduTagRepo();
        return try repo.delete(id);
    }

};
