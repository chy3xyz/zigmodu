//! Auto-generated service for module: table
//! Generated by zmodu orm — templates: tools/zmodu/src/templates/orm/sqlx/
//!
//! ╔═══════════════════════════════════════════════════════════╗
//! ║  AI Metadata: module=table | layer=service      ║
//! ║  role=CRUD delegation | extends=service_ext.zig           ║
//! ╚═══════════════════════════════════════════════════════════╝

const std = @import("std");
const zigmodu = @import("zigmodu");
const model = @import("model.zig");
const persistence = @import("persistence.zig");

pub const TableService = struct {
    persistence: *persistence.TablePersistence,
    // EventBus — uncomment to enable event-driven communication:
    // event_bus: zigmodu.TypedEventBus(TableEvent),

    pub fn init(persistence_ptr: *persistence.TablePersistence) TableService {
        return .{ .persistence = persistence_ptr };
    }

    // Event hooks (uncomment and define event types to enable):
    // fn afterCreate(self: *TableService, entity: anytype) !void {
    //     try self.event_bus.publish(.{ .id = entity.id });
    // }
    // fn afterUpdate(self: *TableService, entity: anytype) !void {
    //     try self.event_bus.publish(.{ .id = entity.id });
    // }
    // fn afterDelete(self: *TableService, id: i64) !void {
    //     try self.event_bus.publish(.{ .id = id });
    // }

    pub fn listZmoduTables(self: *TableService, page: usize, size: usize) !zigmodu.orm.PageResult(model.ZmoduTable) {
        var repo = self.persistence.zmoduTableRepo();
        return try repo.findPage(page, size);
    }

    pub fn getZmoduTable(self: *TableService, id: i64) !?model.ZmoduTable {
        var repo = self.persistence.zmoduTableRepo();
        return try repo.findById(id);
    }

    pub fn createZmoduTable(self: *TableService, entity: model.ZmoduTable) !model.ZmoduTable {
        var repo = self.persistence.zmoduTableRepo();
        return try repo.insert(entity);
    }

    pub fn updateZmoduTable(self: *TableService, entity: model.ZmoduTable) !void {
        var repo = self.persistence.zmoduTableRepo();
        return try repo.update(entity);
    }

    pub fn deleteZmoduTable(self: *TableService, id: i64) !void {
        var repo = self.persistence.zmoduTableRepo();
        return try repo.delete(id);
    }

    pub fn listZmoduTableRecords(self: *TableService, page: usize, size: usize) !zigmodu.orm.PageResult(model.ZmoduTableRecord) {
        var repo = self.persistence.zmoduTableRecordRepo();
        return try repo.findPage(page, size);
    }

    pub fn getZmoduTableRecord(self: *TableService, id: i64) !?model.ZmoduTableRecord {
        var repo = self.persistence.zmoduTableRecordRepo();
        return try repo.findById(id);
    }

    pub fn createZmoduTableRecord(self: *TableService, entity: model.ZmoduTableRecord) !model.ZmoduTableRecord {
        var repo = self.persistence.zmoduTableRecordRepo();
        return try repo.insert(entity);
    }

    pub fn updateZmoduTableRecord(self: *TableService, entity: model.ZmoduTableRecord) !void {
        var repo = self.persistence.zmoduTableRecordRepo();
        return try repo.update(entity);
    }

    pub fn deleteZmoduTableRecord(self: *TableService, id: i64) !void {
        var repo = self.persistence.zmoduTableRecordRepo();
        return try repo.delete(id);
    }

};
