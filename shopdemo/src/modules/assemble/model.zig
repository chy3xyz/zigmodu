//! Auto-generated models for module: assemble
//! Generated by zmodu orm — templates: tools/zmodu/src/templates/orm/sqlx/
//! Do not edit generated files by hand; change SQL and re-run `zmodu orm`.

const std = @import("std");

pub const ZmoduAssembleBill = struct {
    pub const sql_table_name: []const u8 = "zmodu_assemble_bill";
    assemble_bill_id: i64,
    assemble_product_id: i64,
    actual_people: i64,
    creator_id: i64,
    status: i64,
    end_time: ?i64,
    shop_supplier_id: ?i64,
    app_id: i64,
    create_time: i64,
    update_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("assemble_bill_id");
        try jws.write(self.assemble_bill_id);
        try jws.objectField("assemble_product_id");
        try jws.write(self.assemble_product_id);
        try jws.objectField("actual_people");
        try jws.write(self.actual_people);
        try jws.objectField("creator_id");
        try jws.write(self.creator_id);
        try jws.objectField("status");
        try jws.write(self.status);
        try jws.objectField("end_time");
        if (self.end_time) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("shop_supplier_id");
        if (self.shop_supplier_id) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.objectField("update_time");
        try jws.write(self.update_time);
        try jws.endObject();
    }
};

pub const ZmoduAssembleBillUser = struct {
    pub const sql_table_name: []const u8 = "zmodu_assemble_bill_user";
    assemble_bill_user_id: i64,
    assemble_bill_id: i64,
    order_id: i64,
    user_id: i64,
    is_creator: i64,
    nickName: []const u8,
    avatarUrl: []const u8,
    is_virtual: i64,
    app_id: i64,
    create_time: i64,
    update_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("assemble_bill_user_id");
        try jws.write(self.assemble_bill_user_id);
        try jws.objectField("assemble_bill_id");
        try jws.write(self.assemble_bill_id);
        try jws.objectField("order_id");
        try jws.write(self.order_id);
        try jws.objectField("user_id");
        try jws.write(self.user_id);
        try jws.objectField("is_creator");
        try jws.write(self.is_creator);
        try jws.objectField("nickName");
        try jws.write(self.nickName);
        try jws.objectField("avatarUrl");
        try jws.write(self.avatarUrl);
        try jws.objectField("is_virtual");
        try jws.write(self.is_virtual);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.objectField("update_time");
        try jws.write(self.update_time);
        try jws.endObject();
    }
};

pub const ZmoduAssembleProduct = struct {
    pub const sql_table_name: []const u8 = "zmodu_assemble_product";
    assemble_product_id: i64,
    product_id: i64,
    limit_num: i64,
    stock: i64,
    total_sales: i64,
    sales_initial: ?i64,
    assemble_num: i64,
    join_num: ?i64,
    sort: i64,
    status: ?i64,
    remark: ?[]const u8,
    is_delete: i64,
    app_id: i64,
    create_time: i64,
    update_time: i64,
    shop_supplier_id: ?i64,
    state: i64,
    start_time: i64,
    end_time: i64,
    image_id: i64,
    banner: ?[]const u8,
    product_name: []const u8,
    logistics: []const u8,
    delivery_id: i64,
    together_time: i64,
    fake_num: i64,
    single_num: i64,
    is_hot: i64,
    content: ?[]const u8,
    assemble_price: f64,
    product_price: f64,
    is_single: i64,
    is_show: i64,
    selling_point: []const u8,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("assemble_product_id");
        try jws.write(self.assemble_product_id);
        try jws.objectField("product_id");
        try jws.write(self.product_id);
        try jws.objectField("limit_num");
        try jws.write(self.limit_num);
        try jws.objectField("stock");
        try jws.write(self.stock);
        try jws.objectField("total_sales");
        try jws.write(self.total_sales);
        try jws.objectField("sales_initial");
        if (self.sales_initial) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("assemble_num");
        try jws.write(self.assemble_num);
        try jws.objectField("join_num");
        if (self.join_num) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("sort");
        try jws.write(self.sort);
        try jws.objectField("status");
        if (self.status) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("remark");
        if (self.remark) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("is_delete");
        try jws.write(self.is_delete);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.objectField("update_time");
        try jws.write(self.update_time);
        try jws.objectField("shop_supplier_id");
        if (self.shop_supplier_id) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("state");
        try jws.write(self.state);
        try jws.objectField("start_time");
        try jws.write(self.start_time);
        try jws.objectField("end_time");
        try jws.write(self.end_time);
        try jws.objectField("image_id");
        try jws.write(self.image_id);
        try jws.objectField("banner");
        if (self.banner) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("product_name");
        try jws.write(self.product_name);
        try jws.objectField("logistics");
        try jws.write(self.logistics);
        try jws.objectField("delivery_id");
        try jws.write(self.delivery_id);
        try jws.objectField("together_time");
        try jws.write(self.together_time);
        try jws.objectField("fake_num");
        try jws.write(self.fake_num);
        try jws.objectField("single_num");
        try jws.write(self.single_num);
        try jws.objectField("is_hot");
        try jws.write(self.is_hot);
        try jws.objectField("content");
        if (self.content) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("assemble_price");
        try jws.write(self.assemble_price);
        try jws.objectField("product_price");
        try jws.write(self.product_price);
        try jws.objectField("is_single");
        try jws.write(self.is_single);
        try jws.objectField("is_show");
        try jws.write(self.is_show);
        try jws.objectField("selling_point");
        try jws.write(self.selling_point);
        try jws.endObject();
    }
};

pub const ZmoduAssembleProductSku = struct {
    pub const sql_table_name: []const u8 = "zmodu_assemble_product_sku";
    assemble_product_sku_id: i64,
    assemble_product_id: i64,
    product_id: i64,
    product_sku_id: i64,
    assemble_stock: i64,
    assemble_price: f64,
    product_attr: ?[]const u8,
    product_price: ?f64,
    total_sales: i64,
    status: ?i64,
    app_id: i64,
    create_time: i64,
    update_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("assemble_product_sku_id");
        try jws.write(self.assemble_product_sku_id);
        try jws.objectField("assemble_product_id");
        try jws.write(self.assemble_product_id);
        try jws.objectField("product_id");
        try jws.write(self.product_id);
        try jws.objectField("product_sku_id");
        try jws.write(self.product_sku_id);
        try jws.objectField("assemble_stock");
        try jws.write(self.assemble_stock);
        try jws.objectField("assemble_price");
        try jws.write(self.assemble_price);
        try jws.objectField("product_attr");
        if (self.product_attr) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("product_price");
        if (self.product_price) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("total_sales");
        try jws.write(self.total_sales);
        try jws.objectField("status");
        if (self.status) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.objectField("update_time");
        try jws.write(self.update_time);
        try jws.endObject();
    }
};

