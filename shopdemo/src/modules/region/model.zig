//! Auto-generated models for module: region
//! Generated by zmodu orm — templates: tools/zmodu/src/templates/orm/sqlx/
//! Do not edit generated files by hand; change SQL and re-run `zmodu orm`.

const std = @import("std");

pub const ZmoduRegion = struct {
    pub const sql_table_name: []const u8 = "zmodu_region";
    id: i64,
    pid: ?i64,
    shortname: ?[]const u8,
    name: ?[]const u8,
    merger_name: ?[]const u8,
    level: ?i64,
    pinyin: ?[]const u8,
    code: ?[]const u8,
    zip_code: ?[]const u8,
    first: ?[]const u8,
    lng: ?[]const u8,
    lat: ?[]const u8,
    sort: ?i64,
    is_delete: ?i64,
    create_time: ?i64,
    update_time: ?i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("id");
        try jws.write(self.id);
        try jws.objectField("pid");
        if (self.pid) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("shortname");
        if (self.shortname) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("name");
        if (self.name) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("merger_name");
        if (self.merger_name) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("level");
        if (self.level) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("pinyin");
        if (self.pinyin) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("code");
        if (self.code) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("zip_code");
        if (self.zip_code) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("first");
        if (self.first) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("lng");
        if (self.lng) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("lat");
        if (self.lat) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("sort");
        if (self.sort) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("is_delete");
        if (self.is_delete) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("create_time");
        if (self.create_time) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("update_time");
        if (self.update_time) |v| try jws.write(v) else try jws.write(null);
        try jws.endObject();
    }
};

