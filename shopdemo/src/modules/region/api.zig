//! Auto-generated HTTP API for module: region
//! Generated by zmodu orm — uses `zigmodu.http_server` + `ctx.bindJson` / `ctx.jsonStruct`.

const std = @import("std");
const zigmodu = @import("zigmodu");
const service = @import("service.zig");
const model = @import("model.zig");

pub const RegionApi = struct {
    service: *service.RegionService,

    pub fn init(service_ptr: *service.RegionService) RegionApi {
        return .{ .service = service_ptr };
    }

    pub fn registerRoutes(self: *RegionApi, group: *zigmodu.http_server.RouteGroup) !void {
        try group.get("/zmodu_regions", listZmoduRegions, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_regions/:id", getZmoduRegion, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_regions", createZmoduRegion, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_regions/:id", updateZmoduRegion, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_regions/:id", deleteZmoduRegion, @ptrCast(@alignCast(self)));
    }

    fn listZmoduRegions(ctx: *zigmodu.http_server.Context) !void {
        const self: *RegionApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[region] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[region] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduRegions(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduRegion(ctx: *zigmodu.http_server.Context) !void {
        const self: *RegionApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[region] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[region] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduRegion(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduRegion(ctx: *zigmodu.http_server.Context) !void {
        const self: *RegionApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduRegion) catch {
            std.log.warn("[region] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduRegion(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduRegion(ctx: *zigmodu.http_server.Context) !void {
        const self: *RegionApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduRegion) catch {
            std.log.warn("[region] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduRegion(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduRegion(ctx: *zigmodu.http_server.Context) !void {
        const self: *RegionApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[region] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduRegion(id);
        try ctx.json(204, "");
    }

};
