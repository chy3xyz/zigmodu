//! Auto-generated service for module: region
//! Generated by zmodu orm — templates: tools/zmodu/src/templates/orm/sqlx/
//!
//! ╔═══════════════════════════════════════════════════════════╗
//! ║  AI Metadata: module=region | layer=service      ║
//! ║  role=CRUD delegation | extends=service_ext.zig           ║
//! ╚═══════════════════════════════════════════════════════════╝

const std = @import("std");
const zigmodu = @import("zigmodu");
const model = @import("model.zig");
const persistence = @import("persistence.zig");

pub const RegionService = struct {
    persistence: *persistence.RegionPersistence,
    // EventBus — uncomment to enable event-driven communication:
    // event_bus: zigmodu.TypedEventBus(RegionEvent),

    pub fn init(persistence_ptr: *persistence.RegionPersistence) RegionService {
        return .{ .persistence = persistence_ptr };
    }

    // Event hooks (uncomment and define event types to enable):
    // fn afterCreate(self: *RegionService, entity: anytype) !void {
    //     try self.event_bus.publish(.{ .id = entity.id });
    // }
    // fn afterUpdate(self: *RegionService, entity: anytype) !void {
    //     try self.event_bus.publish(.{ .id = entity.id });
    // }
    // fn afterDelete(self: *RegionService, id: i64) !void {
    //     try self.event_bus.publish(.{ .id = id });
    // }

    pub fn listZmoduRegions(self: *RegionService, page: usize, size: usize) !zigmodu.orm.PageResult(model.ZmoduRegion) {
        var repo = self.persistence.zmoduRegionRepo();
        return try repo.findPage(page, size);
    }

    pub fn getZmoduRegion(self: *RegionService, id: i64) !?model.ZmoduRegion {
        var repo = self.persistence.zmoduRegionRepo();
        return try repo.findById(id);
    }

    pub fn createZmoduRegion(self: *RegionService, entity: model.ZmoduRegion) !model.ZmoduRegion {
        var repo = self.persistence.zmoduRegionRepo();
        return try repo.insert(entity);
    }

    pub fn updateZmoduRegion(self: *RegionService, entity: model.ZmoduRegion) !void {
        var repo = self.persistence.zmoduRegionRepo();
        return try repo.update(entity);
    }

    pub fn deleteZmoduRegion(self: *RegionService, id: i64) !void {
        var repo = self.persistence.zmoduRegionRepo();
        return try repo.delete(id);
    }

};
