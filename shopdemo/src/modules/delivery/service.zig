//! Auto-generated service for module: delivery
//! Generated by zmodu orm — templates: tools/zmodu/src/templates/orm/sqlx/
//!
//! ╔═══════════════════════════════════════════════════════════╗
//! ║  AI Metadata: module=delivery | layer=service      ║
//! ║  role=CRUD delegation | extends=service_ext.zig           ║
//! ╚═══════════════════════════════════════════════════════════╝

const std = @import("std");
const zigmodu = @import("zigmodu");
const model = @import("model.zig");
const persistence = @import("persistence.zig");

pub const DeliveryService = struct {
    persistence: *persistence.DeliveryPersistence,
    // EventBus — uncomment to enable event-driven communication:
    // event_bus: zigmodu.TypedEventBus(DeliveryEvent),

    pub fn init(persistence_ptr: *persistence.DeliveryPersistence) DeliveryService {
        return .{ .persistence = persistence_ptr };
    }

    // Event hooks (uncomment and define event types to enable):
    // fn afterCreate(self: *DeliveryService, entity: anytype) !void {
    //     try self.event_bus.publish(.{ .id = entity.id });
    // }
    // fn afterUpdate(self: *DeliveryService, entity: anytype) !void {
    //     try self.event_bus.publish(.{ .id = entity.id });
    // }
    // fn afterDelete(self: *DeliveryService, id: i64) !void {
    //     try self.event_bus.publish(.{ .id = id });
    // }

    pub fn listZmoduDeliverys(self: *DeliveryService, page: usize, size: usize) !zigmodu.orm.PageResult(model.ZmoduDelivery) {
        var repo = self.persistence.zmoduDeliveryRepo();
        return try repo.findPage(page, size);
    }

    pub fn getZmoduDelivery(self: *DeliveryService, id: i64) !?model.ZmoduDelivery {
        var repo = self.persistence.zmoduDeliveryRepo();
        return try repo.findById(id);
    }

    pub fn createZmoduDelivery(self: *DeliveryService, entity: model.ZmoduDelivery) !model.ZmoduDelivery {
        var repo = self.persistence.zmoduDeliveryRepo();
        return try repo.insert(entity);
    }

    pub fn updateZmoduDelivery(self: *DeliveryService, entity: model.ZmoduDelivery) !void {
        var repo = self.persistence.zmoduDeliveryRepo();
        return try repo.update(entity);
    }

    pub fn deleteZmoduDelivery(self: *DeliveryService, id: i64) !void {
        var repo = self.persistence.zmoduDeliveryRepo();
        return try repo.delete(id);
    }

    pub fn listZmoduDeliveryRules(self: *DeliveryService, page: usize, size: usize) !zigmodu.orm.PageResult(model.ZmoduDeliveryRule) {
        var repo = self.persistence.zmoduDeliveryRuleRepo();
        return try repo.findPage(page, size);
    }

    pub fn getZmoduDeliveryRule(self: *DeliveryService, id: i64) !?model.ZmoduDeliveryRule {
        var repo = self.persistence.zmoduDeliveryRuleRepo();
        return try repo.findById(id);
    }

    pub fn createZmoduDeliveryRule(self: *DeliveryService, entity: model.ZmoduDeliveryRule) !model.ZmoduDeliveryRule {
        var repo = self.persistence.zmoduDeliveryRuleRepo();
        return try repo.insert(entity);
    }

    pub fn updateZmoduDeliveryRule(self: *DeliveryService, entity: model.ZmoduDeliveryRule) !void {
        var repo = self.persistence.zmoduDeliveryRuleRepo();
        return try repo.update(entity);
    }

    pub fn deleteZmoduDeliveryRule(self: *DeliveryService, id: i64) !void {
        var repo = self.persistence.zmoduDeliveryRuleRepo();
        return try repo.delete(id);
    }

    pub fn listZmoduDeliverySettings(self: *DeliveryService, page: usize, size: usize) !zigmodu.orm.PageResult(model.ZmoduDeliverySetting) {
        var repo = self.persistence.zmoduDeliverySettingRepo();
        return try repo.findPage(page, size);
    }

    pub fn getZmoduDeliverySetting(self: *DeliveryService, id: i64) !?model.ZmoduDeliverySetting {
        var repo = self.persistence.zmoduDeliverySettingRepo();
        return try repo.findById(id);
    }

    pub fn createZmoduDeliverySetting(self: *DeliveryService, entity: model.ZmoduDeliverySetting) !model.ZmoduDeliverySetting {
        var repo = self.persistence.zmoduDeliverySettingRepo();
        return try repo.insert(entity);
    }

    pub fn updateZmoduDeliverySetting(self: *DeliveryService, entity: model.ZmoduDeliverySetting) !void {
        var repo = self.persistence.zmoduDeliverySettingRepo();
        return try repo.update(entity);
    }

    pub fn deleteZmoduDeliverySetting(self: *DeliveryService, id: i64) !void {
        var repo = self.persistence.zmoduDeliverySettingRepo();
        return try repo.delete(id);
    }

    pub fn listZmoduDeliveryTemplates(self: *DeliveryService, page: usize, size: usize) !zigmodu.orm.PageResult(model.ZmoduDeliveryTemplate) {
        var repo = self.persistence.zmoduDeliveryTemplateRepo();
        return try repo.findPage(page, size);
    }

    pub fn getZmoduDeliveryTemplate(self: *DeliveryService, id: i64) !?model.ZmoduDeliveryTemplate {
        var repo = self.persistence.zmoduDeliveryTemplateRepo();
        return try repo.findById(id);
    }

    pub fn createZmoduDeliveryTemplate(self: *DeliveryService, entity: model.ZmoduDeliveryTemplate) !model.ZmoduDeliveryTemplate {
        var repo = self.persistence.zmoduDeliveryTemplateRepo();
        return try repo.insert(entity);
    }

    pub fn updateZmoduDeliveryTemplate(self: *DeliveryService, entity: model.ZmoduDeliveryTemplate) !void {
        var repo = self.persistence.zmoduDeliveryTemplateRepo();
        return try repo.update(entity);
    }

    pub fn deleteZmoduDeliveryTemplate(self: *DeliveryService, id: i64) !void {
        var repo = self.persistence.zmoduDeliveryTemplateRepo();
        return try repo.delete(id);
    }

};
