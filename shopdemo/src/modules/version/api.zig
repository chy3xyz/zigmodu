//! Auto-generated HTTP API for module: version
//! Generated by zmodu orm — uses `zigmodu.http_server` + `ctx.bindJson` / `ctx.jsonStruct`.

const std = @import("std");
const zigmodu = @import("zigmodu");
const service = @import("service.zig");
const model = @import("model.zig");

pub const VersionApi = struct {
    service: *service.VersionService,

    pub fn init(service_ptr: *service.VersionService) VersionApi {
        return .{ .service = service_ptr };
    }

    pub fn registerRoutes(self: *VersionApi, group: *zigmodu.http_server.RouteGroup) !void {
        try group.get("/zmodu_versions", listZmoduVersions, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_versions/:id", getZmoduVersion, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_versions", createZmoduVersion, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_versions/:id", updateZmoduVersion, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_versions/:id", deleteZmoduVersion, @ptrCast(@alignCast(self)));
    }

    fn listZmoduVersions(ctx: *zigmodu.http_server.Context) !void {
        const self: *VersionApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[version] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[version] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduVersions(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduVersion(ctx: *zigmodu.http_server.Context) !void {
        const self: *VersionApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[version] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[version] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduVersion(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduVersion(ctx: *zigmodu.http_server.Context) !void {
        const self: *VersionApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduVersion) catch {
            std.log.warn("[version] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduVersion(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduVersion(ctx: *zigmodu.http_server.Context) !void {
        const self: *VersionApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduVersion) catch {
            std.log.warn("[version] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduVersion(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduVersion(ctx: *zigmodu.http_server.Context) !void {
        const self: *VersionApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[version] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduVersion(id);
        try ctx.json(204, "");
    }

};
