//! Auto-generated service for module: image
//! Generated by zmodu orm — templates: tools/zmodu/src/templates/orm/sqlx/
//!
//! ╔═══════════════════════════════════════════════════════════╗
//! ║  AI Metadata: module=image | layer=service      ║
//! ║  role=CRUD delegation | extends=service_ext.zig           ║
//! ╚═══════════════════════════════════════════════════════════╝

const std = @import("std");
const zigmodu = @import("zigmodu");
const model = @import("model.zig");
const persistence = @import("persistence.zig");

pub const ImageService = struct {
    persistence: *persistence.ImagePersistence,
    // EventBus — uncomment to enable event-driven communication:
    // event_bus: zigmodu.TypedEventBus(ImageEvent),

    pub fn init(persistence_ptr: *persistence.ImagePersistence) ImageService {
        return .{ .persistence = persistence_ptr };
    }

    // Event hooks (uncomment and define event types to enable):
    // fn afterCreate(self: *ImageService, entity: anytype) !void {
    //     try self.event_bus.publish(.{ .id = entity.id });
    // }
    // fn afterUpdate(self: *ImageService, entity: anytype) !void {
    //     try self.event_bus.publish(.{ .id = entity.id });
    // }
    // fn afterDelete(self: *ImageService, id: i64) !void {
    //     try self.event_bus.publish(.{ .id = id });
    // }

    pub fn listZmoduImageBanks(self: *ImageService, page: usize, size: usize) !zigmodu.orm.PageResult(model.ZmoduImageBank) {
        var repo = self.persistence.zmoduImageBankRepo();
        return try repo.findPage(page, size);
    }

    pub fn getZmoduImageBank(self: *ImageService, id: i64) !?model.ZmoduImageBank {
        var repo = self.persistence.zmoduImageBankRepo();
        return try repo.findById(id);
    }

    pub fn createZmoduImageBank(self: *ImageService, entity: model.ZmoduImageBank) !model.ZmoduImageBank {
        var repo = self.persistence.zmoduImageBankRepo();
        return try repo.insert(entity);
    }

    pub fn updateZmoduImageBank(self: *ImageService, entity: model.ZmoduImageBank) !void {
        var repo = self.persistence.zmoduImageBankRepo();
        return try repo.update(entity);
    }

    pub fn deleteZmoduImageBank(self: *ImageService, id: i64) !void {
        var repo = self.persistence.zmoduImageBankRepo();
        return try repo.delete(id);
    }

};
