//! Auto-generated HTTP API for module: image
//! Generated by zmodu orm — uses `zigmodu.http_server` + `ctx.bindJson` / `ctx.jsonStruct`.

const std = @import("std");
const zigmodu = @import("zigmodu");
const service = @import("service.zig");
const model = @import("model.zig");

pub const ImageApi = struct {
    service: *service.ImageService,

    pub fn init(service_ptr: *service.ImageService) ImageApi {
        return .{ .service = service_ptr };
    }

    pub fn registerRoutes(self: *ImageApi, group: *zigmodu.http_server.RouteGroup) !void {
        try group.get("/zmodu_image_banks", listZmoduImageBanks, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_image_banks/:id", getZmoduImageBank, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_image_banks", createZmoduImageBank, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_image_banks/:id", updateZmoduImageBank, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_image_banks/:id", deleteZmoduImageBank, @ptrCast(@alignCast(self)));
    }

    fn listZmoduImageBanks(ctx: *zigmodu.http_server.Context) !void {
        const self: *ImageApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[image] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[image] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduImageBanks(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduImageBank(ctx: *zigmodu.http_server.Context) !void {
        const self: *ImageApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[image] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[image] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduImageBank(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduImageBank(ctx: *zigmodu.http_server.Context) !void {
        const self: *ImageApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduImageBank) catch {
            std.log.warn("[image] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduImageBank(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduImageBank(ctx: *zigmodu.http_server.Context) !void {
        const self: *ImageApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduImageBank) catch {
            std.log.warn("[image] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduImageBank(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduImageBank(ctx: *zigmodu.http_server.Context) !void {
        const self: *ImageApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[image] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduImageBank(id);
        try ctx.json(204, "");
    }

};
