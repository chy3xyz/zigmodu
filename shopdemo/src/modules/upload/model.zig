//! Auto-generated models for module: upload
//! Generated by zmodu orm — templates: tools/zmodu/src/templates/orm/sqlx/
//! Do not edit generated files by hand; change SQL and re-run `zmodu orm`.

const std = @import("std");

pub const ZmoduUploadFile = struct {
    pub const sql_table_name: []const u8 = "zmodu_upload_file";
    file_id: i64,
    storage: []const u8,
    group_id: i64,
    file_url: []const u8,
    save_name: ?[]const u8,
    file_name: []const u8,
    file_size: i64,
    file_type: []const u8,
    real_name: ?[]const u8,
    extension: []const u8,
    is_user: i64,
    is_recycle: i64,
    shop_supplier_id: ?i64,
    is_delete: i64,
    app_id: ?i64,
    create_time: i64,
    update_time: ?i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("file_id");
        try jws.write(self.file_id);
        try jws.objectField("storage");
        try jws.write(self.storage);
        try jws.objectField("group_id");
        try jws.write(self.group_id);
        try jws.objectField("file_url");
        try jws.write(self.file_url);
        try jws.objectField("save_name");
        if (self.save_name) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("file_name");
        try jws.write(self.file_name);
        try jws.objectField("file_size");
        try jws.write(self.file_size);
        try jws.objectField("file_type");
        try jws.write(self.file_type);
        try jws.objectField("real_name");
        if (self.real_name) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("extension");
        try jws.write(self.extension);
        try jws.objectField("is_user");
        try jws.write(self.is_user);
        try jws.objectField("is_recycle");
        try jws.write(self.is_recycle);
        try jws.objectField("shop_supplier_id");
        if (self.shop_supplier_id) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("is_delete");
        try jws.write(self.is_delete);
        try jws.objectField("app_id");
        if (self.app_id) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.objectField("update_time");
        if (self.update_time) |v| try jws.write(v) else try jws.write(null);
        try jws.endObject();
    }
};

pub const ZmoduUploadGroup = struct {
    pub const sql_table_name: []const u8 = "zmodu_upload_group";
    group_id: i64,
    group_type: []const u8,
    group_name: []const u8,
    sort: i64,
    shop_supplier_id: ?i64,
    is_delete: i64,
    app_id: i64,
    create_time: i64,
    update_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("group_id");
        try jws.write(self.group_id);
        try jws.objectField("group_type");
        try jws.write(self.group_type);
        try jws.objectField("group_name");
        try jws.write(self.group_name);
        try jws.objectField("sort");
        try jws.write(self.sort);
        try jws.objectField("shop_supplier_id");
        if (self.shop_supplier_id) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("is_delete");
        try jws.write(self.is_delete);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.objectField("update_time");
        try jws.write(self.update_time);
        try jws.endObject();
    }
};

