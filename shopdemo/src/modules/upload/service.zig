//! Auto-generated service for module: upload
//! Generated by zmodu orm — templates: tools/zmodu/src/templates/orm/sqlx/
//!
//! ╔═══════════════════════════════════════════════════════════╗
//! ║  AI Metadata: module=upload | layer=service      ║
//! ║  role=CRUD delegation | extends=service_ext.zig           ║
//! ╚═══════════════════════════════════════════════════════════╝

const std = @import("std");
const zigmodu = @import("zigmodu");
const model = @import("model.zig");
const persistence = @import("persistence.zig");

pub const UploadService = struct {
    persistence: *persistence.UploadPersistence,
    // EventBus — uncomment to enable event-driven communication:
    // event_bus: zigmodu.TypedEventBus(UploadEvent),

    pub fn init(persistence_ptr: *persistence.UploadPersistence) UploadService {
        return .{ .persistence = persistence_ptr };
    }

    // Event hooks (uncomment and define event types to enable):
    // fn afterCreate(self: *UploadService, entity: anytype) !void {
    //     try self.event_bus.publish(.{ .id = entity.id });
    // }
    // fn afterUpdate(self: *UploadService, entity: anytype) !void {
    //     try self.event_bus.publish(.{ .id = entity.id });
    // }
    // fn afterDelete(self: *UploadService, id: i64) !void {
    //     try self.event_bus.publish(.{ .id = id });
    // }

    pub fn listZmoduUploadFiles(self: *UploadService, page: usize, size: usize) !zigmodu.orm.PageResult(model.ZmoduUploadFile) {
        var repo = self.persistence.zmoduUploadFileRepo();
        return try repo.findPage(page, size);
    }

    pub fn getZmoduUploadFile(self: *UploadService, id: i64) !?model.ZmoduUploadFile {
        var repo = self.persistence.zmoduUploadFileRepo();
        return try repo.findById(id);
    }

    pub fn createZmoduUploadFile(self: *UploadService, entity: model.ZmoduUploadFile) !model.ZmoduUploadFile {
        var repo = self.persistence.zmoduUploadFileRepo();
        return try repo.insert(entity);
    }

    pub fn updateZmoduUploadFile(self: *UploadService, entity: model.ZmoduUploadFile) !void {
        var repo = self.persistence.zmoduUploadFileRepo();
        return try repo.update(entity);
    }

    pub fn deleteZmoduUploadFile(self: *UploadService, id: i64) !void {
        var repo = self.persistence.zmoduUploadFileRepo();
        return try repo.delete(id);
    }

    pub fn listZmoduUploadGroups(self: *UploadService, page: usize, size: usize) !zigmodu.orm.PageResult(model.ZmoduUploadGroup) {
        var repo = self.persistence.zmoduUploadGroupRepo();
        return try repo.findPage(page, size);
    }

    pub fn getZmoduUploadGroup(self: *UploadService, id: i64) !?model.ZmoduUploadGroup {
        var repo = self.persistence.zmoduUploadGroupRepo();
        return try repo.findById(id);
    }

    pub fn createZmoduUploadGroup(self: *UploadService, entity: model.ZmoduUploadGroup) !model.ZmoduUploadGroup {
        var repo = self.persistence.zmoduUploadGroupRepo();
        return try repo.insert(entity);
    }

    pub fn updateZmoduUploadGroup(self: *UploadService, entity: model.ZmoduUploadGroup) !void {
        var repo = self.persistence.zmoduUploadGroupRepo();
        return try repo.update(entity);
    }

    pub fn deleteZmoduUploadGroup(self: *UploadService, id: i64) !void {
        var repo = self.persistence.zmoduUploadGroupRepo();
        return try repo.delete(id);
    }

};
