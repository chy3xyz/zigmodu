//! Auto-generated HTTP API for module: message
//! Generated by zmodu orm — uses `zigmodu.http_server` + `ctx.bindJson` / `ctx.jsonStruct`.

const std = @import("std");
const zigmodu = @import("zigmodu");
const service = @import("service.zig");
const model = @import("model.zig");

pub const MessageApi = struct {
    service: *service.MessageService,

    pub fn init(service_ptr: *service.MessageService) MessageApi {
        return .{ .service = service_ptr };
    }

    pub fn registerRoutes(self: *MessageApi, group: *zigmodu.http_server.RouteGroup) !void {
        try group.get("/zmodu_messages", listZmoduMessages, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_messages/:id", getZmoduMessage, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_messages", createZmoduMessage, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_messages/:id", updateZmoduMessage, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_messages/:id", deleteZmoduMessage, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_message_fields", listZmoduMessageFields, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_message_fields/:id", getZmoduMessageField, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_message_fields", createZmoduMessageField, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_message_fields/:id", updateZmoduMessageField, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_message_fields/:id", deleteZmoduMessageField, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_message_settingss", listZmoduMessageSettingss, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_message_settingss/:id", getZmoduMessageSettings, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_message_settingss", createZmoduMessageSettings, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_message_settingss/:id", updateZmoduMessageSettings, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_message_settingss/:id", deleteZmoduMessageSettings, @ptrCast(@alignCast(self)));
    }

    fn listZmoduMessages(ctx: *zigmodu.http_server.Context) !void {
        const self: *MessageApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[message] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[message] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduMessages(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduMessage(ctx: *zigmodu.http_server.Context) !void {
        const self: *MessageApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[message] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[message] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduMessage(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduMessage(ctx: *zigmodu.http_server.Context) !void {
        const self: *MessageApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduMessage) catch {
            std.log.warn("[message] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduMessage(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduMessage(ctx: *zigmodu.http_server.Context) !void {
        const self: *MessageApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduMessage) catch {
            std.log.warn("[message] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduMessage(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduMessage(ctx: *zigmodu.http_server.Context) !void {
        const self: *MessageApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[message] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduMessage(id);
        try ctx.json(204, "");
    }

    fn listZmoduMessageFields(ctx: *zigmodu.http_server.Context) !void {
        const self: *MessageApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[message] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[message] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduMessageFields(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduMessageField(ctx: *zigmodu.http_server.Context) !void {
        const self: *MessageApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[message] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[message] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduMessageField(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduMessageField(ctx: *zigmodu.http_server.Context) !void {
        const self: *MessageApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduMessageField) catch {
            std.log.warn("[message] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduMessageField(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduMessageField(ctx: *zigmodu.http_server.Context) !void {
        const self: *MessageApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduMessageField) catch {
            std.log.warn("[message] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduMessageField(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduMessageField(ctx: *zigmodu.http_server.Context) !void {
        const self: *MessageApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[message] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduMessageField(id);
        try ctx.json(204, "");
    }

    fn listZmoduMessageSettingss(ctx: *zigmodu.http_server.Context) !void {
        const self: *MessageApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[message] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[message] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduMessageSettingss(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduMessageSettings(ctx: *zigmodu.http_server.Context) !void {
        const self: *MessageApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[message] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[message] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduMessageSettings(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduMessageSettings(ctx: *zigmodu.http_server.Context) !void {
        const self: *MessageApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduMessageSettings) catch {
            std.log.warn("[message] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduMessageSettings(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduMessageSettings(ctx: *zigmodu.http_server.Context) !void {
        const self: *MessageApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduMessageSettings) catch {
            std.log.warn("[message] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduMessageSettings(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduMessageSettings(ctx: *zigmodu.http_server.Context) !void {
        const self: *MessageApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[message] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduMessageSettings(id);
        try ctx.json(204, "");
    }

};
