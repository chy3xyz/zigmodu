//! Auto-generated models for module: coupon
//! Generated by zmodu orm — templates: tools/zmodu/src/templates/orm/sqlx/
//! Do not edit generated files by hand; change SQL and re-run `zmodu orm`.

const std = @import("std");

pub const ZmoduCoupon = struct {
    pub const sql_table_name: []const u8 = "zmodu_coupon";
    coupon_id: i64,
    name: []const u8,
    color: i64,
    coupon_type: i64,
    reduce_price: f64,
    discount: i64,
    min_price: f64,
    expire_type: i64,
    expire_day: i64,
    start_time: i64,
    end_time: i64,
    apply_range: i64,
    product_ids: ?[]const u8,
    category_ids: ?[]const u8,
    total_num: i64,
    receive_num: i64,
    shop_supplier_id: i64,
    sort: i64,
    show_center: ?i64,
    free_limit: ?i64,
    max_price: f64,
    content: []const u8,
    is_delete: i64,
    app_id: ?i64,
    create_time: i64,
    update_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("coupon_id");
        try jws.write(self.coupon_id);
        try jws.objectField("name");
        try jws.write(self.name);
        try jws.objectField("color");
        try jws.write(self.color);
        try jws.objectField("coupon_type");
        try jws.write(self.coupon_type);
        try jws.objectField("reduce_price");
        try jws.write(self.reduce_price);
        try jws.objectField("discount");
        try jws.write(self.discount);
        try jws.objectField("min_price");
        try jws.write(self.min_price);
        try jws.objectField("expire_type");
        try jws.write(self.expire_type);
        try jws.objectField("expire_day");
        try jws.write(self.expire_day);
        try jws.objectField("start_time");
        try jws.write(self.start_time);
        try jws.objectField("end_time");
        try jws.write(self.end_time);
        try jws.objectField("apply_range");
        try jws.write(self.apply_range);
        try jws.objectField("product_ids");
        if (self.product_ids) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("category_ids");
        if (self.category_ids) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("total_num");
        try jws.write(self.total_num);
        try jws.objectField("receive_num");
        try jws.write(self.receive_num);
        try jws.objectField("shop_supplier_id");
        try jws.write(self.shop_supplier_id);
        try jws.objectField("sort");
        try jws.write(self.sort);
        try jws.objectField("show_center");
        if (self.show_center) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("free_limit");
        if (self.free_limit) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("max_price");
        try jws.write(self.max_price);
        try jws.objectField("content");
        try jws.write(self.content);
        try jws.objectField("is_delete");
        try jws.write(self.is_delete);
        try jws.objectField("app_id");
        if (self.app_id) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.objectField("update_time");
        try jws.write(self.update_time);
        try jws.endObject();
    }
};

