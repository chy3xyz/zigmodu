//! Auto-generated service for module: bargain
//! Generated by zmodu orm — templates: tools/zmodu/src/templates/orm/sqlx/
//!
//! ╔═══════════════════════════════════════════════════════════╗
//! ║  AI Metadata: module=bargain | layer=service      ║
//! ║  role=CRUD delegation | extends=service_ext.zig           ║
//! ╚═══════════════════════════════════════════════════════════╝

const std = @import("std");
const zigmodu = @import("zigmodu");
const model = @import("model.zig");
const persistence = @import("persistence.zig");

pub const BargainService = struct {
    persistence: *persistence.BargainPersistence,
    // EventBus — uncomment to enable event-driven communication:
    // event_bus: zigmodu.TypedEventBus(BargainEvent),

    pub fn init(persistence_ptr: *persistence.BargainPersistence) BargainService {
        return .{ .persistence = persistence_ptr };
    }

    // Event hooks (uncomment and define event types to enable):
    // fn afterCreate(self: *BargainService, entity: anytype) !void {
    //     try self.event_bus.publish(.{ .id = entity.id });
    // }
    // fn afterUpdate(self: *BargainService, entity: anytype) !void {
    //     try self.event_bus.publish(.{ .id = entity.id });
    // }
    // fn afterDelete(self: *BargainService, id: i64) !void {
    //     try self.event_bus.publish(.{ .id = id });
    // }

    pub fn listZmoduBargainProducts(self: *BargainService, page: usize, size: usize) !zigmodu.orm.PageResult(model.ZmoduBargainProduct) {
        var repo = self.persistence.zmoduBargainProductRepo();
        return try repo.findPage(page, size);
    }

    pub fn getZmoduBargainProduct(self: *BargainService, id: i64) !?model.ZmoduBargainProduct {
        var repo = self.persistence.zmoduBargainProductRepo();
        return try repo.findById(id);
    }

    pub fn createZmoduBargainProduct(self: *BargainService, entity: model.ZmoduBargainProduct) !model.ZmoduBargainProduct {
        var repo = self.persistence.zmoduBargainProductRepo();
        return try repo.insert(entity);
    }

    pub fn updateZmoduBargainProduct(self: *BargainService, entity: model.ZmoduBargainProduct) !void {
        var repo = self.persistence.zmoduBargainProductRepo();
        return try repo.update(entity);
    }

    pub fn deleteZmoduBargainProduct(self: *BargainService, id: i64) !void {
        var repo = self.persistence.zmoduBargainProductRepo();
        return try repo.delete(id);
    }

    pub fn listZmoduBargainProductSkus(self: *BargainService, page: usize, size: usize) !zigmodu.orm.PageResult(model.ZmoduBargainProductSku) {
        var repo = self.persistence.zmoduBargainProductSkuRepo();
        return try repo.findPage(page, size);
    }

    pub fn getZmoduBargainProductSku(self: *BargainService, id: i64) !?model.ZmoduBargainProductSku {
        var repo = self.persistence.zmoduBargainProductSkuRepo();
        return try repo.findById(id);
    }

    pub fn createZmoduBargainProductSku(self: *BargainService, entity: model.ZmoduBargainProductSku) !model.ZmoduBargainProductSku {
        var repo = self.persistence.zmoduBargainProductSkuRepo();
        return try repo.insert(entity);
    }

    pub fn updateZmoduBargainProductSku(self: *BargainService, entity: model.ZmoduBargainProductSku) !void {
        var repo = self.persistence.zmoduBargainProductSkuRepo();
        return try repo.update(entity);
    }

    pub fn deleteZmoduBargainProductSku(self: *BargainService, id: i64) !void {
        var repo = self.persistence.zmoduBargainProductSkuRepo();
        return try repo.delete(id);
    }

    pub fn listZmoduBargainSettings(self: *BargainService, page: usize, size: usize) !zigmodu.orm.PageResult(model.ZmoduBargainSetting) {
        var repo = self.persistence.zmoduBargainSettingRepo();
        return try repo.findPage(page, size);
    }

    pub fn getZmoduBargainSetting(self: *BargainService, id: i64) !?model.ZmoduBargainSetting {
        var repo = self.persistence.zmoduBargainSettingRepo();
        return try repo.findById(id);
    }

    pub fn createZmoduBargainSetting(self: *BargainService, entity: model.ZmoduBargainSetting) !model.ZmoduBargainSetting {
        var repo = self.persistence.zmoduBargainSettingRepo();
        return try repo.insert(entity);
    }

    pub fn updateZmoduBargainSetting(self: *BargainService, entity: model.ZmoduBargainSetting) !void {
        var repo = self.persistence.zmoduBargainSettingRepo();
        return try repo.update(entity);
    }

    pub fn deleteZmoduBargainSetting(self: *BargainService, id: i64) !void {
        var repo = self.persistence.zmoduBargainSettingRepo();
        return try repo.delete(id);
    }

    pub fn listZmoduBargainTasks(self: *BargainService, page: usize, size: usize) !zigmodu.orm.PageResult(model.ZmoduBargainTask) {
        var repo = self.persistence.zmoduBargainTaskRepo();
        return try repo.findPage(page, size);
    }

    pub fn getZmoduBargainTask(self: *BargainService, id: i64) !?model.ZmoduBargainTask {
        var repo = self.persistence.zmoduBargainTaskRepo();
        return try repo.findById(id);
    }

    pub fn createZmoduBargainTask(self: *BargainService, entity: model.ZmoduBargainTask) !model.ZmoduBargainTask {
        var repo = self.persistence.zmoduBargainTaskRepo();
        return try repo.insert(entity);
    }

    pub fn updateZmoduBargainTask(self: *BargainService, entity: model.ZmoduBargainTask) !void {
        var repo = self.persistence.zmoduBargainTaskRepo();
        return try repo.update(entity);
    }

    pub fn deleteZmoduBargainTask(self: *BargainService, id: i64) !void {
        var repo = self.persistence.zmoduBargainTaskRepo();
        return try repo.delete(id);
    }

    pub fn listZmoduBargainTaskHelps(self: *BargainService, page: usize, size: usize) !zigmodu.orm.PageResult(model.ZmoduBargainTaskHelp) {
        var repo = self.persistence.zmoduBargainTaskHelpRepo();
        return try repo.findPage(page, size);
    }

    pub fn getZmoduBargainTaskHelp(self: *BargainService, id: i64) !?model.ZmoduBargainTaskHelp {
        var repo = self.persistence.zmoduBargainTaskHelpRepo();
        return try repo.findById(id);
    }

    pub fn createZmoduBargainTaskHelp(self: *BargainService, entity: model.ZmoduBargainTaskHelp) !model.ZmoduBargainTaskHelp {
        var repo = self.persistence.zmoduBargainTaskHelpRepo();
        return try repo.insert(entity);
    }

    pub fn updateZmoduBargainTaskHelp(self: *BargainService, entity: model.ZmoduBargainTaskHelp) !void {
        var repo = self.persistence.zmoduBargainTaskHelpRepo();
        return try repo.update(entity);
    }

    pub fn deleteZmoduBargainTaskHelp(self: *BargainService, id: i64) !void {
        var repo = self.persistence.zmoduBargainTaskHelpRepo();
        return try repo.delete(id);
    }

};
