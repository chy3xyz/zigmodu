//! Auto-generated models for module: bargain
//! Generated by zmodu orm — templates: tools/zmodu/src/templates/orm/sqlx/
//! Do not edit generated files by hand; change SQL and re-run `zmodu orm`.

const std = @import("std");

pub const ZmoduBargainProduct = struct {
    pub const sql_table_name: []const u8 = "zmodu_bargain_product";
    bargain_product_id: i64,
    product_id: i64,
    limit_num: i64,
    total_sales: i64,
    sales_initial: ?i64,
    stock: i64,
    join_num: ?i64,
    sort: i64,
    status: ?i64,
    remark: ?[]const u8,
    is_delete: ?i64,
    app_id: i64,
    create_time: i64,
    update_time: i64,
    shop_supplier_id: ?i64,
    state: i64,
    start_time: i64,
    end_time: i64,
    conditions: i64,
    together_time: ?i64,
    image_id: i64,
    banner: ?[]const u8,
    product_name: []const u8,
    logistics: []const u8,
    delivery_id: i64,
    bargain_num: i64,
    help_num: i64,
    content: ?[]const u8,
    bargain_price: f64,
    product_price: f64,
    selling_point: []const u8,
    rule: ?[]const u8,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("bargain_product_id");
        try jws.write(self.bargain_product_id);
        try jws.objectField("product_id");
        try jws.write(self.product_id);
        try jws.objectField("limit_num");
        try jws.write(self.limit_num);
        try jws.objectField("total_sales");
        try jws.write(self.total_sales);
        try jws.objectField("sales_initial");
        if (self.sales_initial) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("stock");
        try jws.write(self.stock);
        try jws.objectField("join_num");
        if (self.join_num) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("sort");
        try jws.write(self.sort);
        try jws.objectField("status");
        if (self.status) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("remark");
        if (self.remark) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("is_delete");
        if (self.is_delete) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.objectField("update_time");
        try jws.write(self.update_time);
        try jws.objectField("shop_supplier_id");
        if (self.shop_supplier_id) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("state");
        try jws.write(self.state);
        try jws.objectField("start_time");
        try jws.write(self.start_time);
        try jws.objectField("end_time");
        try jws.write(self.end_time);
        try jws.objectField("conditions");
        try jws.write(self.conditions);
        try jws.objectField("together_time");
        if (self.together_time) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("image_id");
        try jws.write(self.image_id);
        try jws.objectField("banner");
        if (self.banner) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("product_name");
        try jws.write(self.product_name);
        try jws.objectField("logistics");
        try jws.write(self.logistics);
        try jws.objectField("delivery_id");
        try jws.write(self.delivery_id);
        try jws.objectField("bargain_num");
        try jws.write(self.bargain_num);
        try jws.objectField("help_num");
        try jws.write(self.help_num);
        try jws.objectField("content");
        if (self.content) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("bargain_price");
        try jws.write(self.bargain_price);
        try jws.objectField("product_price");
        try jws.write(self.product_price);
        try jws.objectField("selling_point");
        try jws.write(self.selling_point);
        try jws.objectField("rule");
        if (self.rule) |v| try jws.write(v) else try jws.write(null);
        try jws.endObject();
    }
};

pub const ZmoduBargainProductSku = struct {
    pub const sql_table_name: []const u8 = "zmodu_bargain_product_sku";
    bargain_product_sku_id: i64,
    bargain_product_id: i64,
    product_id: i64,
    product_sku_id: i64,
    bargain_stock: i64,
    bargain_price: f64,
    total_sales: i64,
    product_attr: ?[]const u8,
    product_price: ?f64,
    status: i64,
    app_id: i64,
    create_time: i64,
    update_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("bargain_product_sku_id");
        try jws.write(self.bargain_product_sku_id);
        try jws.objectField("bargain_product_id");
        try jws.write(self.bargain_product_id);
        try jws.objectField("product_id");
        try jws.write(self.product_id);
        try jws.objectField("product_sku_id");
        try jws.write(self.product_sku_id);
        try jws.objectField("bargain_stock");
        try jws.write(self.bargain_stock);
        try jws.objectField("bargain_price");
        try jws.write(self.bargain_price);
        try jws.objectField("total_sales");
        try jws.write(self.total_sales);
        try jws.objectField("product_attr");
        if (self.product_attr) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("product_price");
        if (self.product_price) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("status");
        try jws.write(self.status);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.objectField("update_time");
        try jws.write(self.update_time);
        try jws.endObject();
    }
};

pub const ZmoduBargainSetting = struct {
    pub const sql_table_name: []const u8 = "zmodu_bargain_setting";
    key: []const u8,
    describe: []const u8,
    values: []const u8,
    app_id: i64,
    update_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("key");
        try jws.write(self.key);
        try jws.objectField("describe");
        try jws.write(self.describe);
        try jws.objectField("values");
        try jws.write(self.values);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("update_time");
        try jws.write(self.update_time);
        try jws.endObject();
    }
};

pub const ZmoduBargainTask = struct {
    pub const sql_table_name: []const u8 = "zmodu_bargain_task";
    bargain_task_id: i64,
    user_id: i64,
    bargain_product_id: i64,
    bargain_product_sku_id: i64,
    product_sku_id: ?i64,
    product_price: f64,
    bargain_price: f64,
    product_name: ?[]const u8,
    image_id: ?i64,
    product_attr: ?[]const u8,
    peoples: i64,
    cut_people: i64,
    section: []const u8,
    cut_money: f64,
    actual_price: f64,
    is_floor: i64,
    end_time: i64,
    is_buy: i64,
    status: i64,
    shop_supplier_id: ?i64,
    is_delete: i64,
    app_id: i64,
    create_time: i64,
    update_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("bargain_task_id");
        try jws.write(self.bargain_task_id);
        try jws.objectField("user_id");
        try jws.write(self.user_id);
        try jws.objectField("bargain_product_id");
        try jws.write(self.bargain_product_id);
        try jws.objectField("bargain_product_sku_id");
        try jws.write(self.bargain_product_sku_id);
        try jws.objectField("product_sku_id");
        if (self.product_sku_id) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("product_price");
        try jws.write(self.product_price);
        try jws.objectField("bargain_price");
        try jws.write(self.bargain_price);
        try jws.objectField("product_name");
        if (self.product_name) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("image_id");
        if (self.image_id) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("product_attr");
        if (self.product_attr) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("peoples");
        try jws.write(self.peoples);
        try jws.objectField("cut_people");
        try jws.write(self.cut_people);
        try jws.objectField("section");
        try jws.write(self.section);
        try jws.objectField("cut_money");
        try jws.write(self.cut_money);
        try jws.objectField("actual_price");
        try jws.write(self.actual_price);
        try jws.objectField("is_floor");
        try jws.write(self.is_floor);
        try jws.objectField("end_time");
        try jws.write(self.end_time);
        try jws.objectField("is_buy");
        try jws.write(self.is_buy);
        try jws.objectField("status");
        try jws.write(self.status);
        try jws.objectField("shop_supplier_id");
        if (self.shop_supplier_id) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("is_delete");
        try jws.write(self.is_delete);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.objectField("update_time");
        try jws.write(self.update_time);
        try jws.endObject();
    }
};

pub const ZmoduBargainTaskHelp = struct {
    pub const sql_table_name: []const u8 = "zmodu_bargain_task_help";
    bargain_task_help_id: i64,
    bargain_product_id: i64,
    bargain_task_id: i64,
    user_id: i64,
    is_creater: i64,
    cut_money: f64,
    shop_supplier_id: i64,
    app_id: i64,
    create_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("bargain_task_help_id");
        try jws.write(self.bargain_task_help_id);
        try jws.objectField("bargain_product_id");
        try jws.write(self.bargain_product_id);
        try jws.objectField("bargain_task_id");
        try jws.write(self.bargain_task_id);
        try jws.objectField("user_id");
        try jws.write(self.user_id);
        try jws.objectField("is_creater");
        try jws.write(self.is_creater);
        try jws.objectField("cut_money");
        try jws.write(self.cut_money);
        try jws.objectField("shop_supplier_id");
        try jws.write(self.shop_supplier_id);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.endObject();
    }
};

