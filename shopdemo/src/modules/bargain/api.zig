//! Auto-generated HTTP API for module: bargain
//! Generated by zmodu orm — uses `zigmodu.http_server` + `ctx.bindJson` / `ctx.jsonStruct`.

const std = @import("std");
const zigmodu = @import("zigmodu");
const service = @import("service.zig");
const model = @import("model.zig");

pub const BargainApi = struct {
    service: *service.BargainService,

    pub fn init(service_ptr: *service.BargainService) BargainApi {
        return .{ .service = service_ptr };
    }

    pub fn registerRoutes(self: *BargainApi, group: *zigmodu.http_server.RouteGroup) !void {
        try group.get("/zmodu_bargain_products", listZmoduBargainProducts, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_bargain_products/:id", getZmoduBargainProduct, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_bargain_products", createZmoduBargainProduct, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_bargain_products/:id", updateZmoduBargainProduct, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_bargain_products/:id", deleteZmoduBargainProduct, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_bargain_product_skus", listZmoduBargainProductSkus, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_bargain_product_skus/:id", getZmoduBargainProductSku, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_bargain_product_skus", createZmoduBargainProductSku, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_bargain_product_skus/:id", updateZmoduBargainProductSku, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_bargain_product_skus/:id", deleteZmoduBargainProductSku, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_bargain_settings", listZmoduBargainSettings, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_bargain_settings/:id", getZmoduBargainSetting, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_bargain_settings", createZmoduBargainSetting, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_bargain_settings/:id", updateZmoduBargainSetting, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_bargain_settings/:id", deleteZmoduBargainSetting, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_bargain_tasks", listZmoduBargainTasks, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_bargain_tasks/:id", getZmoduBargainTask, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_bargain_tasks", createZmoduBargainTask, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_bargain_tasks/:id", updateZmoduBargainTask, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_bargain_tasks/:id", deleteZmoduBargainTask, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_bargain_task_helps", listZmoduBargainTaskHelps, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_bargain_task_helps/:id", getZmoduBargainTaskHelp, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_bargain_task_helps", createZmoduBargainTaskHelp, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_bargain_task_helps/:id", updateZmoduBargainTaskHelp, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_bargain_task_helps/:id", deleteZmoduBargainTaskHelp, @ptrCast(@alignCast(self)));
    }

    fn listZmoduBargainProducts(ctx: *zigmodu.http_server.Context) !void {
        const self: *BargainApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[bargain] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[bargain] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduBargainProducts(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduBargainProduct(ctx: *zigmodu.http_server.Context) !void {
        const self: *BargainApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[bargain] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[bargain] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduBargainProduct(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduBargainProduct(ctx: *zigmodu.http_server.Context) !void {
        const self: *BargainApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduBargainProduct) catch {
            std.log.warn("[bargain] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduBargainProduct(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduBargainProduct(ctx: *zigmodu.http_server.Context) !void {
        const self: *BargainApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduBargainProduct) catch {
            std.log.warn("[bargain] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduBargainProduct(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduBargainProduct(ctx: *zigmodu.http_server.Context) !void {
        const self: *BargainApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[bargain] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduBargainProduct(id);
        try ctx.json(204, "");
    }

    fn listZmoduBargainProductSkus(ctx: *zigmodu.http_server.Context) !void {
        const self: *BargainApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[bargain] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[bargain] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduBargainProductSkus(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduBargainProductSku(ctx: *zigmodu.http_server.Context) !void {
        const self: *BargainApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[bargain] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[bargain] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduBargainProductSku(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduBargainProductSku(ctx: *zigmodu.http_server.Context) !void {
        const self: *BargainApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduBargainProductSku) catch {
            std.log.warn("[bargain] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduBargainProductSku(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduBargainProductSku(ctx: *zigmodu.http_server.Context) !void {
        const self: *BargainApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduBargainProductSku) catch {
            std.log.warn("[bargain] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduBargainProductSku(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduBargainProductSku(ctx: *zigmodu.http_server.Context) !void {
        const self: *BargainApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[bargain] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduBargainProductSku(id);
        try ctx.json(204, "");
    }

    fn listZmoduBargainSettings(ctx: *zigmodu.http_server.Context) !void {
        const self: *BargainApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[bargain] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[bargain] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduBargainSettings(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduBargainSetting(ctx: *zigmodu.http_server.Context) !void {
        const self: *BargainApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[bargain] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[bargain] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduBargainSetting(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduBargainSetting(ctx: *zigmodu.http_server.Context) !void {
        const self: *BargainApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduBargainSetting) catch {
            std.log.warn("[bargain] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduBargainSetting(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduBargainSetting(ctx: *zigmodu.http_server.Context) !void {
        const self: *BargainApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduBargainSetting) catch {
            std.log.warn("[bargain] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduBargainSetting(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduBargainSetting(ctx: *zigmodu.http_server.Context) !void {
        const self: *BargainApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[bargain] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduBargainSetting(id);
        try ctx.json(204, "");
    }

    fn listZmoduBargainTasks(ctx: *zigmodu.http_server.Context) !void {
        const self: *BargainApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[bargain] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[bargain] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduBargainTasks(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduBargainTask(ctx: *zigmodu.http_server.Context) !void {
        const self: *BargainApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[bargain] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[bargain] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduBargainTask(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduBargainTask(ctx: *zigmodu.http_server.Context) !void {
        const self: *BargainApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduBargainTask) catch {
            std.log.warn("[bargain] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduBargainTask(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduBargainTask(ctx: *zigmodu.http_server.Context) !void {
        const self: *BargainApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduBargainTask) catch {
            std.log.warn("[bargain] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduBargainTask(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduBargainTask(ctx: *zigmodu.http_server.Context) !void {
        const self: *BargainApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[bargain] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduBargainTask(id);
        try ctx.json(204, "");
    }

    fn listZmoduBargainTaskHelps(ctx: *zigmodu.http_server.Context) !void {
        const self: *BargainApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[bargain] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[bargain] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduBargainTaskHelps(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduBargainTaskHelp(ctx: *zigmodu.http_server.Context) !void {
        const self: *BargainApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[bargain] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[bargain] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduBargainTaskHelp(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduBargainTaskHelp(ctx: *zigmodu.http_server.Context) !void {
        const self: *BargainApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduBargainTaskHelp) catch {
            std.log.warn("[bargain] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduBargainTaskHelp(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduBargainTaskHelp(ctx: *zigmodu.http_server.Context) !void {
        const self: *BargainApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduBargainTaskHelp) catch {
            std.log.warn("[bargain] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduBargainTaskHelp(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduBargainTaskHelp(ctx: *zigmodu.http_server.Context) !void {
        const self: *BargainApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[bargain] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduBargainTaskHelp(id);
        try ctx.json(204, "");
    }

};
