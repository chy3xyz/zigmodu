//! Auto-generated models for module: live
//! Generated by zmodu orm — templates: tools/zmodu/src/templates/orm/sqlx/
//! Do not edit generated files by hand; change SQL and re-run `zmodu orm`.

const std = @import("std");

pub const ZmoduLiveGift = struct {
    pub const sql_table_name: []const u8 = "zmodu_live_gift";
    gift_id: i64,
    gift_name: []const u8,
    price: i64,
    is_hot: ?i64,
    is_active: ?i64,
    image_id: i64,
    sort: ?i64,
    app_id: ?i64,
    is_delete: ?i64,
    create_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("gift_id");
        try jws.write(self.gift_id);
        try jws.objectField("gift_name");
        try jws.write(self.gift_name);
        try jws.objectField("price");
        try jws.write(self.price);
        try jws.objectField("is_hot");
        if (self.is_hot) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("is_active");
        if (self.is_active) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("image_id");
        try jws.write(self.image_id);
        try jws.objectField("sort");
        if (self.sort) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("app_id");
        if (self.app_id) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("is_delete");
        if (self.is_delete) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.endObject();
    }
};

pub const ZmoduLivePlan = struct {
    pub const sql_table_name: []const u8 = "zmodu_live_plan";
    plan_id: i64,
    plan_name: []const u8,
    money: f64,
    gift_money: f64,
    give_money: f64,
    sort: i64,
    is_delete: i64,
    app_id: i64,
    create_time: i64,
    update_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("plan_id");
        try jws.write(self.plan_id);
        try jws.objectField("plan_name");
        try jws.write(self.plan_name);
        try jws.objectField("money");
        try jws.write(self.money);
        try jws.objectField("gift_money");
        try jws.write(self.gift_money);
        try jws.objectField("give_money");
        try jws.write(self.give_money);
        try jws.objectField("sort");
        try jws.write(self.sort);
        try jws.objectField("is_delete");
        try jws.write(self.is_delete);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.objectField("update_time");
        try jws.write(self.update_time);
        try jws.endObject();
    }
};

pub const ZmoduLivePlanOrder = struct {
    pub const sql_table_name: []const u8 = "zmodu_live_plan_order";
    order_id: i64,
    order_no: []const u8,
    user_id: i64,
    plan_id: i64,
    plan_name: ?[]const u8,
    pay_price: f64,
    gift_money: f64,
    give_money: f64,
    total_money: f64,
    pay_status: i64,
    pay_time: i64,
    transaction_id: []const u8,
    pay_source: ?[]const u8,
    pay_type: ?i64,
    balance: f64,
    online_money: f64,
    app_id: i64,
    create_time: i64,
    update_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("order_id");
        try jws.write(self.order_id);
        try jws.objectField("order_no");
        try jws.write(self.order_no);
        try jws.objectField("user_id");
        try jws.write(self.user_id);
        try jws.objectField("plan_id");
        try jws.write(self.plan_id);
        try jws.objectField("plan_name");
        if (self.plan_name) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("pay_price");
        try jws.write(self.pay_price);
        try jws.objectField("gift_money");
        try jws.write(self.gift_money);
        try jws.objectField("give_money");
        try jws.write(self.give_money);
        try jws.objectField("total_money");
        try jws.write(self.total_money);
        try jws.objectField("pay_status");
        try jws.write(self.pay_status);
        try jws.objectField("pay_time");
        try jws.write(self.pay_time);
        try jws.objectField("transaction_id");
        try jws.write(self.transaction_id);
        try jws.objectField("pay_source");
        if (self.pay_source) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("pay_type");
        if (self.pay_type) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("balance");
        try jws.write(self.balance);
        try jws.objectField("online_money");
        try jws.write(self.online_money);
        try jws.objectField("app_id");
        try jws.write(self.app_id);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.objectField("update_time");
        try jws.write(self.update_time);
        try jws.endObject();
    }
};

pub const ZmoduLiveProduct = struct {
    pub const sql_table_name: []const u8 = "zmodu_live_product";
    live_product_id: i64,
    user_id: i64,
    product_id: i64,
    shop_supplier_id: i64,
    app_id: ?i64,
    create_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("live_product_id");
        try jws.write(self.live_product_id);
        try jws.objectField("user_id");
        try jws.write(self.user_id);
        try jws.objectField("product_id");
        try jws.write(self.product_id);
        try jws.objectField("shop_supplier_id");
        try jws.write(self.shop_supplier_id);
        try jws.objectField("app_id");
        if (self.app_id) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.endObject();
    }
};

pub const ZmoduLiveRoom = struct {
    pub const sql_table_name: []const u8 = "zmodu_live_room";
    room_id: i64,
    user_id: ?i64,
    name: []const u8,
    room_name: ?[]const u8,
    cover_img_id: ?i64,
    share_img_id: ?i64,
    live_status: i64,
    anchor_name: []const u8,
    start_time: i64,
    end_time: i64,
    real_start_time: ?i64,
    real_end_time: ?i64,
    sort: ?i64,
    is_top: i64,
    view_num: ?i64,
    gift_num: ?i64,
    sales_num: ?i64,
    digg_num: ?i64,
    product_id: ?i64,
    virtual_num: ?i64,
    is_notice: ?i64,
    shop_supplier_id: i64,
    audit_remark: ?[]const u8,
    category_id: i64,
    record_uid: ?i64,
    record_resource_id: ?[]const u8,
    record_sid: ?[]const u8,
    record_url: ?[]const u8,
    app_id: ?i64,
    is_delete: i64,
    create_time: i64,
    update_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("room_id");
        try jws.write(self.room_id);
        try jws.objectField("user_id");
        if (self.user_id) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("name");
        try jws.write(self.name);
        try jws.objectField("room_name");
        if (self.room_name) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("cover_img_id");
        if (self.cover_img_id) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("share_img_id");
        if (self.share_img_id) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("live_status");
        try jws.write(self.live_status);
        try jws.objectField("anchor_name");
        try jws.write(self.anchor_name);
        try jws.objectField("start_time");
        try jws.write(self.start_time);
        try jws.objectField("end_time");
        try jws.write(self.end_time);
        try jws.objectField("real_start_time");
        if (self.real_start_time) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("real_end_time");
        if (self.real_end_time) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("sort");
        if (self.sort) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("is_top");
        try jws.write(self.is_top);
        try jws.objectField("view_num");
        if (self.view_num) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("gift_num");
        if (self.gift_num) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("sales_num");
        if (self.sales_num) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("digg_num");
        if (self.digg_num) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("product_id");
        if (self.product_id) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("virtual_num");
        if (self.virtual_num) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("is_notice");
        if (self.is_notice) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("shop_supplier_id");
        try jws.write(self.shop_supplier_id);
        try jws.objectField("audit_remark");
        if (self.audit_remark) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("category_id");
        try jws.write(self.category_id);
        try jws.objectField("record_uid");
        if (self.record_uid) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("record_resource_id");
        if (self.record_resource_id) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("record_sid");
        if (self.record_sid) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("record_url");
        if (self.record_url) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("app_id");
        if (self.app_id) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("is_delete");
        try jws.write(self.is_delete);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.objectField("update_time");
        try jws.write(self.update_time);
        try jws.endObject();
    }
};

pub const ZmoduLiveRoomGift = struct {
    pub const sql_table_name: []const u8 = "zmodu_live_room_gift";
    room_gift_id: i64,
    room_id: i64,
    user_id: ?i64,
    gift_name: []const u8,
    price: ?i64,
    shop_supplier_id: i64,
    app_id: ?i64,
    create_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("room_gift_id");
        try jws.write(self.room_gift_id);
        try jws.objectField("room_id");
        try jws.write(self.room_id);
        try jws.objectField("user_id");
        if (self.user_id) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("gift_name");
        try jws.write(self.gift_name);
        try jws.objectField("price");
        if (self.price) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("shop_supplier_id");
        try jws.write(self.shop_supplier_id);
        try jws.objectField("app_id");
        if (self.app_id) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.endObject();
    }
};

pub const ZmoduLiveUserGift = struct {
    pub const sql_table_name: []const u8 = "zmodu_live_user_gift";
    user_gift_id: i64,
    room_id: i64,
    user_id: ?i64,
    gift_num: ?i64,
    shop_supplier_id: i64,
    app_id: ?i64,
    create_time: i64,

    pub fn jsonStringify(self: @This(), jws: anytype) !void {
        try jws.beginObject();
        try jws.objectField("user_gift_id");
        try jws.write(self.user_gift_id);
        try jws.objectField("room_id");
        try jws.write(self.room_id);
        try jws.objectField("user_id");
        if (self.user_id) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("gift_num");
        if (self.gift_num) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("shop_supplier_id");
        try jws.write(self.shop_supplier_id);
        try jws.objectField("app_id");
        if (self.app_id) |v| try jws.write(v) else try jws.write(null);
        try jws.objectField("create_time");
        try jws.write(self.create_time);
        try jws.endObject();
    }
};

