//! Auto-generated HTTP API for module: ad
//! Generated by zmodu orm — uses `zigmodu.http_server` + `ctx.bindJson` / `ctx.jsonStruct`.

const std = @import("std");
const zigmodu = @import("zigmodu");
const service = @import("service.zig");
const model = @import("model.zig");

pub const AdApi = struct {
    service: *service.AdService,

    pub fn init(service_ptr: *service.AdService) AdApi {
        return .{ .service = service_ptr };
    }

    pub fn registerRoutes(self: *AdApi, group: *zigmodu.http_server.RouteGroup) !void {
        try group.get("/zmodu_ads", listZmoduAds, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_ads/:id", getZmoduAd, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_ads", createZmoduAd, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_ads/:id", updateZmoduAd, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_ads/:id", deleteZmoduAd, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_ad_categorys", listZmoduAdCategorys, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_ad_categorys/:id", getZmoduAdCategory, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_ad_categorys", createZmoduAdCategory, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_ad_categorys/:id", updateZmoduAdCategory, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_ad_categorys/:id", deleteZmoduAdCategory, @ptrCast(@alignCast(self)));
    }

    fn listZmoduAds(ctx: *zigmodu.http_server.Context) !void {
        const self: *AdApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[ad] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[ad] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduAds(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduAd(ctx: *zigmodu.http_server.Context) !void {
        const self: *AdApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[ad] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[ad] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduAd(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduAd(ctx: *zigmodu.http_server.Context) !void {
        const self: *AdApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAd) catch {
            std.log.warn("[ad] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduAd(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduAd(ctx: *zigmodu.http_server.Context) !void {
        const self: *AdApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAd) catch {
            std.log.warn("[ad] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduAd(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduAd(ctx: *zigmodu.http_server.Context) !void {
        const self: *AdApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[ad] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduAd(id);
        try ctx.json(204, "");
    }

    fn listZmoduAdCategorys(ctx: *zigmodu.http_server.Context) !void {
        const self: *AdApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[ad] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[ad] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduAdCategorys(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduAdCategory(ctx: *zigmodu.http_server.Context) !void {
        const self: *AdApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[ad] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[ad] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduAdCategory(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduAdCategory(ctx: *zigmodu.http_server.Context) !void {
        const self: *AdApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAdCategory) catch {
            std.log.warn("[ad] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduAdCategory(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduAdCategory(ctx: *zigmodu.http_server.Context) !void {
        const self: *AdApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduAdCategory) catch {
            std.log.warn("[ad] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduAdCategory(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduAdCategory(ctx: *zigmodu.http_server.Context) !void {
        const self: *AdApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[ad] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduAdCategory(id);
        try ctx.json(204, "");
    }

};
