//! Auto-generated service for module: category
//! Generated by zmodu orm — templates: tools/zmodu/src/templates/orm/sqlx/
//!
//! ╔═══════════════════════════════════════════════════════════╗
//! ║  AI Metadata: module=category | layer=service      ║
//! ║  role=CRUD delegation | extends=service_ext.zig           ║
//! ╚═══════════════════════════════════════════════════════════╝

const std = @import("std");
const zigmodu = @import("zigmodu");
const model = @import("model.zig");
const persistence = @import("persistence.zig");

pub const CategoryService = struct {
    persistence: *persistence.CategoryPersistence,
    // EventBus — uncomment to enable event-driven communication:
    // event_bus: zigmodu.TypedEventBus(CategoryEvent),

    pub fn init(persistence_ptr: *persistence.CategoryPersistence) CategoryService {
        return .{ .persistence = persistence_ptr };
    }

    // Event hooks (uncomment and define event types to enable):
    // fn afterCreate(self: *CategoryService, entity: anytype) !void {
    //     try self.event_bus.publish(.{ .id = entity.id });
    // }
    // fn afterUpdate(self: *CategoryService, entity: anytype) !void {
    //     try self.event_bus.publish(.{ .id = entity.id });
    // }
    // fn afterDelete(self: *CategoryService, id: i64) !void {
    //     try self.event_bus.publish(.{ .id = id });
    // }

    pub fn listZmoduCategorys(self: *CategoryService, page: usize, size: usize) !zigmodu.orm.PageResult(model.ZmoduCategory) {
        var repo = self.persistence.zmoduCategoryRepo();
        return try repo.findPage(page, size);
    }

    pub fn getZmoduCategory(self: *CategoryService, id: i64) !?model.ZmoduCategory {
        var repo = self.persistence.zmoduCategoryRepo();
        return try repo.findById(id);
    }

    pub fn createZmoduCategory(self: *CategoryService, entity: model.ZmoduCategory) !model.ZmoduCategory {
        var repo = self.persistence.zmoduCategoryRepo();
        return try repo.insert(entity);
    }

    pub fn updateZmoduCategory(self: *CategoryService, entity: model.ZmoduCategory) !void {
        var repo = self.persistence.zmoduCategoryRepo();
        return try repo.update(entity);
    }

    pub fn deleteZmoduCategory(self: *CategoryService, id: i64) !void {
        var repo = self.persistence.zmoduCategoryRepo();
        return try repo.delete(id);
    }

};
