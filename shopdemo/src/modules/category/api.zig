//! Auto-generated HTTP API for module: category
//! Generated by zmodu orm — uses `zigmodu.http_server` + `ctx.bindJson` / `ctx.jsonStruct`.

const std = @import("std");
const zigmodu = @import("zigmodu");
const service = @import("service.zig");
const model = @import("model.zig");

pub const CategoryApi = struct {
    service: *service.CategoryService,

    pub fn init(service_ptr: *service.CategoryService) CategoryApi {
        return .{ .service = service_ptr };
    }

    pub fn registerRoutes(self: *CategoryApi, group: *zigmodu.http_server.RouteGroup) !void {
        try group.get("/zmodu_categorys", listZmoduCategorys, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_categorys/:id", getZmoduCategory, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_categorys", createZmoduCategory, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_categorys/:id", updateZmoduCategory, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_categorys/:id", deleteZmoduCategory, @ptrCast(@alignCast(self)));
    }

    fn listZmoduCategorys(ctx: *zigmodu.http_server.Context) !void {
        const self: *CategoryApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[category] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[category] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduCategorys(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduCategory(ctx: *zigmodu.http_server.Context) !void {
        const self: *CategoryApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[category] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[category] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduCategory(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduCategory(ctx: *zigmodu.http_server.Context) !void {
        const self: *CategoryApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduCategory) catch {
            std.log.warn("[category] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduCategory(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduCategory(ctx: *zigmodu.http_server.Context) !void {
        const self: *CategoryApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduCategory) catch {
            std.log.warn("[category] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduCategory(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduCategory(ctx: *zigmodu.http_server.Context) !void {
        const self: *CategoryApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[category] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduCategory(id);
        try ctx.json(204, "");
    }

};
