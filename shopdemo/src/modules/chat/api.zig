//! Auto-generated HTTP API for module: chat
//! Generated by zmodu orm — uses `zigmodu.http_server` + `ctx.bindJson` / `ctx.jsonStruct`.

const std = @import("std");
const zigmodu = @import("zigmodu");
const service = @import("service.zig");
const model = @import("model.zig");

pub const ChatApi = struct {
    service: *service.ChatService,

    pub fn init(service_ptr: *service.ChatService) ChatApi {
        return .{ .service = service_ptr };
    }

    pub fn registerRoutes(self: *ChatApi, group: *zigmodu.http_server.RouteGroup) !void {
        try group.get("/zmodu_chats", listZmoduChats, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_chats/:id", getZmoduChat, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_chats", createZmoduChat, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_chats/:id", updateZmoduChat, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_chats/:id", deleteZmoduChat, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_chat_relations", listZmoduChatRelations, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_chat_relations/:id", getZmoduChatRelation, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_chat_relations", createZmoduChatRelation, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_chat_relations/:id", updateZmoduChatRelation, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_chat_relations/:id", deleteZmoduChatRelation, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_chat_users", listZmoduChatUsers, @ptrCast(@alignCast(self)));
        try group.get("/zmodu_chat_users/:id", getZmoduChatUser, @ptrCast(@alignCast(self)));
        try group.post("/zmodu_chat_users", createZmoduChatUser, @ptrCast(@alignCast(self)));
        try group.put("/zmodu_chat_users/:id", updateZmoduChatUser, @ptrCast(@alignCast(self)));
        try group.delete("/zmodu_chat_users/:id", deleteZmoduChatUser, @ptrCast(@alignCast(self)));
    }

    fn listZmoduChats(ctx: *zigmodu.http_server.Context) !void {
        const self: *ChatApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[chat] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[chat] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduChats(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduChat(ctx: *zigmodu.http_server.Context) !void {
        const self: *ChatApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[chat] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[chat] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduChat(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduChat(ctx: *zigmodu.http_server.Context) !void {
        const self: *ChatApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduChat) catch {
            std.log.warn("[chat] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduChat(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduChat(ctx: *zigmodu.http_server.Context) !void {
        const self: *ChatApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduChat) catch {
            std.log.warn("[chat] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduChat(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduChat(ctx: *zigmodu.http_server.Context) !void {
        const self: *ChatApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[chat] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduChat(id);
        try ctx.json(204, "");
    }

    fn listZmoduChatRelations(ctx: *zigmodu.http_server.Context) !void {
        const self: *ChatApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[chat] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[chat] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduChatRelations(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduChatRelation(ctx: *zigmodu.http_server.Context) !void {
        const self: *ChatApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[chat] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[chat] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduChatRelation(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduChatRelation(ctx: *zigmodu.http_server.Context) !void {
        const self: *ChatApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduChatRelation) catch {
            std.log.warn("[chat] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduChatRelation(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduChatRelation(ctx: *zigmodu.http_server.Context) !void {
        const self: *ChatApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduChatRelation) catch {
            std.log.warn("[chat] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduChatRelation(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduChatRelation(ctx: *zigmodu.http_server.Context) !void {
        const self: *ChatApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[chat] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduChatRelation(id);
        try ctx.json(204, "");
    }

    fn listZmoduChatUsers(ctx: *zigmodu.http_server.Context) !void {
        const self: *ChatApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const page_str = ctx.query.get("page") orelse "0";
        const size_str = ctx.query.get("size") orelse "10";
        const page = std.fmt.parseInt(usize, page_str, 10) catch {
            std.log.warn("[chat] list: invalid page", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid page");
            return;
        };
        const size = std.fmt.parseInt(usize, size_str, 10) catch {
            std.log.warn("[chat] list: invalid size", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid size");
            return;
        };
        const result = try self.service.listZmoduChatUsers(page, size);
        try ctx.jsonStruct(200, result);
    }

    fn getZmoduChatUser(ctx: *zigmodu.http_server.Context) !void {
        const self: *ChatApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[chat] get: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "missing id");
            return;
        };
        const id = std.fmt.parseInt(i64, id_str, 10) catch {
            std.log.warn("[chat] get: invalid id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        const entity = try self.service.getZmoduChatUser(id);
        try ctx.jsonStruct(200, entity);
    }

    fn createZmoduChatUser(ctx: *zigmodu.http_server.Context) !void {
        const self: *ChatApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduChatUser) catch {
            std.log.warn("[chat] create: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        const created = try self.service.createZmoduChatUser(entity);
        try ctx.jsonStruct(201, created);
    }

    fn updateZmoduChatUser(ctx: *zigmodu.http_server.Context) !void {
        const self: *ChatApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const entity = ctx.bindJson(model.ZmoduChatUser) catch {
            std.log.warn("[chat] update: invalid body", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid body");
            return;
        };
        try self.service.updateZmoduChatUser(entity);
        try ctx.jsonStruct(200, entity);
    }

    fn deleteZmoduChatUser(ctx: *zigmodu.http_server.Context) !void {
        const self: *ChatApi = @ptrCast(@alignCast(ctx.user_data orelse return error.InternalError));
        const id_str = ctx.params.get("id") orelse {
            std.log.warn("[chat] delete: missing id", .{});
            try ctx.sendErrorResponse(400, @intFromEnum(zigmodu.HttpCode.BadRequest), "invalid id");
            return;
        };
        try self.service.deleteZmoduChatUser(id);
        try ctx.json(204, "");
    }

};
